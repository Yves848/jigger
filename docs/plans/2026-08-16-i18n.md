# Internationalisation de jigger — plan d'implémentation

> **Pour un agent :** SOUS-SKILL REQUIS — utiliser `superpowers:subagent-driven-development`
> (recommandé) ou `superpowers:executing-plans` pour exécuter ce plan tâche par tâche. Les
> étapes sont des cases à cocher (`- [ ]`).

**But :** rendre jigger multilingue (anglais et français), et faire de l'anglais sa langue
de publication.

**Architecture :** un paquet `internal/i18n` porte un catalogue unique `clé → [EN, FR]` dans
le code, une résolution de langue lue une fois au démarrage, et deux fonctions d'accès
(`T`, `Tf`). Chaque surface visible migre vers ce catalogue, une tâche par surface. Les
greffons gardent leurs six chaînes en propre : leurs messages s'affichent précisément quand
le binaire est absent ou trop vieux. La documentation bascule en dernier, ses captures
d'écran devant être régénérées en anglais.

**Pile technique :** Go 1.24 (stdlib seule, aucune dépendance nouvelle), zsh, PowerShell 7.

**Spec :** [`docs/specs/2026-08-16-i18n-design.md`](../specs/2026-08-16-i18n-design.md)

## Contraintes globales

Elles s'appliquent à **toutes** les tâches.

- **Aucune dépendance nouvelle.** `go.mod` et `go.sum` ne changent pas.
- **Commentaires, messages de commit et noms de tests en français.** Le code reste
  francophone ; seules les chaînes destinées à l'utilisateur deviennent bilingues.
- **TDD** : le test d'abord, qu'on regarde échouer, puis le code minimal.
- **Un commit par tâche**, message en français, style nominal (cf. `git log --oneline`).
- **Ne jamais traduire** : la première ligne de `jigger render` (`count=`, `sel=`, `exec=`,
  `left=`), les clés JSON (`name`, `version`, `available`, `kind`, `source`, `pm`), les noms
  de verbes, de gestionnaires et d'options (`install`, `--pm`, `brew`, `formula`, `cask`,
  `tap`, `bucket`), et les erreurs d'invariant interne (« liaison sans Native, Build ni
  Direct », « Direct rend déjà des Package : Parse est de trop »).
- **Ne jamais traduire non plus les erreurs sentinelles**, qui sont comparées et non lues :
  `internal/prompt/status.go:34` (`ErrVerrouille` = « rafraîchissement déjà en cours ») et
  `internal/prompt/status.go:243` (« ni winget ni scoop : %w »). Les traduire casserait les
  comparaisons `errors.Is`.
- **Le français ne bouge pas d'un octet.** À `JIGGER_LANG=fr`, la sortie de `jigger render`
  doit rester identique à celle d'avant le chantier — c'est ce que vérifie la tâche 2, et
  chaque tâche suivante la rejoue.
- **Tout test qui assère une chaîne visible pose `JIGGER_LANG`.** Sans cela il passe ou
  échoue selon la locale de la machine.
- `make test-all` doit passer à la fin de chaque tâche.

---

### Tâche 1 : le paquet `internal/i18n`

Le socle. Aucune chaîne ne migre encore : cette tâche livre la mécanique et ses tests.

**Fichiers :**
- Créer : `internal/i18n/i18n.go` — résolution de la langue, `T`, `Tf`, replis
- Créer : `internal/i18n/catalogue.go` — la table, vide à ce stade sauf deux entrées de test
- Créer : `internal/i18n/langue_unix.go` — culture système hors Windows
- Créer : `internal/i18n/langue_windows.go` — culture système sous Windows
- Test : `internal/i18n/i18n_test.go`

**Interfaces :**
- Consomme : rien.
- Produit : `i18n.T(cle string) string`, `i18n.Tf(cle string, args ...any) string`,
  `i18n.Recharger()` (relit l'environnement — réservé aux tests),
  `i18n.Courante() Langue`, les constantes `i18n.EN` et `i18n.FR`, et la table
  `catalogue map[string][nbLangues]string` (non exportée, complétée par les tâches
  suivantes).

- [ ] **Étape 1 : écrire les tests qui échouent**

```go
// internal/i18n/i18n_test.go
package i18n

import "testing"

// Le catalogue de test remplace le vrai le temps d'un test, et le rend ensuite.
func avecCatalogue(t *testing.T, table map[string][nbLangues]string) {
	t.Helper()
	ancien := catalogue
	catalogue = table
	t.Cleanup(func() { catalogue = ancien })
}

func TestTRendLaLangueCourante(t *testing.T) {
	avecCatalogue(t, map[string][nbLangues]string{
		"essai.bonjour": {"hello", "bonjour"},
	})
	t.Setenv("JIGGER_LANG", "fr")
	Recharger()
	if got := T("essai.bonjour"); got != "bonjour" {
		t.Fatalf("fr : %q, attendu « bonjour »", got)
	}
	t.Setenv("JIGGER_LANG", "en")
	Recharger()
	if got := T("essai.bonjour"); got != "hello" {
		t.Fatalf("en : %q, attendu « hello »", got)
	}
}

// Une clé oubliée doit se voir à l'œil plutôt que laisser un trou.
func TestCleAbsenteRendueTelleQuelle(t *testing.T) {
	avecCatalogue(t, map[string][nbLangues]string{})
	t.Setenv("JIGGER_LANG", "fr")
	Recharger()
	if got := T("popup.inconnue"); got != "popup.inconnue" {
		t.Fatalf("%q, attendu la clé elle-même", got)
	}
}

// C'est ce repli qui permettra de livrer une langue partiellement traduite.
func TestTraductionVideSeReplieSurAnglais(t *testing.T) {
	avecCatalogue(t, map[string][nbLangues]string{
		"essai.partiel": {"only english", ""},
	})
	t.Setenv("JIGGER_LANG", "fr")
	Recharger()
	if got := T("essai.partiel"); got != "only english" {
		t.Fatalf("%q, attendu le repli anglais", got)
	}
}

func TestTfFormate(t *testing.T) {
	avecCatalogue(t, map[string][nbLangues]string{
		"essai.compte": {"%d packages", "%d paquets"},
	})
	t.Setenv("JIGGER_LANG", "fr")
	Recharger()
	if got := Tf("essai.compte", 3); got != "3 paquets" {
		t.Fatalf("%q, attendu « 3 paquets »", got)
	}
}

// L'ordre de résolution : JIGGER_LANG l'emporte sur LANG.
func TestJiggerLangPrimeSurLang(t *testing.T) {
	t.Setenv("LANG", "fr_FR.UTF-8")
	t.Setenv("JIGGER_LANG", "en")
	Recharger()
	if Courante() != EN {
		t.Fatalf("JIGGER_LANG doit primer sur LANG")
	}
}

func TestLangDonneLaLangue(t *testing.T) {
	t.Setenv("JIGGER_LANG", "")
	t.Setenv("LC_ALL", "")
	t.Setenv("LC_MESSAGES", "")
	t.Setenv("LANG", "fr_FR.UTF-8")
	Recharger()
	if Courante() != FR {
		t.Fatalf("LANG=fr_FR.UTF-8 doit donner le français")
	}
}

// Une langue que jigger ne connaît pas n'est pas une erreur : elle retombe sur l'anglais.
func TestLangueInconnueRetombeSurAnglais(t *testing.T) {
	t.Setenv("JIGGER_LANG", "ja")
	Recharger()
	if Courante() != EN {
		t.Fatalf("une langue inconnue doit retomber sur l'anglais")
	}
}
```

- [ ] **Étape 2 : lancer les tests pour les voir échouer**

Lancer : `go test ./internal/i18n/`
Attendu : ÉCHEC — le paquet n'existe pas.

- [ ] **Étape 3 : écrire le paquet**

```go
// internal/i18n/i18n.go

// Les chaînes que jigger affiche à ses utilisateurs, dans la langue de leur système.
//
// Un catalogue unique (catalogue.go) porte toutes les langues côte à côte : la dérive de
// clés — un fichier de traduction qui oublie une entrée — devient impossible par
// construction. Le tableau est de taille fixe : le jour où une troisième langue arrive,
// nbLangues change et le compilateur désigne chaque ligne à compléter.
//
// Deux replis, et aucune page blanche : une clé absente est rendue telle quelle (donc
// visible à l'œil dès le premier essai), une traduction vide se replie sur l'anglais.
package i18n

import (
	"fmt"
	"os"
	"strings"
)

// Langue est l'index d'une colonne du catalogue.
type Langue int

const (
	EN Langue = iota
	FR
	nbLangues
)

// courante est résolue une fois, au chargement du paquet : un processus qui vit huit
// millisecondes n'a pas besoin d'un changement de langue à chaud.
var courante = resoudre()

// Courante rend la langue effectivement retenue.
func Courante() Langue { return courante }

// Recharger relit l'environnement. Réservé aux tests : rien en production ne change de
// langue en cours d'exécution.
func Recharger() { courante = resoudre() }

// T rend la traduction de la clé dans la langue courante.
func T(cle string) string {
	trad, ok := catalogue[cle]
	if !ok {
		return cle // clé oubliée : visible, plutôt qu'un trou
	}
	if s := trad[courante]; s != "" {
		return s
	}
	return trad[EN]
}

// Tf formate la traduction avec des paramètres.
func Tf(cle string, args ...any) string { return fmt.Sprintf(T(cle), args...) }

// resoudre applique l'ordre de la spec : JIGGER_LANG, puis les variables POSIX, puis la
// culture du système, puis l'anglais.
func resoudre() Langue {
	if l, ok := depuisCode(os.Getenv("JIGGER_LANG")); ok {
		return l
	}
	for _, v := range []string{"LC_ALL", "LC_MESSAGES", "LANG"} {
		if l, ok := depuisCode(os.Getenv(v)); ok {
			return l
		}
	}
	if l, ok := depuisCode(cultureSysteme()); ok {
		return l
	}
	return EN
}

// depuisCode lit un code de locale — « fr », « fr_FR.UTF-8 », « fr-FR » — et rend la
// langue correspondante. Le deuxième retour dit si le code était reconnu : un code vide
// ou inconnu laisse la résolution continuer.
func depuisCode(code string) (Langue, bool) {
	code = strings.TrimSpace(code)
	if code == "" {
		return EN, false
	}
	code = strings.ToLower(code)
	if i := strings.IndexAny(code, "_-."); i > 0 {
		code = code[:i]
	}
	switch code {
	case "en":
		return EN, true
	case "fr":
		return FR, true
	}
	return EN, false
}
```

```go
// internal/i18n/langue_unix.go
//go:build !windows

package i18n

// cultureSysteme n'a rien à ajouter hors Windows : les variables POSIX ont déjà été
// consultées par resoudre.
func cultureSysteme() string { return "" }
```

```go
// internal/i18n/langue_windows.go
//go:build windows

package i18n

import (
	"syscall"
	"unsafe"
)

// cultureSysteme rend la langue de l'interface utilisateur Windows (« fr-FR »), là où les
// variables POSIX n'existent pas. Appel direct à l'API, sans dépendance ajoutée — comme
// le fait déjà internal/pm/proc_windows.go.
func cultureSysteme() string {
	const tailleMax = 85 // LOCALE_NAME_MAX_LENGTH
	kernel32 := syscall.NewLazyDLL("kernel32.dll")
	proc := kernel32.NewProc("GetUserDefaultLocaleName")

	buf := make([]uint16, tailleMax)
	n, _, _ := proc.Call(uintptr(unsafe.Pointer(&buf[0])), uintptr(tailleMax))
	if n <= 1 {
		return ""
	}
	return syscall.UTF16ToString(buf[:n-1]) // n compte le zéro final
}
```

```go
// internal/i18n/catalogue.go

// Le catalogue des chaînes visibles, une ligne par clé, toutes les langues côte à côte.
//
//	{EN, FR}
//
// L'anglais est l'original : il n'est jamais vide. Le français peut l'être — le repli joue.
// Les clés sont préfixées par la surface où la chaîne s'affiche (popup, facade, cli), pour
// qu'une relecture de traduction soit possible sans lire le code.
package i18n

var catalogue = map[string][nbLangues]string{}
```

- [ ] **Étape 4 : lancer les tests pour les voir passer**

Lancer : `go test ./internal/i18n/ -v`
Attendu : les sept tests passent.

- [ ] **Étape 5 : vérifier la compilation Windows**

Lancer : `GOOS=windows GOARCH=amd64 go build ./... && GOOS=windows GOARCH=amd64 go vet ./...`
Attendu : aucune sortie.

- [ ] **Étape 6 : commit**

```bash
git add internal/i18n
git commit -m "Paquet i18n : catalogue unique, résolution de la langue et replis"
```

---

### Tâche 2 : le banc de non-régression de `render`

À faire **avant** toute migration : il capture la sortie de référence pendant qu'elle est
encore celle d'aujourd'hui. Sans lui, « le français n'a pas bougé » resterait une opinion.

**Fichiers :**
- Créer : `tests/render-golden.sh`
- Créer : `tests/golden/render-fr.txt` (généré, commité)

**Interfaces :**
- Consomme : le binaire construit par `make build`.
- Produit : `tests/render-golden.sh --capturer` (écrit la référence) et
  `tests/render-golden.sh --verifier` (compare, code 1 en cas d'écart).

- [ ] **Étape 1 : écrire le script**

```sh
#!/bin/sh
# Banc de non-régression de `jigger render`.
#
#   tests/render-golden.sh --capturer   # écrit la référence (avant le chantier)
#   tests/render-golden.sh --verifier   # compare la sortie courante à la référence
#
# La sortie du popup est ce que le greffon lit à chaque frappe : elle ne doit pas bouger
# d'un octet tant qu'on reste en français. Le banc balaie 480 combinaisons — 12 lignes,
# 5 largeurs, 4 profils de couleur, avec et sans focus.
set -eu

racine=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
reference="$racine/tests/golden/render-fr.txt"
binaire="$racine/jigger"

[ -x "$binaire" ] || { echo "compile d'abord : make build" >&2; exit 1; }

engendrer() {
  for ligne in "brew" "brew inst" "brew install fire" "brew uninstall " \
               "brew list --" "winget install Git." "scoop uninstall 7z" \
               "jg" "jg inst" "jg install fire" "jg source " "git commit"; do
    for cols in 30 48 58 80 120; do
      for couleur in never 16 256 truecolor; do
        for focus in true false; do
          printf '### %s | %s | %s | %s\n' "$ligne" "$cols" "$couleur" "$focus"
          JIGGER_LANG=fr "$binaire" render --line "$ligne" --cols "$cols" \
            --color "$couleur" --focus="$focus" --rows 8 2>&1 || true
        done
      done
    done
  done
}

case "${1:---verifier}" in
  --capturer)
    mkdir -p "$(dirname "$reference")"
    engendrer > "$reference"
    echo "référence écrite : $reference ($(wc -l < "$reference" | tr -d ' ') lignes)"
    ;;
  --verifier)
    [ -f "$reference" ] || { echo "référence absente : lance --capturer" >&2; exit 1; }
    if engendrer | diff -u "$reference" - > /tmp/jigger-golden.diff; then
      echo "aucune différence (480 combinaisons)"
    else
      echo "ÉCART détecté :" >&2
      head -40 /tmp/jigger-golden.diff >&2
      exit 1
    fi
    ;;
  *) echo "usage: $0 [--capturer|--verifier]" >&2; exit 2 ;;
esac
```

- [ ] **Étape 2 : rendre exécutable, capturer la référence**

```bash
chmod +x tests/render-golden.sh
make build
./tests/render-golden.sh --capturer
```

Attendu : « référence écrite », plusieurs milliers de lignes.

- [ ] **Étape 3 : vérifier que le banc détecte bien un écart**

```bash
./tests/render-golden.sh --verifier          # doit dire « aucune différence »
sed -i '' 's/insérer/INSÉRER/' internal/ui/picker.go && make build
./tests/render-golden.sh --verifier          # doit ÉCHOUER, code 1
git checkout -- internal/ui/picker.go && make build
```

Attendu : la deuxième vérification échoue et montre le diff. **Un banc qu'on n'a pas vu
échouer ne prouve rien.**

- [ ] **Étape 4 : brancher le banc dans le Makefile**

Dans `Makefile`, ajouter la cible et l'ajouter à `test-all` (côté Unix uniquement) :

```make
# Le popup ne doit pas bouger d'un octet en français (cf. docs/plans/2026-08-16-i18n.md).
test-golden: build
	./tests/render-golden.sh --verifier
```

et remplacer `TEST_SHELL := test-shell` par `TEST_SHELL := test-shell test-golden`.

- [ ] **Étape 5 : lancer la suite complète**

Lancer : `make test-all`
Attendu : tout passe, dont « aucune différence (480 combinaisons) ».

- [ ] **Étape 6 : commit**

```bash
git add tests/render-golden.sh tests/golden/render-fr.txt Makefile
git commit -m "Banc de non-régression du rendu, capturé avant l'i18n"
```

---

### Tâche 3 : le popup

Première migration. Douze chaînes, toutes dans le chemin du rendu.

**Fichiers :**
- Modifier : `internal/i18n/catalogue.go` (ajout des clés `popup.*`)
- Modifier : `internal/ui/picker.go:124,226,228,230,239`
- Modifier : `main.go:224,362,364,373-377,385,391`
- Modifier : `internal/brew/catalog.go:147`
- Modifier : `internal/winget/winget.go:175`
- Test : `internal/i18n/catalogue_test.go` (créer)

**Interfaces :**
- Consomme : `i18n.T`, `i18n.Tf`, `i18n.Recharger` (tâche 1).
- Produit : les clés `popup.insert`, `popup.execute`, `popup.navigate`, `popup.browse`,
  `popup.close`, `popup.cancel`, `popup.choose`, `popup.filter`, `popup.empty`,
  `popup.filter_hint`, `popup.catalog_brew`, `popup.catalog_winget`.

- [ ] **Étape 1 : écrire le test du catalogue**

```go
// internal/i18n/catalogue_test.go
package i18n

import "testing"

// L'anglais est l'original : une entrée vide n'aurait rien sur quoi se replier.
func TestAucuneEntreeAnglaiseVide(t *testing.T) {
	for cle, trad := range catalogue {
		if trad[EN] == "" {
			t.Errorf("%s : l'anglais est vide", cle)
		}
	}
}

// Les clés du popup existent et sont traduites dans les deux langues.
func TestClesDuPopup(t *testing.T) {
	for _, cle := range []string{
		"popup.insert", "popup.execute", "popup.navigate", "popup.browse",
		"popup.close", "popup.cancel", "popup.choose", "popup.filter",
		"popup.empty", "popup.filter_hint", "popup.catalog_brew", "popup.catalog_winget",
	} {
		trad, ok := catalogue[cle]
		if !ok {
			t.Errorf("%s : absente du catalogue", cle)
			continue
		}
		if trad[FR] == "" {
			t.Errorf("%s : le français est vide", cle)
		}
	}
}

// Le français d'aujourd'hui, mot pour mot : c'est lui que le banc de la tâche 2 compare.
func TestLibellesFrancaisInchanges(t *testing.T) {
	t.Setenv("JIGGER_LANG", "fr")
	Recharger()
	for cle, attendu := range map[string]string{
		"popup.insert":   "insérer",
		"popup.execute":  "exécuter",
		"popup.navigate": "naviguer",
		"popup.browse":   "parcourir",
		"popup.close":    "fermer",
		"popup.cancel":   "annuler",
		"popup.choose":   "choisir",
		"popup.filter":   "filtrer…",
		"popup.empty":    "aucun candidat",
	} {
		if got := T(cle); got != attendu {
			t.Errorf("%s : %q, attendu %q", cle, got, attendu)
		}
	}
}
```

- [ ] **Étape 2 : lancer le test pour le voir échouer**

Lancer : `go test ./internal/i18n/ -run 'Popup|Francais|Anglaise' -v`
Attendu : ÉCHEC — les clés sont absentes du catalogue.

- [ ] **Étape 3 : remplir le catalogue**

Dans `internal/i18n/catalogue.go`, entre les accolades de `catalogue` :

```go
	// ── Popup ─────────────────────────────────────────────────────────────────────────
	"popup.insert":   {"insert", "insérer"},
	"popup.execute":  {"execute", "exécuter"},
	"popup.navigate": {"navigate", "naviguer"},
	"popup.browse":   {"browse", "parcourir"},
	"popup.close":    {"close", "fermer"},
	"popup.cancel":   {"cancel", "annuler"},
	"popup.choose":   {"choose", "choisir"},
	"popup.filter":   {"filter…", "filtrer…"},
	"popup.empty":    {"no match", "aucun candidat"},
	// %d est le nombre de paquets du catalogue.
	"popup.filter_hint":    {"type to filter… (%d packages)", "tapez pour filtrer… (%d paquets)"},
	"popup.catalog_brew":   {"building the Homebrew catalog…", "catalogue Homebrew en préparation…"},
	"popup.catalog_winget": {"building the winget catalog…", "catalogue winget en préparation…"},
```

- [ ] **Étape 4 : brancher les appelants**

`internal/ui/picker.go` — remplacer les littéraux :

```go
	ti.Placeholder = i18n.T("popup.filter")                                   // ligne 124
	keys = []Key{{"⇥", i18n.T("popup.insert")}}                               // ligne 226
	keys = append(keys, Key{"↩", i18n.T("popup.execute")})                    // ligne 228
	keys = append(keys, Key{"↑↓", i18n.T("popup.navigate")}, Key{"esc", i18n.T("popup.cancel")}) // ligne 230
	Empty: i18n.T("popup.empty"),                                             // ligne 239
```

`main.go` :

```go
	// ligne 224
	model.Keys = []ui.Key{{Key: "↵", Label: i18n.T("popup.choose")}, {Key: "^G", Label: i18n.T("popup.cancel")}}

	// lignes 362-364
	navigation := ui.Key{Key: "↓", Label: i18n.T("popup.browse")}
	if *focus {
		navigation = ui.Key{Key: "↑↓", Label: i18n.T("popup.navigate")}
	}

	// lignes 373-377
		Keys: []ui.Key{
			{Key: "⇥", Label: i18n.T("popup.insert")},
			navigation,
			{Key: "^G", Label: i18n.T("popup.close")},
		},

	// ligne 385
		frame.Empty = i18n.Tf("popup.filter_hint", len(res.Items))
```

`internal/brew/catalog.go:147` → `cat.Note = i18n.T("popup.catalog_brew")`
`internal/winget/winget.go:175` → `cat.Note = i18n.T("popup.catalog_winget")`

Ajouter l'import `"gitlab.yg-devworks.com/yves/jigger/internal/i18n"` dans chaque fichier
modifié.

- [ ] **Étape 5 : lancer les tests et le banc**

```bash
go test ./... && make build && ./tests/render-golden.sh --verifier
```

Attendu : tests verts, et **« aucune différence (480 combinaisons) »** — le français n'a pas
bougé.

- [ ] **Étape 6 : vérifier l'anglais à l'œil**

```bash
JIGGER_LANG=en ./jigger render --line "brew install fire" --cols 56 --color never | tail -n +2
```

Attendu : le pied affiche `⇥ insert`, `↓ browse`, `^G close`.

- [ ] **Étape 7 : commit**

```bash
git add internal/i18n internal/ui/picker.go main.go internal/brew/catalog.go internal/winget/winget.go
git commit -m "Popup : les libellés passent par le catalogue"
```

---

### Tâche 4 : la façade

Les messages d'erreur et le tableau vide. Ce sont les chaînes les plus lues par un
utilisateur qui se trompe — donc celles qui comptent le plus.

**Fichiers :**
- Modifier : `internal/i18n/catalogue.go`
- Modifier : `internal/facade/verbe.go:28,83,86`
- Modifier : `internal/facade/routage.go:173,176,244` et le message « Proche / --pm »
- Modifier : `internal/facade/executer.go:79,97,100,108,125`
- Modifier : `internal/facade/format.go:27` et les en-têtes `49-60`
- Test : `internal/i18n/catalogue_test.go` (compléter)

**Interfaces :**
- Consomme : `i18n.T`, `i18n.Tf`.
- Produit : les clés `facade.*` et `table.*`.

- [ ] **Étape 1 : compléter le test des clés**

Ajouter dans `internal/i18n/catalogue_test.go` :

```go
func TestClesDeLaFacade(t *testing.T) {
	for _, cle := range []string{
		"facade.no_verb", "facade.unknown_verb", "facade.nobody_can",
		"facade.unknown_pm", "facade.pm_unavailable", "facade.unknown_name",
		"facade.near", "facade.too_recent", "facade.ambiguous", "facade.choose_pm",
		"facade.failed", "facade.unreadable", "facade.no_parser", "facade.nothing",
		"table.package", "table.current", "table.available", "table.source", "table.pm",
	} {
		trad, ok := catalogue[cle]
		if !ok {
			t.Errorf("%s : absente du catalogue", cle)
			continue
		}
		if trad[FR] == "" {
			t.Errorf("%s : le français est vide", cle)
		}
	}
}

// Les en-têtes de tableaux sont traduits — c'est sans risque parce que --json existe.
func TestEntetesTraduits(t *testing.T) {
	t.Setenv("JIGGER_LANG", "en")
	Recharger()
	if got := T("table.package"); got != "PACKAGE" {
		t.Fatalf("en-tête anglais : %q", got)
	}
	t.Setenv("JIGGER_LANG", "fr")
	Recharger()
	if got := T("table.package"); got != "PAQUET" {
		t.Fatalf("en-tête français : %q", got)
	}
}
```

- [ ] **Étape 2 : lancer pour voir échouer**

Lancer : `go test ./internal/i18n/ -run 'Facade|Entetes' -v`
Attendu : ÉCHEC — clés absentes.

- [ ] **Étape 3 : remplir le catalogue**

```go
	// ── Façade ────────────────────────────────────────────────────────────────────────
	"facade.no_verb": {
		"jigger: no verb. Try « jg install <package> » or « jg outdated »",
		"jigger : aucun verbe. Essaie « jg install <paquet> » ou « jg outdated »",
	},
	"facade.unknown_verb": {
		"jigger: « %s » — unknown verb. « jg ⇥ » lists what jigger can do",
		"jigger : « %s » — verbe inconnu. « jg ⇥ » liste ce que jigger sait faire",
	},
	"facade.nobody_can": {
		"jigger: « %s » — no available manager can do that.\n        %s, but it is not installed",
		"jigger : « %s » — aucun gestionnaire disponible ne sait faire ça.\n        %s, mais n'est pas installé",
	},
	"facade.unknown_pm": {
		"jigger: --pm %s — unknown manager. Known: %s",
		"jigger : --pm %s — gestionnaire inconnu de jigger. Connus : %s",
	},
	"facade.pm_unavailable": {
		"jigger: --pm %s — manager unavailable for this verb. Available: %s",
		"jigger : --pm %s — gestionnaire indisponible pour ce verbe. Disponibles : %s",
	},
	"facade.unknown_name": {"jigger: « %s » — unknown to %s", "jigger : « %s » — inconnu de %s"},
	"facade.near":         {"\n        Close: ", "\n        Proche : "},
	"facade.too_recent": {
		"\n        If the package is too recent for the catalog: jg … --pm %s %s",
		"\n        Si le paquet est trop récent pour le catalogue : jg … --pm %s %s",
	},
	"facade.ambiguous": {
		"jigger: « %s » — known to several managers:\n",
		"jigger : « %s » — connu de plusieurs gestionnaires :\n",
	},
	"facade.choose_pm":  {"        Choose with --pm <manager>", "        Choisis avec --pm <gestionnaire>"},
	"facade.failed":     {"jigger (%s): failed\n", "jigger (%s) : échec\n"},
	"facade.unreadable": {"jigger (%s): unreadable output — %v\n", "jigger (%s) : sortie illisible — %v\n"},
	"facade.no_parser": {
		"jigger (%s): verb %q normalized without a declared parser — output ignored for safety\n",
		"jigger (%s) : verbe %q normalisé sans analyseur déclaré — sortie ignorée par sécurité\n",
	},
	"facade.nothing": {"nothing to report\n", "rien à signaler\n"},

	// ── En-têtes de tableaux ──────────────────────────────────────────────────────────
	// Traduits sans risque : --json sert ceux qui analysent.
	"table.package":   {"PACKAGE", "PAQUET"},
	"table.current":   {"CURRENT", "ACTUEL"},
	"table.available": {"AVAILABLE", "DISPO"},
	"table.source":    {"SOURCE", "SOURCE"},
	"table.pm":        {"PM", "PM"},
```

- [ ] **Étape 4 : brancher les appelants**

Le gabarit, sur `verbe.go:83` :

```go
// avant
return fmt.Errorf("jigger : « %s » — verbe inconnu. « jg ⇥ » liste ce que jigger sait faire", mot)

// après — Tf a déjà formaté, errors.New suffit et évite une double interprétation des %
return errors.New(i18n.Tf("facade.unknown_verb", mot))
```

Même transformation pour `verbe.go:28` (sans paramètre : `errors.New(i18n.T("facade.no_verb"))`),
`verbe.go:86`, `routage.go:173,176,244`, et pour les `fmt.Fprintf(os.Stderr, …)`
d'`executer.go:79,97,100,108,125` — qui deviennent
`fmt.Fprint(os.Stderr, i18n.Tf("facade.failed", cible.Mgr.Cmd()))`. Puis `format.go:27`
(`return i18n.T("facade.nothing")`) et les en-têtes :

```go
	entete := []string{i18n.T("table.package")}
	// … i18n.T("table.current"), i18n.T("table.available"), i18n.T("table.source"), i18n.T("table.pm")
```

- [ ] **Étape 5 : lancer les tests, corriger les tests de la façade**

Lancer : `go test ./...`
Attendu : `internal/facade` échoue — ses tests assèrent les messages français.
Corriger : ajouter `t.Setenv("JIGGER_LANG", "fr")` puis `i18n.Recharger()` en tête de chaque
test qui compare un message.

- [ ] **Étape 6 : relancer, puis le banc**

```bash
go test ./... && make build && ./tests/render-golden.sh --verifier
```

Attendu : tout vert, aucune différence.

- [ ] **Étape 7 : commit**

```bash
git add internal/i18n internal/facade
git commit -m "Façade : messages et en-têtes de tableaux passent par le catalogue"
```

---

### Tâche 5 : la ligne de commande

L'usage, les descriptions de drapeaux, et les deux messages restants de `main.go`.

**Fichiers :**
- Modifier : `internal/i18n/catalogue.go`
- Modifier : `main.go:104-106,210-214,249-250,272,287-289,311,346-351`
- Test : `internal/i18n/catalogue_test.go` (compléter)

**Interfaces :**
- Consomme : `i18n.T`, `i18n.Tf`.
- Produit : les clés `cli.*`.

- [ ] **Étape 1 : compléter le test des clés**

```go
func TestClesDuCli(t *testing.T) {
	for _, cle := range []string{
		"cli.usage1", "cli.usage2", "cli.usage3",
		"cli.flag_all", "cli.flag_installed", "cli.flag_refresh", "cli.flag_wait",
		"cli.flag_path", "cli.flag_line", "cli.flag_sel", "cli.flag_cols",
		"cli.flag_rows", "cli.flag_color", "cli.flag_focus", "cli.warm_failed",
	} {
		if _, ok := catalogue[cle]; !ok {
			t.Errorf("%s : absente du catalogue", cle)
		}
	}
}
```

- [ ] **Étape 2 : lancer pour voir échouer**

Lancer : `go test ./internal/i18n/ -run Cli -v`
Attendu : ÉCHEC.

- [ ] **Étape 3 : remplir le catalogue**

```go
	// ── Ligne de commande ─────────────────────────────────────────────────────────────
	"cli.usage1": {
		"usage: jigger <verb> [--pm <manager>] [--json] [--yes] [arguments…]",
		"usage: jigger <verbe> [--pm <gestionnaire>] [--json] [--yes] [arguments…]",
	},
	"cli.usage2": {
		"       jigger pick|complete \"<line>\" | jigger render --line \"<line>\"",
		"       jigger pick|complete \"<ligne>\" | jigger render --line \"<ligne>\"",
	},
	"cli.usage3": {
		"       jigger prompt [--refresh [--wait]|--path] | jigger warm [--all|--installed]",
		"       jigger prompt [--refresh [--wait]|--path] | jigger warm [--all|--installed]",
	},
	"cli.flag_all":       {"rebuild everything, even what is still fresh", "refait tout, même ce qui est encore frais"},
	"cli.flag_installed": {"rebuild only the installed-package lists", "refait les seules listes de paquets installés"},
	"cli.flag_refresh":   {"query the manager and rewrite the cache (slow)", "interroge brew et réécrit le cache (lent)"},
	"cli.flag_wait":      {"with --refresh: wait for the lock instead of giving up", "avec --refresh : attend le verrou au lieu de renoncer"},
	"cli.flag_path":      {"print the cache file path", "imprime le chemin du fichier de cache"},
	"cli.flag_line":      {"line to complete (up to the cursor)", "ligne à compléter (jusqu'au curseur)"},
	"cli.flag_sel":       {"index of the current candidate", "index du candidat courant"},
	"cli.flag_cols":      {"terminal width", "largeur du terminal"},
	"cli.flag_rows":      {"number of candidates shown", "nombre de candidats affichés"},
	"cli.flag_color":     {"color profile: auto|never|16|256|truecolor", "profil couleur : auto|never|16|256|truecolor"},
	"cli.flag_focus":     {"the popup owns the keyboard: arrows go to it", "le popup a le clavier : les flèches lui reviennent"},
	"cli.warm_failed":    {"jigger warm (%s): %v\n", "jigger warm (%s) : %v\n"},
	"cli.prompt_failed":  {"jigger prompt --refresh:", "jigger prompt --refresh :"},
```

`cli.prompt_failed` sert la ligne 311, où le préfixe est passé à `fmt.Fprintln` avant
l'erreur : `fmt.Fprintln(os.Stderr, i18n.T("cli.prompt_failed"), err)`.

- [ ] **Étape 4 : brancher les appelants**

Remplacer les littéraux de `usage()`, les descriptions passées à `fs.Bool`/`fs.String`/
`fs.Int`, le bloc d'ambiguïté (lignes 210-214, via `facade.ambiguous` et `facade.choose_pm`
de la tâche 4) et le message de `warm` (ligne 272).

- [ ] **Étape 5 : lancer les tests et le banc**

```bash
go test ./... && make build && ./tests/render-golden.sh --verifier
JIGGER_LANG=en ./jigger 2>&1 | head -3
```

Attendu : tests verts, aucune différence, et l'usage s'affiche en anglais.

- [ ] **Étape 6 : commit**

```bash
git add internal/i18n main.go
git commit -m "Ligne de commande : usage et drapeaux passent par le catalogue"
```

---

### Tâche 6 : les greffons

Six chaînes, autonomes. Le greffon zsh exporte `JIGGER_LANG` pour que le binaire parle la
même langue que lui.

**Fichiers :**
- Modifier : `shell/jigger.plugin.zsh:34-53`
- Modifier : `shell/jigger.psm1:106,122,130,136`
- Modifier : `tests/zpty.zsh` (poser `JIGGER_LANG=fr` dans l'environnement du zpty)
- Modifier : `tests/smoke.ps1` et `tests/pty.ps1` (poser `$env:JIGGER_LANG = 'fr'`)

**Interfaces :**
- Consomme : rien du binaire — c'est tout l'intérêt.
- Produit : la variable `_jigger_lang` côté zsh, `$script:Lang` côté PowerShell.

- [ ] **Étape 1 : côté zsh, résoudre la langue et exporter**

Dans `shell/jigger.plugin.zsh`, avant les vérifications d'installation :

```zsh
# La langue des messages du greffon. Même ordre que le binaire : JIGGER_LANG, puis les
# variables POSIX, puis l'anglais.
#
# L'export n'est pas décoratif : les réglages de jigger se posent sans lui (JIGGER_ROWS=12
# avant le source) parce que le greffon les lit lui-même, mais celui-ci doit atteindre le
# *binaire*, sans quoi le popup parlerait une autre langue que ces messages.
if [[ -n ${JIGGER_LANG-} ]]; then
  export JIGGER_LANG
fi
typeset -g _jigger_lang=${${JIGGER_LANG:-${LC_ALL:-${LC_MESSAGES:-$LANG}}}%%[_.]*}
[[ $_jigger_lang == fr ]] || _jigger_lang=en
```

- [ ] **Étape 2 : traduire les deux messages zsh**

```zsh
if ! command -v jigger >/dev/null 2>&1; then
  if [[ $_jigger_lang == fr ]]; then
    print -u2 "jigger : binaire introuvable dans le PATH. Greffon inactif."
  else
    print -u2 "jigger: binary not found in PATH. Plugin inactive."
  fi
  return 0
fi
```

et, pour la version :

```zsh
  if [[ $_jigger_lang == fr ]]; then
    print -u2 "jigger : le binaire $(command -v jigger) est en $_jigger_v, or ce greffon en demande $JIGGER_VERSION_REQUISE. Recompile-le (« make install ») ou remplace celui du PATH. Greffon inactif."
  else
    print -u2 "jigger: the binary at $(command -v jigger) is $_jigger_v, but this plugin requires $JIGGER_VERSION_REQUISE. Rebuild it (« make install ») or replace the one in PATH. Plugin inactive."
  fi
```

- [ ] **Étape 3 : côté PowerShell, résoudre et traduire les quatre messages**

Dans `shell/jigger.psm1`, après le bloc de réglages :

```powershell
# Langue des messages du module. $env:JIGGER_LANG est déjà une variable d'environnement :
# le binaire la voit sans qu'on ait rien à exporter.
$script:Lang = if ($env:JIGGER_LANG) { $env:JIGGER_LANG }
               else { [System.Globalization.CultureInfo]::CurrentUICulture.TwoLetterISOLanguageName }
if ($script:Lang -ne 'fr') { $script:Lang = 'en' }

function Get-JiggerText([string]$En, [string]$Fr) {
    if ($script:Lang -eq 'fr') { return $Fr }
    return $En
}
```

Puis chaque `Write-Warning` devient, par exemple :

```powershell
    Write-Warning (Get-JiggerText `
        "jigger: binary « $($script:Exe) » not found in PATH. Module inactive." `
        "jigger : binaire « $($script:Exe) » introuvable dans le PATH. Module inactif.")
```

Les trois autres, en toutes lettres — un exécutant peut lire cette tâche sans les autres :

```powershell
    # version incompatible (ligne 122)
    Write-Warning (Get-JiggerText `
        "jigger: the binary at $chemin is $version, but this module requires $($script:VersionRequise). Rebuild it (« make install ») or replace the one in PATH. Module inactive." `
        "jigger : le binaire $chemin est en $version, or ce module en demande $($script:VersionRequise). Recompile-le (« make install ») ou remplace celui du PATH. Module inactif.")

    # PSReadLine absent (ligne 130)
    Write-Warning (Get-JiggerText `
        'jigger: PSReadLine is missing. Module inactive.' `
        'jigger : PSReadLine est absent. Module inactif.')

    # mode Vi (ligne 136)
    Write-Warning (Get-JiggerText `
        'jigger: live popup disabled in Vi mode (⇥ opens the picker).' `
        'jigger : popup vivant désactivé en mode Vi (⇥ ouvre le sélecteur).')
```

Attention à la ligne 122 : le message actuel est construit par concaténation sur deux
lignes. Le remplacer entièrement plutôt que d'insérer `Get-JiggerText` au milieu.

- [ ] **Étape 4 : épingler la langue des suites de test**

`tests/zpty.zsh` : ajouter `JIGGER_LANG=fr` à l'environnement passé au zpty, à côté des
autres variables de test.
`tests/smoke.ps1` et `tests/pty.ps1` : ajouter `$env:JIGGER_LANG = 'fr'` avant
l'`Import-Module`.

- [ ] **Étape 5 : lancer les deux suites**

```bash
make test-all
JIGGER_LANG=en zsh -f -c 'source shell/jigger.plugin.zsh' 2>&1   # doit se charger sans un mot
PATH=/tmp/faux:$PATH JIGGER_LANG=en zsh -f -c 'source shell/jigger.plugin.zsh' 2>&1
```

Attendu : la suite passe ; le dernier essai (avec un faux binaire trop vieux dans le PATH,
comme en tâche 2) affiche le message **anglais**.

- [ ] **Étape 6 : commit**

```bash
git add shell/jigger.plugin.zsh shell/jigger.psm1 tests/zpty.zsh tests/smoke.ps1 tests/pty.ps1
git commit -m "Greffons : messages bilingues et export de JIGGER_LANG"
```

---

### Tâche 7 : épinglage des tests Go et vérification croisée

Aucune chaîne ne migre ici : on ferme la porte à la dépendance à la locale.

**Fichiers :**
- Modifier : tous les `*_test.go` qui comparent une chaîne visible
- Créer : `internal/i18n/locale_test.go`

- [ ] **Étape 1 : trouver les tests exposés**

```bash
grep -rln 'insérer\|annuler\|parcourir\|aucun candidat\|rien à signaler\|verbe inconnu' --include='*_test.go' .
```

- [ ] **Étape 2 : poser la langue dans chacun**

En tête de chaque test listé :

```go
	t.Setenv("JIGGER_LANG", "fr")
	i18n.Recharger()
```

- [ ] **Étape 3 : écrire le test qui prouve l'indépendance à la locale**

```go
// internal/i18n/locale_test.go
package i18n

import "testing"

// Une locale exotique ne doit ni planter ni changer autre chose que la langue.
func TestLocaleExotiqueNeCassePas(t *testing.T) {
	for _, valeur := range []string{"", "C", "POSIX", "ja_JP.UTF-8", "fr", "en_GB", "fr-CA"} {
		t.Setenv("JIGGER_LANG", "")
		t.Setenv("LC_ALL", valeur)
		Recharger()
		if l := Courante(); l != EN && l != FR {
			t.Fatalf("LC_ALL=%q donne une langue hors bornes", valeur)
		}
	}
}
```

- [ ] **Étape 4 : vérification croisée**

```bash
LANG=fr_FR.UTF-8 make test-all
LANG=en_US.UTF-8 make test-all
LANG=ja_JP.UTF-8 make test-all
```

Attendu : les trois passent à l'identique. **C'est la preuve que la suite ne dépend plus de
la machine qui la lance.**

- [ ] **Étape 5 : commit**

```bash
git add -A
git commit -m "Tests : langue épinglée, indépendance à la locale vérifiée"
```

---

### Tâche 8 : le README en anglais

Première tâche de documentation. À faire **après** les tâches 1 à 7 : les captures doivent
être régénérées avec un binaire qui parle anglais.

**Fichiers :**
- Créer : `README.fr.md` (le contenu français actuel, tel quel)
- Modifier : `README.md` (traduction anglaise)

- [ ] **Étape 1 : déplacer le français**

```bash
git mv README.md README.fr.md
```

- [ ] **Étape 2 : écrire la version anglaise**

Traduire `README.fr.md` dans `README.md`. Règles :

- **les captures de cadres sont régénérées**, pas traduites à la main :
  `JIGGER_LANG=en ./jigger render --line "brew install fire" --cols 56 --color never | tail -n +2` ;
- les sorties de la façade (`jg outdated`, `jg list`) sont **rejouées** en anglais ;
- le vocabulaire du domaine ne bouge pas : `formula`, `cask`, `tap`, `bucket`, `brew`,
  `winget`, `scoop` ;
- les chemins, commandes et noms de variables restent identiques.

- [ ] **Étape 3 : poser les renvois croisés**

En tête de `README.md` : `*Read this in [French](README.fr.md).*`
En tête de `README.fr.md` : `*Ce document existe aussi en [anglais](README.md).*`

- [ ] **Étape 4 : vérifier les liens**

```bash
grep -oE '\]\([^)h][^)]*\)' README.md | tr -d '()]' | while read -r f; do
  [ -e "${f%%#*}" ] || echo "lien mort : $f"
done
```

Attendu : aucune sortie.

- [ ] **Étape 5 : traquer le français resté en route**

Une traduction de 6 000 mots laisse toujours un paragraphe derrière elle. Ce filtre lit la
prose hors blocs de code et signale les mots outils français :

```sh
awk '/^```/{c=!c; next} !c' README.md |
  grep -nwE 'le|la|les|des|une|dans|pour|avec|que|qui|est|sont|cette|ainsi|donc' |
  head -20
```

Attendu : aucune sortie. C'est une heuristique, pas une preuve — mais elle attrape le
paragraphe oublié, qui est le cas réel.

- [ ] **Étape 6 : commit**

```bash
git add README.md README.fr.md
git commit -m "README en anglais, version française conservée"
```

---

### Tâche 9 : le guide de premiers pas en anglais

**Fichiers :**
- Créer : `docs/fr/getting-started.md` (le français actuel)
- Modifier : `docs/getting-started.md` (traduction anglaise)
- Modifier : `README.md` et `README.fr.md` (le lien vers le guide)

- [ ] **Étape 1 : déplacer le français**

```bash
mkdir -p docs/fr && git mv docs/getting-started.md docs/fr/getting-started.md
```

- [ ] **Étape 2 : traduire, captures régénérées**

Mêmes règles qu'en tâche 8. Le guide contient **deux cadres** (§ accroche et § 6) et
plusieurs sorties de la façade : toutes rejouées avec `JIGGER_LANG=en`.

- [ ] **Étape 3 : corriger les chemins relatifs**

`docs/fr/getting-started.md` descend d'un niveau : ses liens `../README.md` deviennent
`../../README.fr.md`, et ses liens vers `../shell/…` deviennent `../../shell/…`.

- [ ] **Étape 4 : vérifier les liens des deux guides**

```bash
for f in docs/getting-started.md docs/fr/getting-started.md; do
  d=$(dirname "$f")
  grep -oE '\]\([^)h][^)]*\)' "$f" | tr -d '()]' | while read -r l; do
    [ -e "$d/${l%%#*}" ] || echo "$f → lien mort : $l"
  done
done
```

Attendu : aucune sortie.

- [ ] **Étape 5 : traquer le français resté en route**

Le même filtre qu'en tâche 8, appliqué au guide :

```sh
awk '/^```/{c=!c; next} !c' docs/getting-started.md |
  grep -nwE 'le|la|les|des|une|dans|pour|avec|que|qui|est|sont|cette|ainsi|donc' |
  head -20
```

Attendu : aucune sortie.

- [ ] **Étape 6 : commit**

```bash
git add docs/getting-started.md docs/fr/getting-started.md README.md README.fr.md
git commit -m "Guide de premiers pas en anglais, version française conservée"
```

---

### Tâche 10 : le CHANGELOG et la fiche du projet

**Fichiers :**
- Modifier : `CHANGELOG.md` (traduction anglaise, **sans** version française)
- Modifier : la description et les topics du projet GitLab (par l'API)

- [ ] **Étape 1 : traduire le CHANGELOG**

Traduire les sections `[Non publié]` à `[v0.1.6]` en anglais. Le fichier n'a **pas** de
version française : le traduire à chaque release serait un engagement perpétuel pour un
document qu'on ne relit pas deux fois. Ajouter en tête :

```markdown
*This changelog is kept in English only. The rest of the documentation exists in
[French](README.fr.md) too.*
```

- [ ] **Étape 2 : mettre la fiche du projet à jour**

```bash
python3 ~/.claude/skills/gitlab-changelog/scripts/gl.py api PUT /projects/:id \
  --param "description=Contextual completion and interactive package picker for Homebrew, winget and scoop — one syntax for all three" \
  --param "topics=cli,homebrew,winget,scoop,zsh,powershell,go,bubbletea,package-manager"
```

- [ ] **Étape 3 : vérifier**

```bash
python3 ~/.claude/skills/gitlab-changelog/scripts/gl.py api GET /projects/:id | \
  python3 -c "import json,sys; d=json.load(sys.stdin); print(d['description']); print(d['topics'])"
```

Attendu : la description anglaise et les topics.

- [ ] **Étape 4 : dernière vérification d'ensemble**

```bash
make test-all
./tests/render-golden.sh --verifier
JIGGER_LANG=en ./jigger render --line "brew inst" --cols 56 --color never | tail -n +2
JIGGER_LANG=fr ./jigger render --line "brew inst" --cols 56 --color never | tail -n +2
```

Attendu : suite verte, aucune différence sur le banc, et les deux cadres dans leurs langues.

- [ ] **Étape 5 : commit**

```bash
git add CHANGELOG.md
git commit -m "CHANGELOG en anglais, fiche du projet mise à jour"
```

---

## Ce que ce plan ne fait pas

- **Le site de présentation** — chantier suivant, sur du contenu enfin stable.
- **L'espagnol et l'allemand** — `nbLangues` passera à 3 ou 4 le jour où quelqu'un pourra
  les relire ; le compilateur désignera alors chaque ligne à compléter.
- **La traduction des sorties des gestionnaires** — `winget` répond déjà dans la langue du
  système, et jigger sait la découper. Rien n'y touche.
