# Le site de jigger — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** publier `jigger.yg-devworks.com` — une page unique, bilingue, statique, dont la
source vit dans ce dépôt sous `website/`.

**Architecture:** trois fichiers servis (`index.html`, `styles.css`, `app.js`), sans build,
sans dépendance réseau. L'anglais est écrit **en clair dans le HTML**, le français vit dans
un dictionnaire de `app.js` : l'inverse de Cocktails, dont on reprend par ailleurs le
mécanisme au mot près. Le déploiement décalque `cocktails-website` : archive → LXC nginx
`192.168.50.11` → route Caddy `192.168.50.10`.

**Tech Stack:** HTML5, CSS (variables natives, pas de framework), JavaScript ES5 sans
dépendance, bash pour les contrôles et le déploiement.

**Spec:** [`docs/specs/2026-08-16-site-jigger-design.md`](../specs/2026-08-16-site-jigger-design.md)

## Global Constraints

Ces règles lient **toutes** les tâches.

- **Aucune ressource externe.** Pas de CDN, pas de Google Fonts, pas d'analytique. La CSP
  servie est `default-src 'self'` : toute ressource distante, et tout script ou style
  *inline*, sera bloquée par le navigateur. Les polices sont celles du système.
- **Anglais dans le HTML, français dans `app.js`.** Aucune chaîne française dans
  `index.html`, aucune chaîne anglaise dans le dictionnaire `FR`.
- **Repli sur l'anglais.** Clé française absente ⇒ l'anglais reste affiché. Jamais de clé
  brute à l'écran.
- **Une entrée de dictionnaire par ligne**, de la forme `  'cle.sous': 'texte',` — le
  contrôle de parité lit `app.js` avec `grep`, pas avec un analyseur JavaScript.
- **Toute sortie de terminal affichée est une vraie sortie**, obtenue par la commande
  citée en commentaire HTML juste au-dessus du bloc. Aucun cadre dessiné à la main.
- **Version affichée : `0.9.0`** — celle des captures du guide. Elle n'apparaît que dans
  les cadres capturés, jamais en dur dans une phrase.
- **Responsive jusqu'à 360 px de large**, sans débordement horizontal. Les cadres de
  terminal, plus larges que ça, défilent dans leur propre conteneur.
- **Aucun secret, aucune adresse IP privée dans les fichiers servis.** Les adresses du
  Proxmox n'existent que dans `deploy-proxmox.sh` et les fichiers de `deploy/`.
- Commit après chaque tâche, en français, à l'impératif ou en forme nominale.

---

## Structure des fichiers

| Fichier | Responsabilité |
|---|---|
| `website/index.html` | La page : structure, contenu anglais, clés `data-i18n` |
| `website/styles.css` | La feuille, dérivée des jetons de `cocktails-website` |
| `website/app.js` | Le dictionnaire `FR`, la bascule de langue, la langue initiale |
| `website/jigger-icon.svg` | Le favicon : l'angle arrondi du cadre |
| `website/og.png` | L'image d'aperçu des liens partagés (1200×630) |
| `website/og.html` | Le gabarit qui a produit `og.png` — **non déployé** |
| `website/verifier.sh` | Parité des langues, commandes d'installation, liens |
| `website/deploy/nginx-jigger.conf` | Le vhost nginx |
| `website/deploy/caddy-jigger.conf` | La route HTTPS |
| `website/deploy-proxmox.sh` | Le déploiement |
| `website/README.md` | Prévisualiser, vérifier, déployer, refaire l'`og.png` |

---

## Task 1 : le squelette et le moteur bilingue

Le plus petit site qui soit déjà **un site bilingue qui marche** : en-tête, bascule de
langue, accroche, et le moteur qui bascule tout le reste.

**Files:**
- Create: `website/index.html`
- Create: `website/styles.css`
- Create: `website/app.js`

**Interfaces:**
- Consumes: rien.
- Produces:
  - la convention de clé `data-i18n="section.role"` sur tout élément traduisible ;
  - le dictionnaire `FR` de `app.js`, délimité par les marqueurs
    `/* --- FR --- */` et `/* --- /FR --- */`, une entrée par ligne ;
  - `setLang('fr'|'en')`, la clé `localStorage` `jigger-lang`, l'attribut `data-en`
    posé au chargement sur chaque élément traduisible.

- [ ] **Step 1 : écrire `website/index.html`**

L'anglais est en clair ; chaque élément traduisible porte sa clé.

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>jigger — one syntax for Homebrew, winget and scoop</title>
  <meta name="description" content="A live package picker in your shell, and one syntax across Homebrew, winget and scoop. A small Go binary wired into zsh and PowerShell.">
  <meta name="theme-color" content="#07090e">
  <link rel="stylesheet" href="/styles.css">
  <script src="/app.js" defer></script>
</head>
<body>
  <header class="site-header">
    <div class="wrap header-inner">
      <a class="brand" href="#top">jigger</a>
      <nav aria-label="Main navigation">
        <a href="#popup" data-i18n="nav.popup">The popup</a>
        <a href="#facade" data-i18n="nav.facade">One syntax</a>
        <a href="#install" data-i18n="nav.install">Install</a>
      </nav>
      <div class="lang-toggle" role="group" aria-label="Language">
        <button type="button" data-lang="en" aria-pressed="true">EN</button>
        <button type="button" data-lang="fr" aria-pressed="false">FR</button>
      </div>
    </div>
  </header>

  <main id="top">
    <section class="hero">
      <div class="wrap">
        <span class="eyebrow" data-i18n="hero.eyebrow">macOS · Windows · Linux</span>
        <h1 data-i18n="hero.h1">Your package manager,<br><em>as you type.</em></h1>
        <p class="lede" data-i18n="hero.lede">
          A frame appears under your prompt and follows every keystroke. And across
          Homebrew, winget and scoop, one syntax instead of three.
        </p>

        <!-- capture : jigger render --line "brew install fire" --cols 56 -->
        <pre class="frame" aria-label="jigger in a terminal">❯ brew install fire
╭──────────────────────────────────────────────────────╮
│❯ brew install                            jigger 0.9.0│
│  ▣  firealpaca                                       │
│  ▣  firebase-admin                                   │
│  ◆  firebase-cli                                     │
│  ▣  firebird-emu                                     │
│  ▣  firecamp                                         │
│                                                      │
│   ⇥  insert   ↓  browse   ^G  close                  │
╰──────────────────────────────────────────────────────╯</pre>

        <div class="hero-actions">
          <a class="btn primary" href="#install" data-i18n="hero.install">Install it <span class="arw">→</span></a>
          <a class="btn ghost" href="https://gitlab.yg-devworks.com/yves/jigger" data-i18n="hero.source">Read the source</a>
        </div>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <div class="wrap">
      <p data-i18n="foot.made">Free software, Apache 2.0. Built in the open.</p>
    </div>
  </footer>
</body>
</html>
```

- [ ] **Step 2 : écrire `website/styles.css`**

Les jetons sont ceux de Cocktails — la parenté doit se voir — moins ce qui ne sert pas
ici (vidéo de fond, cartes produit).

```css
/* ============================================================================
   jigger — site de présentation. Dark, accent bleu, repris de cocktails-website.
   Type : system-ui + ui-monospace, natifs partout.
   ============================================================================ */

:root {
  --bg:        #07090e;
  --panel:     #0e111b;
  --card:      #141726;
  --line:      rgba(255,255,255,.08);
  --line-2:    rgba(255,255,255,.14);
  --text:      #eceef5;
  --muted:     #8b90a6;
  --accent:    #5b8cff;
  --accent-2:  #86a6ff;
  --green:     #37d67a;
  --amber:     #f5a623;
  --radius:    18px;
  --maxw:      1080px;
  --sans: system-ui, -apple-system, "SF Pro Text", "Segoe UI", Roboto, sans-serif;
  --mono: ui-monospace, "SF Mono", "JetBrains Mono", "Fira Code", Menlo, monospace;
}

* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }

body {
  margin: 0;
  background: var(--bg);
  color: var(--text);
  font-family: var(--sans);
  font-size: 17px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  overflow-x: hidden;
}

body::before {
  content: "";
  position: fixed; inset: 0; z-index: -1;
  background:
    radial-gradient(1100px 700px at 78% -8%, rgba(91,140,255,.20), transparent 60%),
    radial-gradient(900px 620px at 8% 12%, rgba(91,140,255,.10), transparent 55%);
}

a { color: inherit; text-decoration: none; }
.wrap { width: min(100% - 40px, var(--maxw)); margin-inline: auto; }
.mono { font-family: var(--mono); }

.eyebrow {
  font-family: var(--mono);
  font-size: 12.5px; letter-spacing: .14em; text-transform: uppercase;
  color: var(--accent-2);
}

/* --- en-tête ------------------------------------------------------------- */
.site-header {
  position: sticky; top: 0; z-index: 10;
  background: rgba(7,9,14,.72);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--line);
}
.header-inner { display: flex; align-items: center; gap: 24px; height: 60px; }
.brand { font-family: var(--mono); font-weight: 600; font-size: 18px; }
.site-header nav { display: flex; gap: 20px; margin-inline-end: auto; }
.site-header nav a { color: var(--muted); font-size: 15px; }
.site-header nav a:hover { color: var(--text); }

.lang-toggle { display: flex; border: 1px solid var(--line-2); border-radius: 999px; overflow: hidden; }
.lang-toggle button {
  background: none; border: 0; color: var(--muted);
  font: inherit; font-size: 13px; padding: 4px 12px; cursor: pointer;
}
.lang-toggle button.on { background: var(--accent); color: #06080f; }

/* --- corps --------------------------------------------------------------- */
.hero { padding: 72px 0 56px; }
h1 { font-size: clamp(32px, 5vw, 52px); line-height: 1.12; margin: 12px 0 16px; }
h1 em { color: var(--accent-2); font-style: normal; }
h2 { font-size: clamp(24px, 3.4vw, 34px); margin: 0 0 12px; }
.lede { color: var(--muted); font-size: 19px; max-width: 62ch; }

section { padding: 56px 0; border-top: 1px solid var(--line); }
.hero { border-top: 0; }

/* --- cadres de terminal --------------------------------------------------- */
.frame {
  font-family: var(--mono);
  font-size: 13px; line-height: 1.45;
  background: var(--panel);
  border: 1px solid var(--line-2);
  border-radius: 12px;
  padding: 16px 18px;
  margin: 28px 0;
  overflow-x: auto;          /* les cadres sont plus larges que 360 px */
  white-space: pre;
  color: var(--text);
}

/* --- boutons -------------------------------------------------------------- */
.btn {
  display: inline-block; padding: 11px 20px; border-radius: 999px;
  font-size: 15px; font-weight: 500; border: 1px solid transparent;
}
.btn.primary { background: var(--accent); color: #06080f; }
.btn.ghost { border-color: var(--line-2); color: var(--text); }
.hero-actions { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }

/* --- pied ----------------------------------------------------------------- */
.site-footer { border-top: 1px solid var(--line); padding: 40px 0; color: var(--muted); font-size: 15px; }

@media (max-width: 640px) {
  .site-header nav { display: none; }
  .header-inner { gap: 12px; }
  .lang-toggle { margin-inline-start: auto; }
}
```

- [ ] **Step 3 : écrire `website/app.js`**

Le mécanisme de Cocktails, **inversé** : l'anglais est la source, le français est le
dictionnaire.

```javascript
/* jigger — bascule de langue (fichier externe : compatible CSP 'self'). */
(function () {
  var docEl = document.documentElement;

  /* --- FR --- */
  var FR = {
    'nav.popup': 'La fenêtre',
    'nav.facade': 'Une syntaxe',
    'nav.install': 'Installer',
    'hero.eyebrow': 'macOS · Windows · Linux',
    'hero.h1': 'Votre gestionnaire de paquets,<br><em>pendant que vous tapez.</em>',
    'hero.lede': 'Un cadre apparaît sous votre invite et suit chaque frappe. Et pour Homebrew, winget et scoop, une seule syntaxe au lieu de trois.',
    'hero.install': 'L’installer <span class="arw">→</span>',
    'hero.source': 'Lire le code',
    'foot.made': 'Logiciel libre, Apache 2.0. Développé à ciel ouvert.'
  };
  /* --- /FR --- */

  var TITLE = {
    en: 'jigger — one syntax for Homebrew, winget and scoop',
    fr: 'jigger — une syntaxe pour Homebrew, winget et scoop'
  };

  var i18nEls = Array.prototype.slice.call(document.querySelectorAll('[data-i18n]'));
  i18nEls.forEach(function (el) { el.setAttribute('data-en', el.innerHTML); });

  var lang = 'en';

  function setLang(l) {
    lang = (l === 'fr') ? 'fr' : 'en';
    docEl.lang = lang;
    document.title = TITLE[lang];
    i18nEls.forEach(function (el) {
      var k = el.getAttribute('data-i18n');
      /* Repli sur l'anglais : une clé française absente laisse le texte d'origine. */
      el.innerHTML = (lang === 'fr' && FR[k] != null) ? FR[k] : el.getAttribute('data-en');
    });
    document.querySelectorAll('.lang-toggle button').forEach(function (b) {
      var on = b.getAttribute('data-lang') === lang;
      b.setAttribute('aria-pressed', on ? 'true' : 'false');
      b.classList.toggle('on', on);
    });
    try { localStorage.setItem('jigger-lang', lang); } catch (e) {}
  }

  document.querySelectorAll('.lang-toggle button').forEach(function (b) {
    b.addEventListener('click', function () { setLang(b.getAttribute('data-lang')); });
  });

  /* Langue initiale : le choix mémorisé, sinon le navigateur, sinon l'anglais —
     comme le binaire depuis la v0.9.0. */
  var saved = null;
  try { saved = localStorage.getItem('jigger-lang'); } catch (e) {}
  var initial = saved || ((navigator.language || 'en').toLowerCase().indexOf('fr') === 0 ? 'fr' : 'en');
  setLang(initial);
})();
```

- [ ] **Step 4 : servir la page et la regarder**

```bash
cd website && python3 -m http.server 8080
```

Ouvrir `http://localhost:8080/`. Attendu :
- la page s'affiche en anglais, `EN` en surbrillance ;
- un clic sur `FR` traduit en-tête, accroche et pied **sans recharger** ; le cadre de
  terminal, lui, ne bouge pas (il n'a pas de clé) ;
- `document.documentElement.lang` passe à `fr` ;
- après rechargement, la page reste en français ;
- à 360 px de large (mode responsive du navigateur), aucun défilement horizontal de la
  page — seul le cadre défile dans sa boîte.

- [ ] **Step 5 : commit**

```bash
git add website/index.html website/styles.css website/app.js
git commit -m "Site : squelette de la page et bascule de langue"
```

---

## Task 2 : `verifier.sh` — parité des langues et liens

Les deux contrôles qui peuvent déjà mordre. Le troisième (les commandes d'installation)
arrivera avec la section qu'il garde, en Task 5.

**Files:**
- Create: `website/verifier.sh`

**Interfaces:**
- Consumes: les marqueurs `/* --- FR --- */` de `app.js`, les clés `data-i18n` de
  `index.html`.
- Produces: `verifier.sh [--reseau]`, sortie 0 si tout passe, 1 sinon ; chaque contrôle
  imprime `ok —` ou `ÉCHEC —`.

- [ ] **Step 1 : écrire le contrôle, et le voir échouer**

Créer `website/verifier.sh` :

```bash
#!/usr/bin/env bash
# Contrôles de la page avant déploiement. `--reseau` ajoute la vérification des
# liens externes (sinon la passe est hors-ligne, donc déterministe).
set -euo pipefail

cd -- "$(dirname -- "${BASH_SOURCE[0]}")"
RACINE="$(cd .. && pwd)"
RESEAU=0
[ "${1:-}" = "--reseau" ] && RESEAU=1

echecs=0
ok()     { printf 'ok     — %s\n' "$1"; }
echec()  { printf 'ÉCHEC  — %s\n' "$1" >&2; echecs=$((echecs + 1)); }

# --- 1. Parité des langues ------------------------------------------------
# Les clés employées dans le HTML, et celles définies dans le dictionnaire FR.
cles_html="$(grep -o 'data-i18n="[^"]*"' index.html | cut -d'"' -f2 | sort -u)"
cles_fr="$(sed -n '/--- FR ---/,/--- \/FR ---/p' app.js \
           | grep -o "^  '[^']*':" | tr -d " ':" | sort -u)"

manquantes="$(comm -23 <(printf '%s\n' "$cles_html") <(printf '%s\n' "$cles_fr"))"
orphelines="$(comm -13 <(printf '%s\n' "$cles_html") <(printf '%s\n' "$cles_fr"))"

if [ -n "$manquantes" ]; then
    echec "clés sans traduction française : $(echo "$manquantes" | tr '\n' ' ')"
else
    ok "toutes les clés du HTML sont traduites ($(printf '%s\n' "$cles_html" | wc -l | tr -d ' '))"
fi

if [ -n "$orphelines" ]; then
    echec "clés françaises inutilisées : $(echo "$orphelines" | tr '\n' ' ')"
else
    ok "aucune entrée orpheline dans le dictionnaire"
fi

# --- 2. Liens -------------------------------------------------------------
# Internes : chaque ancre doit désigner un id existant.
for ancre in $(grep -o 'href="#[^"]*"' index.html | cut -d'"' -f2 | cut -c2- | sort -u); do
    [ "$ancre" = "top" ] && continue
    if grep -q "id=\"$ancre\"" index.html; then
        ok "ancre #$ancre"
    else
        echec "ancre #$ancre ne désigne aucun id"
    fi
done

# Externes : seulement sur demande, car ça dépend du réseau.
if [ "$RESEAU" = 1 ]; then
    for url in $(grep -o 'href="https\?://[^"]*"' index.html | cut -d'"' -f2 | sort -u); do
        code="$(curl -sIL --max-time 10 -o /dev/null -w '%{http_code}' "$url" || echo 000)"
        case "$code" in
            2*|3*) ok "$url → $code" ;;
            *)     echec "$url → $code" ;;
        esac
    done
fi

if [ "$echecs" -gt 0 ]; then
    printf '\n%d contrôle(s) en échec.\n' "$echecs" >&2
    exit 1
fi
printf '\nTout est vert.\n'
```

- [ ] **Step 2 : prouver que le contrôle de parité mord**

```bash
chmod +x website/verifier.sh
# On casse volontairement : une clé du HTML sans traduction.
sed -i.bak "/'nav.install':/d" website/app.js
website/verifier.sh; echo "sortie=$?"
```

Attendu : `ÉCHEC — clés sans traduction française : nav.install` et `sortie=1`.

```bash
mv website/app.js.bak website/app.js
```

- [ ] **Step 3 : prouver que le contrôle d'ancre mord**

```bash
# #install ne désignera aucun id tant que la section n'existe pas (Task 5).
website/verifier.sh; echo "sortie=$?"
```

Attendu : `ÉCHEC — ancre #install ne désigne aucun id`, `sortie=1`. C'est **le
comportement voulu** : la navigation promet une section qui n'existe pas encore.

Retirer provisoirement les trois entrées de navigation dont les sections arrivent aux
Tasks 3 à 5 n'est **pas** la bonne réponse. À la place, ajouter dès maintenant les trois
sections vides qui les portent, dans `index.html`, juste avant `</main>` :

```html
    <section id="popup"><div class="wrap"><h2 data-i18n="popup.h2">The popup</h2></div></section>
    <section id="facade"><div class="wrap"><h2 data-i18n="facade.h2">One syntax for three</h2></div></section>
    <section id="install"><div class="wrap"><h2 data-i18n="install.h2">Install</h2></div></section>
```

et les trois clés dans le dictionnaire `FR` de `app.js`, avant `/* --- /FR --- */` :

```javascript
    'popup.h2': 'La fenêtre',
    'facade.h2': 'Une syntaxe pour trois',
    'install.h2': 'Installer',
```

- [ ] **Step 4 : la passe doit être verte**

```bash
website/verifier.sh; echo "sortie=$?"
```

Attendu : chaque ligne en `ok`, `Tout est vert.`, `sortie=0`.

```bash
website/verifier.sh --reseau; echo "sortie=$?"
```

Attendu : en plus, `https://gitlab.yg-devworks.com/yves/jigger → 200`.

- [ ] **Step 5 : commit**

```bash
git add website/verifier.sh website/index.html website/app.js
git commit -m "Site : contrôles de parité des langues et de liens"
```

---

## Task 3 : la fenêtre et les douze verbes

Les deux sections qui portent le message. Elles remplissent les conteneurs `#popup` et
`#facade` créés en Task 2.

**Files:**
- Modify: `website/index.html` — sections `#popup` et `#facade`
- Modify: `website/app.js` — les clés françaises correspondantes
- Modify: `website/styles.css` — la liste de touches et la grille des verbes

**Interfaces:**
- Consumes: `data-i18n`, le dictionnaire `FR`, la classe `.frame`.
- Produces: les classes `.keys` (liste de touches) et `.verbs` (grille des verbes).

- [ ] **Step 1 : remplacer la section `#popup` de `index.html`**

```html
    <section id="popup">
      <div class="wrap">
        <span class="eyebrow" data-i18n="popup.eyebrow">The popup</span>
        <h2 data-i18n="popup.h2">It follows your typing.</h2>
        <p class="lede" data-i18n="popup.lede">
          Type a package-manager command and the frame appears on its own, narrowing
          down with every letter. Nothing to trigger, nothing to remember.
        </p>

        <ul class="keys">
          <li><kbd>⇥</kbd> <span data-i18n="popup.k1">inserts the current candidate</span></li>
          <li><kbd>↓</kbd> <span data-i18n="popup.k2">enters the list</span></li>
          <li><kbd>↑</kbd> <span data-i18n="popup.k3">moves up, then hands the keyboard back to the shell</span></li>
          <li><kbd>^G</kbd> <span data-i18n="popup.k4">closes it for this line</span></li>
        </ul>

        <p data-i18n="popup.arrows">
          <strong>Your arrow keys are never taken over.</strong> Until the popup holds
          the keyboard, <kbd>↑</kbd> and <kbd>↓</kbd> stay your shell history — where
          they belong.
        </p>
      </div>
    </section>
```

- [ ] **Step 2 : remplacer la section `#facade` de `index.html`**

Le cadre est la capture réelle du guide (§ 6).

```html
    <section id="facade">
      <div class="wrap">
        <span class="eyebrow" data-i18n="facade.eyebrow">One syntax</span>
        <h2 data-i18n="facade.h2">Three managers, one vocabulary.</h2>
        <p class="lede" data-i18n="facade.lede">
          <span class="mono">jg install fd</span> reaches whichever of Homebrew, winget
          or scoop knows <span class="mono">fd</span> — without you having to know which.
        </p>

        <!-- capture : jigger render --line "jg " --cols 56 -->
        <pre class="frame" aria-label="the twelve verbs in the popup">❯ jg
╭──────────────────────────────────────────────────────╮
│❯ jigger                                  jigger 0.9.0│
│  •  cleanup                                          │
│  •  doctor                                           │
│  •  info                                             │
│  •  install                                          │
│                                                      │
│   ⇥  insert   ↓  browse   ^G  close                  │
╰──────────────────────────────────────────────────────╯</pre>

        <ul class="verbs">
          <li class="mono">install</li><li class="mono">uninstall</li>
          <li class="mono">upgrade</li><li class="mono">list</li>
          <li class="mono">outdated</li><li class="mono">search</li>
          <li class="mono">info</li><li class="mono">source</li>
          <li class="mono">pin</li><li class="mono">unpin</li>
          <li class="mono">cleanup</li><li class="mono">doctor</li>
        </ul>
        <p class="muted-note" data-i18n="facade.verbs">
          The first seven work everywhere. The others exist only where the manager does
          — asking winget for <span class="mono">cleanup</span> fails cleanly, naming
          who would know how to do it.
        </p>

        <p data-i18n="facade.choice">
          <strong>Never an automatic choice.</strong> When a single manager knows the
          name, it wins silently. When several do, the picker opens and you decide — two
          packages sharing a name are not necessarily the same software. No setting
          changes that; <span class="mono">--pm</span> is the way to settle it in a
          script.
        </p>
      </div>
    </section>
```

- [ ] **Step 3 : ajouter les clés françaises dans `app.js`**

Dans le bloc `FR`, en remplaçant les entrées `popup.h2` et `facade.h2` posées en Task 2 :

```javascript
    'popup.eyebrow': 'La fenêtre',
    'popup.h2': 'Elle suit votre frappe.',
    'popup.lede': 'Tapez une commande de gestionnaire : le cadre apparaît seul et se resserre à chaque lettre. Rien à déclencher, rien à retenir.',
    'popup.k1': 'insère le candidat courant',
    'popup.k2': 'entre dans la liste',
    'popup.k3': 'remonte, puis rend le clavier au shell',
    'popup.k4': 'referme pour cette ligne',
    'popup.arrows': '<strong>Vos flèches ne sont jamais confisquées.</strong> Tant que la fenêtre n’a pas le clavier, <kbd>↑</kbd> et <kbd>↓</kbd> restent l’historique du shell — là où elles doivent être.',
    'facade.eyebrow': 'Une syntaxe',
    'facade.h2': 'Trois gestionnaires, un vocabulaire.',
    'facade.lede': '<span class="mono">jg install fd</span> atteint celui de Homebrew, winget ou scoop qui connaît <span class="mono">fd</span> — sans que vous ayez à savoir lequel.',
    'facade.verbs': 'Les sept premiers marchent partout. Les autres n’existent que là où le gestionnaire les connaît — demander <span class="mono">cleanup</span> à winget échoue proprement, en nommant qui saurait le faire.',
    'facade.choice': '<strong>Jamais de choix automatique.</strong> Si un seul gestionnaire connaît le nom, il gagne sans rien demander. Si plusieurs le connaissent, le sélecteur s’ouvre et vous tranchez — deux paquets qui portent le même nom ne sont pas forcément le même logiciel. Aucun réglage ne change ça ; <span class="mono">--pm</span> est là pour trancher dans un script.'
```

- [ ] **Step 4 : ajouter les styles dans `styles.css`**

```css
/* --- listes de touches et de verbes --------------------------------------- */
.keys { list-style: none; padding: 0; margin: 24px 0; display: grid; gap: 10px; }
.keys li { display: flex; align-items: baseline; gap: 12px; color: var(--muted); }

kbd {
  font-family: var(--mono); font-size: 13px;
  background: var(--card); border: 1px solid var(--line-2); border-radius: 6px;
  padding: 2px 8px; color: var(--text);
  display: inline-block; min-width: 34px; text-align: center;
}

.verbs {
  list-style: none; padding: 0; margin: 24px 0 12px;
  display: grid; gap: 8px;
  grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
}
.verbs li {
  background: var(--card); border: 1px solid var(--line);
  border-radius: 8px; padding: 8px 12px; font-size: 14px;
}
.muted-note { color: var(--muted); font-size: 15px; max-width: 62ch; }
```

- [ ] **Step 5 : vérifier**

```bash
website/verifier.sh; echo "sortie=$?"
```

Attendu : vert, `sortie=0` — donc toutes les nouvelles clés ont leur traduction.

Puis, page servie (`cd website && python3 -m http.server 8080`), basculer EN↔FR : les
deux sections changent entièrement de langue, les deux cadres restent identiques, et les
noms de verbes ne sont **pas** traduits (ce sont des mots de la ligne de commande).

- [ ] **Step 6 : commit**

```bash
git add website/index.html website/app.js website/styles.css
git commit -m "Site : la fenêtre et les douze verbes"
```

---

## Task 4 : les garanties, le bloc de prompt et Cocktails

Ce que jigger promet, le segment de prompt, et les liens croisés — plus le pied de page
complet.

**Files:**
- Modify: `website/index.html` — sections `#garanties`, `#prompt`, `#cocktails`, pied
- Modify: `website/app.js` — les clés correspondantes
- Modify: `website/styles.css` — la grille des garanties

**Interfaces:**
- Consumes: `data-i18n`, `.frame`, `.wrap`.
- Produces: la classe `.grid-3`, et les ancres `#garanties`, `#prompt`, `#cocktails`.

- [ ] **Step 1 : insérer les trois sections avant `#install` dans `index.html`**

```html
    <section id="garanties">
      <div class="wrap">
        <span class="eyebrow" data-i18n="guar.eyebrow">What it guarantees</span>
        <h2 data-i18n="guar.h2">It stays out of the way.</h2>
        <ul class="grid-3">
          <li>
            <h3 data-i18n="guar.1h">Nothing slow while you type</h3>
            <p data-i18n="guar.1p">Catalogs are cached and read from disk. A keystroke
              never waits on a package manager.</p>
          </li>
          <li>
            <h3 data-i18n="guar.2h">Your manager's output, untouched</h3>
            <p data-i18n="guar.2p">Prompts, progress bars, elevation dialogs — jigger
              relays them as they are instead of parsing them away.</p>
          </li>
          <li>
            <h3 data-i18n="guar.3h">Made for pipes too</h3>
            <p data-i18n="guar.3p">Every listing verb takes <span class="mono">--json</span>,
              so a script never has to read a table meant for humans.</p>
          </li>
        </ul>
      </div>
    </section>

    <section id="prompt">
      <div class="wrap">
        <span class="eyebrow" data-i18n="prompt.eyebrow">In your prompt</span>
        <h2 data-i18n="prompt.h2">A Homebrew block, if you want one.</h2>
        <p class="lede" data-i18n="prompt.lede">
          Ready-made segments for oh-my-posh and starship: the brew version, and how many
          updates are waiting.
        </p>

        <!-- capture : jigger prompt --format text -->
        <pre class="frame" aria-label="the prompt segment"> 4.6.19  3 ⬆</pre>

        <p data-i18n="prompt.how">
          Set <span class="mono">JIGGER_PROMPT=1</span> before loading the plugin, then
          paste the segment for your prompt — both ship with jigger.
        </p>
      </div>
    </section>

    <section id="cocktails">
      <div class="wrap">
        <span class="eyebrow" data-i18n="coc.eyebrow">Nearby</span>
        <h2 data-i18n="coc.h2">And when you'd rather see than type.</h2>
        <p class="lede" data-i18n="coc.lede">
          <strong>Cocktails</strong> is the macOS app of the same family: your packages,
          their dependencies and your updates, at a glance. jigger works perfectly well
          without it — they are independent.
        </p>
        <a class="btn ghost" href="https://cocktails.yg-devworks.com/" data-i18n="coc.link">Visit the Cocktails site <span class="arw">→</span></a>
      </div>
    </section>
```

- [ ] **Step 2 : remplacer le pied de page de `index.html`**

```html
  <footer class="site-footer">
    <div class="wrap">
      <p data-i18n="foot.made">Free software, Apache 2.0. Built in the open.</p>
      <p class="foot-links">
        <a href="https://gitlab.yg-devworks.com/yves/jigger" data-i18n="foot.repo">Repository</a>
        <a href="https://gitlab.yg-devworks.com/yves/jigger/-/blob/main/docs/getting-started.md" data-i18n="foot.guide">Getting started</a>
        <a href="https://gitlab.yg-devworks.com/yves/jigger/-/blob/main/CHANGELOG.md" data-i18n="foot.changelog">Changelog</a>
      </p>
    </div>
  </footer>
```

- [ ] **Step 3 : ajouter les clés françaises dans `app.js`**

```javascript
    'guar.eyebrow': 'Ce qu’il garantit',
    'guar.h2': 'Il ne se met pas en travers.',
    'guar.1h': 'Rien de lent pendant la frappe',
    'guar.1p': 'Les catalogues sont en cache et lus sur disque. Une frappe n’attend jamais un gestionnaire de paquets.',
    'guar.2h': 'La sortie de votre gestionnaire, intacte',
    'guar.2p': 'Invites, barres de progression, demandes d’élévation : jigger les relaie telles quelles au lieu de les digérer.',
    'guar.3h': 'Fait aussi pour les tubes',
    'guar.3p': 'Chaque verbe qui liste accepte <span class="mono">--json</span> : un script n’a jamais à lire un tableau écrit pour des humains.',
    'prompt.eyebrow': 'Dans votre invite',
    'prompt.h2': 'Un bloc Homebrew, si vous en voulez un.',
    'prompt.lede': 'Des segments prêts à coller pour oh-my-posh et starship : la version de brew, et le nombre de mises à jour en attente.',
    'prompt.how': 'Posez <span class="mono">JIGGER_PROMPT=1</span> avant de charger le greffon, puis collez le segment de votre invite — les deux sont livrés avec jigger.',
    'coc.eyebrow': 'À côté',
    'coc.h2': 'Et quand vous préférez voir que taper.',
    'coc.lede': '<strong>Cocktails</strong> est l’application macOS de la même famille : vos paquets, leurs dépendances et vos mises à jour, d’un coup d’œil. jigger fonctionne très bien sans elle — les deux sont indépendants.',
    'coc.link': 'Voir le site de Cocktails <span class="arw">→</span>',
    'foot.repo': 'Le dépôt',
    'foot.guide': 'Premiers pas',
    'foot.changelog': 'Journal des versions'
```

- [ ] **Step 4 : ajouter les styles dans `styles.css`**

```css
/* --- grille de garanties --------------------------------------------------- */
.grid-3 {
  list-style: none; padding: 0; margin: 28px 0 0;
  display: grid; gap: 16px;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
}
.grid-3 li {
  background: var(--card); border: 1px solid var(--line);
  border-radius: var(--radius); padding: 20px;
}
.grid-3 h3 { margin: 0 0 8px; font-size: 17px; }
.grid-3 p { margin: 0; color: var(--muted); font-size: 15px; }

.foot-links { display: flex; flex-wrap: wrap; gap: 18px; margin-top: 8px; }
.foot-links a { color: var(--accent-2); }
```

- [ ] **Step 5 : produire la vraie capture du segment de prompt**

Le bloc `#prompt` ci-dessus contient une sortie **à remplacer par la vraie** :

```bash
jigger prompt --format text
```

Coller la sortie exacte dans le `<pre class="frame">` de la section `#prompt`. Si la
commande n'existe pas sous cette forme, lancer `jigger prompt --help`, prendre la forme
texte qu'elle documente, et **mettre à jour le commentaire de capture** au-dessus du bloc
pour qu'il cite la commande réellement employée.

- [ ] **Step 6 : vérifier**

```bash
website/verifier.sh --reseau; echo "sortie=$?"
```

Attendu : vert, y compris `https://cocktails.yg-devworks.com/ → 200` et les trois liens
GitLab du pied de page.

- [ ] **Step 7 : commit**

```bash
git add website/index.html website/app.js website/styles.css
git commit -m "Site : garanties, bloc de prompt et liens vers Cocktails"
```

---

## Task 5 : l'installation, et le contrôle qui la garde

La section que les visiteurs viennent chercher — et le contrôle qui empêche la page de
mentir sur les commandes, le défaut qui a déjà envoyé les utilisateurs Windows vers un
`make install` inexistant chez eux.

**Files:**
- Modify: `website/index.html` — section `#install`
- Modify: `website/app.js` — les clés correspondantes
- Modify: `website/verifier.sh` — le contrôle des commandes
- Modify: `website/styles.css` — les blocs de commande

**Interfaces:**
- Consumes: `data-i18n`, `.frame`.
- Produces: l'attribut `data-verifier="install"` sur chaque commande d'installation, lu
  par `verifier.sh` ; la classe `.cmd`.

- [ ] **Step 1 : remplacer la section `#install` de `index.html`**

Les commandes sont **exactement** celles de `docs/getting-started.md` (§ 2). Aucune balise
imbriquée dans un bloc `data-verifier` : le contrôle compare du texte brut.

```html
    <section id="install">
      <div class="wrap">
        <span class="eyebrow" data-i18n="install.eyebrow">Install</span>
        <h2 data-i18n="install.h2">Three lines, on any of the three.</h2>

        <h3 data-i18n="install.mac">macOS and Linux — Homebrew</h3>
        <pre class="cmd"><code data-verifier="install">brew tap yves/cocktails https://gitlab.yg-devworks.com/yves/homebrew-cocktails.git</code></pre>
        <pre class="cmd"><code data-verifier="install">brew install jigger</code></pre>

        <h3 data-i18n="install.win">Windows — from the clone</h3>
        <pre class="cmd"><code data-verifier="install">pwsh -NoProfile -File install-windows.ps1</code></pre>
        <p class="muted-note" data-i18n="install.winp">
          It builds, then puts <span class="mono">jigger</span> within reach: a scoop shim
          when scoop is around, a copy into your user directory otherwise. There is no
          winget or scoop package for jigger yet.
        </p>

        <h3 data-i18n="install.go">Any platform — Go</h3>
        <pre class="cmd"><code data-verifier="install">go install gitlab.yg-devworks.com/yves/jigger@latest</code></pre>

        <p data-i18n="install.plugin">
          Then wire the plugin into your shell — one <span class="mono">source</span> line
          in <span class="mono">~/.zshrc</span>, one <span class="mono">Import-Module</span>
          in your PowerShell profile. The guide walks through both, and through what to do
          when nothing shows up.
        </p>
        <a class="btn primary" href="https://gitlab.yg-devworks.com/yves/jigger/-/blob/main/docs/getting-started.md" data-i18n="install.guide">Read the getting-started guide <span class="arw">→</span></a>
      </div>
    </section>
```

- [ ] **Step 2 : ajouter les clés françaises dans `app.js`**

En remplaçant l'entrée `install.h2` posée en Task 2 :

```javascript
    'install.eyebrow': 'Installer',
    'install.h2': 'Trois lignes, sur chacun des trois.',
    'install.mac': 'macOS et Linux — Homebrew',
    'install.win': 'Windows — depuis le clone',
    'install.winp': 'Le script compile, puis met <span class="mono">jigger</span> à portée : un shim scoop si scoop est là, une copie dans votre répertoire utilisateur sinon. Il n’existe pas encore de paquet winget ou scoop pour jigger.',
    'install.go': 'Toute plateforme — Go',
    'install.plugin': 'Reste à brancher le greffon dans votre shell : une ligne <span class="mono">source</span> dans <span class="mono">~/.zshrc</span>, un <span class="mono">Import-Module</span> dans votre profil PowerShell. Le guide déroule les deux, et ce qu’il faut faire quand rien n’apparaît.',
    'install.guide': 'Lire le guide de premiers pas <span class="arw">→</span>'
```

- [ ] **Step 3 : ajouter le style des commandes dans `styles.css`**

```css
/* --- blocs de commande ----------------------------------------------------- */
.cmd {
  font-family: var(--mono); font-size: 13.5px;
  background: var(--panel); border: 1px solid var(--line-2);
  border-radius: 10px; padding: 12px 14px; margin: 10px 0;
  overflow-x: auto; white-space: pre;
}
.cmd code { color: var(--accent-2); }
section h3 { font-size: 17px; margin: 28px 0 4px; }
```

- [ ] **Step 4 : ajouter le contrôle dans `verifier.sh`**

À insérer **avant** le bloc final `if [ "$echecs" -gt 0 ]` :

```bash
# --- 3. Commandes d'installation ------------------------------------------
# Chaque commande affichée doit exister mot pour mot dans le guide : si le guide
# change et pas la page, la page ment.
guide="$RACINE/docs/getting-started.md"
commandes="$(grep -o '<code data-verifier="install">[^<]*</code>' index.html \
             | sed 's/<[^>]*>//g')"

# Here-string, pas un tube : un `while` en bout de tube tourne dans un sous-shell,
# où l'incrément de $echecs serait perdu et où le premier échec masquerait les suivants.
while IFS= read -r commande; do
    [ -z "$commande" ] && continue
    if grep -Fq "$commande" "$guide"; then
        ok "commande présente dans le guide : $commande"
    else
        echec "commande absente du guide : $commande"
    fi
done <<< "$commandes"
```

- [ ] **Step 5 : prouver que le contrôle mord**

```bash
# On fausse une commande dans la page.
sed -i.bak 's|go install gitlab.yg-devworks.com/yves/jigger@latest|go get gitlab.yg-devworks.com/yves/jigger|' website/index.html
website/verifier.sh; echo "sortie=$?"
```

Attendu : `ÉCHEC — commande absente du guide : go get …` et `sortie=1`.

```bash
mv website/index.html.bak website/index.html
```

- [ ] **Step 6 : la passe doit être verte**

```bash
website/verifier.sh --reseau; echo "sortie=$?"
```

Attendu : vert, avec une ligne `ok` par commande d'installation, `sortie=0`.

- [ ] **Step 7 : commit**

```bash
git add website/index.html website/app.js website/styles.css website/verifier.sh
git commit -m "Site : section d'installation, et son contrôle contre le guide"
```

---

## Task 6 : le favicon, l'image d'aperçu et les métadonnées

Ce dont A-7 aura besoin pour qu'un lien partagé ressemble à quelque chose.

> **Écart assumé à l'exécution.** Le plan logeait ces fichiers dans `website/public/`,
> comme Cocktails, alors que le déploiement les sert **à la racine** du site. La
> prévisualisation locale aurait donc cherché `/jigger-icon.svg` en vain, et le contrôle
> « l'onglet affiche le favicon » n'aurait pas été faisable hors production. Ils sont
> écrits directement dans `website/` : mêmes chemins en local et en ligne. `og.html`,
> qui ne se déploie pas, est simplement absent de la liste d'archive.

**Files:**
- Create: `website/jigger-icon.svg`
- Create: `website/og.html`
- Create: `website/og.png`
- Modify: `website/index.html` — `<head>`

**Interfaces:**
- Consumes: les jetons de couleur de `styles.css`.
- Produces: `/jigger-icon.svg` et `/og.png`, aux mêmes chemins en local et en ligne.

- [ ] **Step 1 : écrire `website/public/jigger-icon.svg`**

L'angle arrondi du cadre, rien d'autre — jigger n'a pas de logo et on n'en invente pas.

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <rect width="64" height="64" rx="14" fill="#07090e"/>
  <rect x="12" y="16" width="40" height="32" rx="8"
        fill="none" stroke="#5b8cff" stroke-width="4"/>
  <path d="M20 32h10" stroke="#86a6ff" stroke-width="4" stroke-linecap="round"/>
</svg>
```

- [ ] **Step 2 : écrire le gabarit `website/public/og.html`**

Il n'est **pas déployé** : il ne sert qu'à produire l'image, une fois, et à la refaire à
l'identique le jour où le cadre change.

```html
<!doctype html>
<meta charset="utf-8">
<title>og — jigger</title>
<style>
  html, body { margin: 0; }
  body {
    width: 1200px; height: 630px; overflow: hidden;
    background: #07090e;
    background-image: radial-gradient(900px 560px at 78% -10%, rgba(91,140,255,.28), transparent 60%);
    color: #eceef5;
    font-family: system-ui, -apple-system, "SF Pro Text", sans-serif;
    display: flex; flex-direction: column; justify-content: center; gap: 26px;
    padding: 0 72px; box-sizing: border-box;
  }
  h1 { font-size: 54px; margin: 0; line-height: 1.1; }
  h1 em { color: #86a6ff; font-style: normal; }
  pre {
    font-family: ui-monospace, "SF Mono", Menlo, monospace;
    font-size: 19px; line-height: 1.4; margin: 0;
    background: #0e111b; border: 1px solid rgba(255,255,255,.14);
    border-radius: 14px; padding: 22px 26px; white-space: pre;
  }
</style>
<h1>jigger — <em>as you type.</em></h1>
<pre>❯ brew install fire
╭──────────────────────────────────────────────────────╮
│❯ brew install                            jigger 0.9.0│
│  ▣  firealpaca                                       │
│  ◆  firebase-cli                                     │
│  ▣  firebird-emu                                     │
│                                                      │
│   ⇥  insert   ↓  browse   ^G  close                  │
╰──────────────────────────────────────────────────────╯</pre>
```

- [ ] **Step 3 : produire `og.png`**

Chrome en mode headless rend le gabarit à la dimension exacte : reproductible, et la
commande se garde dans le README.

```bash
cd website && "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless --disable-gpu --hide-scrollbars \
  --screenshot=og.png --window-size=1200,630 "file://$PWD/og.html"
```

Vérifier ensuite les dimensions — l'aperçu des réseaux attend du 1200×630 :

```bash
sips -g pixelWidth -g pixelHeight website/og.png
```

Attendu : `pixelWidth: 1200`, `pixelHeight: 630`.

- [ ] **Step 4 : compléter le `<head>` de `index.html`**

À insérer après la balise `<meta name="theme-color" …>` :

```html
  <meta property="og:type" content="website">
  <meta property="og:title" content="jigger — one syntax for Homebrew, winget and scoop">
  <meta property="og:description" content="A live package picker in your shell, and one syntax across three package managers. A small Go binary wired into zsh and PowerShell.">
  <meta property="og:url" content="https://jigger.yg-devworks.com/">
  <meta property="og:image" content="https://jigger.yg-devworks.com/og.png">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="jigger — one syntax for Homebrew, winget and scoop">
  <meta name="twitter:description" content="A live package picker in your shell, and one syntax across three package managers.">
  <meta name="twitter:image" content="https://jigger.yg-devworks.com/og.png">
  <link rel="icon" href="/jigger-icon.svg">
```

- [ ] **Step 5 : vérifier**

```bash
website/verifier.sh --reseau; echo "sortie=$?"
```

Attendu : vert. Les `<meta>` ne portent pas de `href="http…"`, donc le contrôle de liens
externes ne les visite pas — c'est voulu : `og:image` pointe sur un site pas encore
publié.

Page servie, l'onglet du navigateur doit afficher le favicon.

- [ ] **Step 6 : commit**

```bash
git add website/public website/index.html
git commit -m "Site : favicon, image d'aperçu et métadonnées de partage"
```

---

## Task 7 : le déploiement

Le décalque de la recette Cocktails, réduit à ce dont une page statique a besoin : ni
`/api`, ni `/admin`, ni `/stats`.

**Files:**
- Create: `website/deploy/nginx-jigger.conf`
- Create: `website/deploy/caddy-jigger.conf`
- Create: `website/deploy-proxmox.sh`
- Create: `website/README.md`

**Interfaces:**
- Consumes: `index.html`, `styles.css`, `app.js`, `public/`.
- Produces: `/var/www/jigger/releases/<horodatage>` et le lien `current` côté serveur ;
  `deploy-proxmox.sh` sans argument.

- [ ] **Step 1 : écrire `website/deploy/nginx-jigger.conf`**

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name jigger.yg-devworks.com;

    root /var/www/jigger/current;
    index index.html;

    access_log /var/log/nginx/jigger.access.log;
    error_log /var/log/nginx/jigger.error.log;

    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; style-src 'self'; img-src 'self'; form-action 'self'; base-uri 'self'; frame-ancestors 'self'" always;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(?:png|svg|css|js)$ {
        expires 7d;
        add_header Cache-Control "public";
    }
}
```

- [ ] **Step 2 : écrire `website/deploy/caddy-jigger.conf`**

```
jigger.yg-devworks.com {
    encode zstd gzip
    reverse_proxy 192.168.50.11:80
}
```

- [ ] **Step 3 : écrire `website/deploy-proxmox.sh`**

Même squelette que celui de Cocktails, y compris la restauration en cas de configuration
invalide — c'est ce qui rend le script rejouable sans crainte.

```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
WEB_HOST="root@192.168.50.11"
PROXY_HOST="root@192.168.50.10"
DOMAIN="jigger.yg-devworks.com"
RELEASE="$(date -u +%Y%m%d%H%M%S)"
WORK_DIR="$(mktemp -d)"
# Les sockets Unix sont limités à 104 octets sur macOS, et TMPDIR y vit sous un
# long /var/folders : on garde les sockets de multiplexage SSH sous /tmp.
CONTROL_DIR="$(mktemp -d /tmp/jigger-ssh.XXXXXX)"
ARCHIVE="$WORK_DIR/jigger-site.tar.gz"

chmod 700 "$CONTROL_DIR"
trap 'rm -rf "$WORK_DIR" "$CONTROL_DIR"' EXIT

SSH_OPTIONS=(
  -o StrictHostKeyChecking=accept-new
  -o ControlMaster=auto
  -o ControlPersist=60
  -o "ControlPath=$CONTROL_DIR/%C"
)

echo "Contrôles avant publication…"
"$SCRIPT_DIR/verifier.sh"

# og.html est le gabarit qui a produit og.png : il n'a rien à faire en ligne.
tar -czf "$ARCHIVE" \
  -C "$SCRIPT_DIR" index.html styles.css app.js \
  -C "$SCRIPT_DIR/public" jigger-icon.svg og.png

echo "Publication des fichiers sur ${WEB_HOST}…"
scp "${SSH_OPTIONS[@]}" \
  "$ARCHIVE" \
  "$SCRIPT_DIR/deploy/nginx-jigger.conf" \
  "${WEB_HOST}:/tmp/"

ssh "${SSH_OPTIONS[@]}" "${WEB_HOST}" bash -s -- "$RELEASE" <<'REMOTE_WEB'
set -euo pipefail
release="$1"
release_dir="/var/www/jigger/releases/$release"

install -d -m 0755 "$release_dir"
tar -xzf /tmp/jigger-site.tar.gz -C "$release_dir"
chown -R root:root "$release_dir"
find "$release_dir" -type d -exec chmod 0755 {} +
find "$release_dir" -type f -exec chmod 0644 {} +

install -d -m 0755 /var/www/jigger
ln -sfn "$release_dir" /var/www/jigger/current

nginx_config=/etc/nginx/sites-available/jigger
nginx_backup="${nginx_config}.before-deploy"
had_nginx_config=false
if [ -e "$nginx_config" ]; then
    cp -a "$nginx_config" "$nginx_backup"
    had_nginx_config=true
fi

install -m 0644 /tmp/nginx-jigger.conf "$nginx_config"
ln -sfn "$nginx_config" /etc/nginx/sites-enabled/jigger

if ! nginx -t; then
    if [ "$had_nginx_config" = true ]; then
        cp -a "$nginx_backup" "$nginx_config"
    else
        unlink /etc/nginx/sites-enabled/jigger 2>/dev/null || true
        rm -f "$nginx_config"
    fi
    echo "Configuration Nginx invalide : restauration effectuée" >&2
    exit 1
fi

systemctl reload nginx
rm -f /tmp/jigger-site.tar.gz /tmp/nginx-jigger.conf
REMOTE_WEB

echo "Ajout de la route HTTPS sur ${PROXY_HOST}…"
scp "${SSH_OPTIONS[@]}" \
  "$SCRIPT_DIR/deploy/caddy-jigger.conf" \
  "${PROXY_HOST}:/tmp/"

ssh "${SSH_OPTIONS[@]}" "${PROXY_HOST}" bash -s -- "${DOMAIN}" <<'REMOTE_PROXY'
set -euo pipefail
domain="$1"
caddyfile=/etc/caddy/Caddyfile
backup="${caddyfile}.before-jigger.$(date -u +%Y%m%d%H%M%S)"
cp -a "$caddyfile" "$backup"

if ! grep -Fq "$domain {" "$caddyfile"; then
    printf '\n' >> "$caddyfile"
    cat /tmp/caddy-jigger.conf >> "$caddyfile"
fi

if ! caddy validate --config "$caddyfile"; then
    cp -a "$backup" "$caddyfile"
    echo "Configuration Caddy invalide : restauration de $backup" >&2
    exit 1
fi

if ! systemctl reload caddy; then
    cp -a "$backup" "$caddyfile"
    systemctl reload caddy
    echo "Échec du rechargement de Caddy : restauration de $backup" >&2
    exit 1
fi
rm -f /tmp/caddy-jigger.conf
REMOTE_PROXY

echo "Vérification de https://${DOMAIN}/…"
curl --fail --silent --show-error --location \
  --retry 12 --retry-delay 5 --retry-all-errors \
  --output /dev/null \
  "https://${DOMAIN}/"

echo "jigger est publié sur https://${DOMAIN}/"
```

- [ ] **Step 4 : écrire `website/README.md`**

````markdown
# Le site de jigger

Une page statique, bilingue, sans build : trois fichiers servis tels quels.
La conception est dans [`docs/specs/2026-08-16-site-jigger-design.md`](../docs/specs/2026-08-16-site-jigger-design.md).

## Prévisualiser

```sh
cd website && python3 -m http.server 8080
```

Puis <http://localhost:8080/>. Ouvrir le fichier directement (`file://`) marche aussi,
mais les chemins absolus (`/styles.css`) ne résolvent pas : passez par le serveur.

## Vérifier

```sh
./verifier.sh            # parité des langues, ancres, commandes d'installation
./verifier.sh --reseau   # en plus : les liens externes répondent
```

Le déploiement lance `verifier.sh` de lui-même et s'arrête si un contrôle échoue.

## Traduire

L'anglais est écrit **en clair dans `index.html`** ; le français vit dans le dictionnaire
`FR` de `app.js`, entre les marqueurs `/* --- FR --- */`. Une clé française absente laisse
l'anglais s'afficher — jamais de clé brute à l'écran.

Une entrée par ligne, de la forme `  'cle.sous': 'texte',` : `verifier.sh` lit ce bloc
avec `grep`, pas avec un analyseur JavaScript.

## Les captures

Toutes les sorties de terminal affichées sont **de vraies sorties**. Chaque bloc porte en
commentaire HTML la commande qui l'a produite ; à rejouer quand l'affichage de jigger
change :

```sh
jigger render --line "brew install fire" --cols 56
jigger render --line "jg " --cols 56
```

## Refaire l'image d'aperçu

`public/og.html` est le gabarit — il n'est pas déployé. Le servir, l'ouvrir dans une
fenêtre de 1200×630, capturer, enregistrer sous `public/og.png`.

## Déployer

```sh
./deploy-proxmox.sh
```

Archive les fichiers, les dépose dans `/var/www/jigger/releases/<horodatage>` sur le LXC
nginx, bascule le lien `current`, installe le vhost, puis ajoute la route HTTPS au Caddy.
Les deux configurations sont validées avant rechargement et restaurées en cas d'échec.
Demande les clés SSH du Proxmox maison.
````

- [ ] **Step 5 : vérifier ce qui est vérifiable sans serveur**

```bash
chmod +x website/deploy-proxmox.sh
bash -n website/deploy-proxmox.sh && echo "syntaxe du script : ok"
website/verifier.sh --reseau; echo "sortie=$?"
```

Attendu : `syntaxe du script : ok`, puis la passe verte.

Contrôle de la liste d'archive — le tar doit nommer des fichiers qui existent :

```bash
for f in index.html styles.css app.js; do test -f "website/$f" || echo "manque $f"; done
for f in jigger-icon.svg og.png; do test -f "website/public/$f" || echo "manque public/$f"; done
```

Attendu : aucune sortie.

- [ ] **Step 6 : commit**

```bash
git add website/deploy website/deploy-proxmox.sh website/README.md
git commit -m "Site : déploiement sur le Proxmox et mode d'emploi"
```

---

## Task 8 : la mise en ligne

**Cette tâche sort du dépôt** : elle touche deux serveurs. Un agent qui exécute ce plan
**s'arrête ici** et rend la main — c'est Yves qui lance le déploiement, ou qui donne son
accord explicite.

**Files:**
- Modify: `docs/ameliorations.md` — A-6 de « En cours » vers « Fait »
- Modify: `docs/historique/2026-08-16.md` — l'entrée « Réalisé »

- [ ] **Step 1 : déployer**

```bash
website/deploy-proxmox.sh
```

Attendu, en dernière ligne : `jigger est publié sur https://jigger.yg-devworks.com/`.

- [ ] **Step 2 : vérifier depuis l'extérieur**

```bash
curl -sI https://jigger.yg-devworks.com/ | head -1
curl -sI https://jigger.yg-devworks.com/styles.css | head -1
curl -sI https://jigger.yg-devworks.com/app.js | head -1
curl -sI https://jigger.yg-devworks.com/og.png | head -1
curl -sI https://jigger.yg-devworks.com/jigger-icon.svg | head -1
```

Attendu : `HTTP/2 200` cinq fois.

Le gabarit, lui, ne doit **pas** être en ligne :

```bash
curl -sI https://jigger.yg-devworks.com/og.html | head -1
```

Attendu : une réponse **200 servant `index.html`** — le vhost renvoie l'accueil pour tout
chemin inconnu (`try_files … /index.html`). Vérifier alors que le contenu servi est bien
la page d'accueil et non le gabarit :

```bash
curl -s https://jigger.yg-devworks.com/og.html | grep -c 'lang-toggle'
```

Attendu : `1` — c'est la page d'accueil ; le gabarit, lui, n'a pas de sélecteur de langue.

- [ ] **Step 3 : regarder la page dans un navigateur**

Sur `https://jigger.yg-devworks.com/` : la bascule FR/EN fonctionne, la console
n'affiche **aucune violation de CSP**, et les deux cadres de terminal sont lisibles sur
un téléphone.

- [ ] **Step 4 : consigner**

Déplacer A-6 de « En cours » vers « Fait » dans `docs/ameliorations.md`, avec la date, le
commit et l'URL. Ajouter l'entrée « Réalisé » du jour dans `docs/historique/`.

```bash
git add docs/ameliorations.md docs/historique/
git commit -m "Site publié : A-6 close"
```

- [ ] **Step 5 : sortir la MR !14 du brouillon**

La branche `feat/site` porte la conception et l'implémentation. Retirer le préfixe
`Draft: ` du titre de la MR, et compléter sa description avec l'URL en ligne.

---

## Auto-relecture

**1. Couverture de la spec**

| Exigence de la spec | Tâche |
|---|---|
| §1 — les fichiers sous `website/` | 1, 6, 7 |
| §1 — déploiement décalqué de Cocktails, releases horodatées, route Caddy | 7, 8 |
| §1 — prévisualisation `python3 -m http.server` | 1 (step 4), 7 (README) |
| §2 — accroche + cadre réel + installation | 1 |
| §2 — la fenêtre vivante, les touches, les flèches | 3 |
| §2 — `jg` et les douze verbes, pas de choix automatique | 3 |
| §2 — les garanties (rien de lent, sortie relayée, `--json`) | 4 |
| §2 — installer (brew, script Windows, Go) | 5 |
| §2 — le bloc de prompt | 4 |
| §2 — Cocktails et les liens croisés | 4 |
| §2 — identité visuelle : cadre en aperçu, favicon aux angles arrondis | 6 |
| §3 — mécanisme `data-i18n`, `localStorage`, repli sur l'**anglais** | 1 |
| §4 — parité des langues | 2 |
| §4 — commandes d'installation mot pour mot | 5 |
| §4 — liens internes et externes | 2 |
| §4 — captures rejouables, commande en commentaire | 1, 3, 4, 7 (README) |
| Non-but — pas de générateur statique, pas de Node | tout : aucun fichier de build |
| Non-but — pas d'analytique, pas de formulaire | 7 : le vhost n'a ni `/api` ni `/admin` |

Aucune exigence sans tâche.

**2. Placeholders** — aucun « TBD » ni « à compléter ». Deux points demandent une
observation plutôt qu'une transcription, et le disent : la sortie réelle de
`jigger prompt` (Task 4, step 5) et la capture d'écran de l'aperçu (Task 6, step 3).

**3. Cohérence des noms** — `data-i18n`, `data-en`, `data-verifier="install"`, les
marqueurs `/* --- FR --- */`, la clé `localStorage` `jigger-lang`, `setLang`, les classes
`.frame`, `.cmd`, `.keys`, `.verbs`, `.grid-3`, `.wrap`, `.eyebrow`, `.lede` : employés
partout sous la même forme. Les ancres `#popup`, `#facade`, `#install` naissent en Task 2
(vides, pour que le contrôle de liens passe) et sont remplies aux Tasks 3 et 5 ;
`#garanties`, `#prompt` et `#cocktails` naissent remplies en Task 4 et ne sont visées par
aucun lien de navigation.
