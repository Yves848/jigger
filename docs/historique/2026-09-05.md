# Historique — 2026-09-05

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 08:41:50 — Demande

> relance le garde-fou du miroir pour vérifier

## 08:49:36 — Demande

> deployer

# 2026-09-05

## 08:35 — Réalisé : garde-fou du miroir relancé, puis site déployé

**Demandes :** relancer le garde-fou du miroir pour vérifier, puis déployer.

**Actions :**
- Garde-fou vérifié trois fois : `go run ./tools/miroir` en local (lecture seule, sans
  jeton), la pipeline **57** partie seule à 06:03 UTC, et la pipeline **58** déclenchée à
  la main via `pipeline_schedules/1/play` — donc avec `-issue` et `GARDE_FOU_TOKEN`, comme
  la nuit. Même verdict partout : « miroir à jour : les deux dépôts portent les mêmes
  références. » Aucune issue d'écart ouverte sur le projet.
- Déploiement du site par `website/deploy-proxmox.sh` : huit contrôles au vert, empreintes
  `styles.css?v=1368b970` et `app.js?v=90136d5e`, fichiers poussés sur `192.168.50.11`,
  route HTTPS rechargée sur `192.168.50.10`.
- Vérification externe indépendante du script : les quatre pages, `robots.txt`,
  `sitemap.xml`, `og.png`, un média et les deux ressources empreintes répondent en `200` ;
  une URL inconnue rend `404` ; `http://` redirige en `308` vers `https://` ; certificat
  TLS valide ; les empreintes servies dans le HTML sont bien celles annoncées.

**Fichiers :** aucun — déploiement et vérifications seulement.

**Décisions / notes :**
- Le script de déploiement a été **refusé par le classificateur du mode automatique**
  (sessions SSH `root@` vers deux machines du homelab + publication publique). Lancé par
  l'utilisateur lui-même ; rien n'a été contourné.
- Conséquence assumée du bump anticipé, signalée avant publication et vérifiée après : le
  site affiche **`jigger 0.17.0`** dans ses deux cadres de terminal alors que `brew` et
  GitHub servent la `0.16.0`. Aucun lien de téléchargement n'est concerné — les commandes
  affichées ne portent pas de numéro.
- La planification du garde-fou est réglée sur `0 8 * * *` Europe/Paris mais s'exécute à
  06:03 UTC : GitLab étale les crons pour éviter les pics.

## 08:54:04 — Demande

> installer l'application en local

## 08:55:49 — Demande

> installe le plugin git

## 08:58:33 — Demande

> ouvre les issues

## 09:01:38 — Demande

> la popup n'apparaît pas quand je tape `git`+espace

## 09:15:27 — Demande

> oui

## 09:40 — Réalisé : le popup ne s'ouvre pas sur `git`, diagnostic et correctif

**Demande :** le popup n'apparaît pas quand je tape `git` + espace ; puis « attaque #141 ».

**Actions :**
- Diagnostic en deux temps. Le binaire répond parfaitement (`render --line "git "` →
  popup à six verbes) : le défaut est dans le greffon zsh, dont `_jigger_is_watched` ne
  déclenche que sur la liste **statique** `JIGGER_COMMANDS` (ligne 181), jamais reliée à
  la découverte des plugins. → **#140**.
- Contournement `JIGGER_COMMANDS="… git"` posé dans `~/.zshrc` (lien vers `~/git/config`),
  **essayé puis retiré** : une fois `git` armé, `git checkout `, `git push origin ` et
  `git commit -m ` se voyaient proposer des noms de dépôts, `exec=1`. Fichier revenu à
  l'octet près, rien commité dans `config`.
- Second défaut isolé, **préexistant et non lié aux plugins** (`brew blahblah ` propose
  tout le catalogue) → **#141**, déclarée bloquante pour #140.
- Correctif de #141 en TDD : quatre cas posés d'abord, le premier échouant sur les trois
  symptômes. Contrat optionnel `pm.Exhaustif`, garde dans `completeWith`, déclaration par
  `PluginManager`. Consigné en #142, #141 et #142 fermées.

**Fichiers :**
- `internal/pm/exhaustif.go` — le contrat, `VerbesExhaustifsDe`, `VerbeConnu`
- `internal/complete/complete.go` — la garde, qui rend un résultat `Silencieux`
- `internal/plugin/discovery.go` — `VerbesExhaustifs() → true`
- les deux fichiers de test correspondants

**Décisions / notes :**
- **Une erreur d'analyse corrigée en cours de route** : j'avais lu la garde de
  `completeFacade:194` et conclu qu'un verbe inconnu était déjà ignoré. Ce chemin ne sert
  que les lignes `jigger`/`jg` ; une ligne `git …` passe par `completeWith`, où
  `isPackage` ne regardait que la **position** du mot.
- **Le durcissement ne vaut que pour les plugins**, arbitré avec l'utilisateur :
  `Subcommands()` d'un plugin vient de son `config.json` et est exhaustif par
  construction, tandis que les 25 sous-commandes de brew sont un choix parmi une
  centaine — `brew fetch fir` doit continuer de proposer des formules.
- La garde **se tait** au lieu d'afficher « aucun candidat » : le cadre vide à chaque
  frappe est le défaut que l'ADR-0006 avait déjà fait corriger pour `ssh`.
- Comparaison de verbe **insensible à la casse** : pacman déclare `-Rns`, complete passe
  `-rns`.
- #140 reste ouverte : le mécanisme (cache écrit par `warm`) et la question de savoir si
  un plugin doit s'armer **par défaut** — `git` est omniprésent — restent à trancher.

**Commits :** `fb5f6b4` Un verbe qu'un plugin ne déclare pas ne propose plus de paquets

## 10:33:16 — Demande

> fais #140 aussi

## 10:55 — Réalisé : un plugin arme le popup sur son propre mot (#140)

**Demande :** faire #140 aussi.

**Actions :**
- Mesure préalable qui a changé le plan : `git st`, `git status`, `git push` rendaient
  encore **5 lignes** — un cadre « aucun candidat » à chaque frappe. #142 n'avait durci que
  les verbes *suivis d'un argument* ; le premier mot restait à traiter, et sans lui armer
  `git` aurait été pire que de ne rien faire. Fait en TDD, avec le cas de non-régression
  qui garde son cadre à un natif.
- Mécanisme retenu (piste 2 de l'issue) : `jigger warm` dépose les mots découverts dans
  `plugin-commands`, au fond du cache ; les greffons le lisent au chargement **sans lancer
  de processus** — `$(<…)` en zsh, `Get-Content` en PowerShell. Fichier réécrit à chaque
  warm, y compris vide, pour qu'un plugin désinstallé cesse d'être armé.
- `JIGGER_PLUGIN_COMMANDS=0` pour refuser l'armement sans réécrire `JIGGER_COMMANDS`.
- Résolution du dossier de cache extraite du bloc `JIGGER_PROMPT` en zsh : le bloc des
  mots surveillés en a besoin plus haut.
- Documentation mise à jour dans les deux langues. Consigné en #143, #140 fermée.

**Fichiers :**
- `internal/managers/managers.go` — `PluginCommands`, `WritePluginCommands`, `PluginCommandsPath`
- `main.go` — `warm` écrit le fichier, son échec remonte en code 1
- `internal/complete/complete.go` — silence sur le premier mot d'un exhaustif
- `shell/jigger.plugin.zsh`, `shell/jigger.psm1` — lecture et réglage, des deux côtés
- `docs/plugins.md`, `docs/fr/plugins.md` — le warm arme le popup ; comment ne pas armer

**Décisions / notes :**
- **Armer par défaut, avec refus possible.** L'ADR-0005 refuse de *retirer le choix* à
  l'utilisateur, pas d'armer par défaut — `ssh`, `scp` et `sftp` y sont déjà.
- **Un fichier plutôt qu'un appel** : les deux greffons refusent de payer un fork au
  chargement du shell. Conséquence assumée : un plugin installé mais jamais réchauffé n'est
  pas armé, ce qui est cohérent avec la procédure documentée.
- Vérifié sous `pwsh`, disponible sur cette machine : `smoke.ps1` passe ses 96 assertions.
- Deux incidents d'outillage de mon fait, sans dégât : un `printf` dont les accents graves
  ont été interprétés (quelques `ssh` sans argument), puis une ligne parasite dans la
  commande suivante. Les deux ont échoué avant d'écrire quoi que ce soit.

**Commits :** `a587b39` Un plugin arme le popup sur son propre mot

## 10:45:20 — Demande

> prépare la release

## 11:30 — Réalisé : la v0.17.0 est publiée

**Demande :** prépare la release.

**Actions :**
- Croisement commits ↔ issues prescrit par le skill : **trois écarts** trouvés et
  rattrapés avant d'agréger — `0a9afe2` et `b5a7e0a` (deux journaux) n'avaient jamais été
  consignés (→ #144), et #128 portait encore le label `v0.16.1` d'un cycle qui n'a jamais
  existé (réétiquetée `v0.17.0`).
- Section `## [v0.17.0]` écrite dans le `CHANGELOG.md` : deux entrées seulement, #143 en
  *Added* et #142 en *Fixed*, les onze autres issues étant internes. Extraction éprouvée
  avec l'`awk` exact de la CI : 43 lignes.
- Deux liens d'ADR inventés, corrigés avant commit (`0005-completion-sans-facade.md` et
  `0006-silence-sur-catalogue-vide.md`), puis tous les liens ADR du fichier vérifiés
  existants.
- Les quatre pièges du `forge.md` levés dans l'ordre, **avant** le tag.
- `CHANGELOG` commité et poussé (`188706c`), **puis** tag annoté `v0.17.0` posé dessus et
  poussé. La release n'a pas été créée à la main : c'est le job `release:` qui s'en charge.

**Fichiers :**
- `CHANGELOG.md` — section `v0.17.0`

**Décisions / notes :**
- **Mineur et non correctif** : #143 ajoute une capacité qui n'existait pas. Le numéro
  était de toute façon déjà posé depuis le 4 septembre (#129).
- **Pipeline 59 : les cinq jobs au vert**, `github:` compris **du premier coup** — c'est
  ce que tout le travail sur le jeton visait, et la v0.16.0 ne l'avait jamais obtenu.
- Vérifié depuis l'extérieur : release GitLab `jigger 0.17.0` avec ses 5 liens et 43
  lignes de notes ; miroir GitHub à `latest = v0.17.0` avec ses 5 archives.

**Commits :** `188706c` Version 0.17.0 : les plugins arrivent jusqu'au shell · tag `v0.17.0`

## 15:37:00 — Demande

> fais #138 et #139

## 12:20 — Réalisé : les deux défauts du plugin git (#138, #139)

**Demande :** fais #138 et #139.

**Actions :**
- Écrits en TDD, 9 cas posés avant l'implémentation.
- **#138** : `desambiguer` qualifie les homonymes par leur chemin relatif à la racine,
  **racine comprise** ; `trouver` accepte un nom qualifié ou un chemin et refuse le nom nu
  en listant les candidats ; repli sur le chemin absolu si deux racines portent le même nom
  de base.
- **#139** : la branche se lit par `symbolic-ref --short HEAD`, avec repli sur
  `rev-parse --short` pour la tête détachée.
- Deux effets de bord latents traités : `connus.json` écrit avec le nom du **dossier** (il
  sert à recloner), et `estClone` comparant aussi ce nom.
- Éprouvé en bac à sable — deux clones homonymes fabriqués, `uninstall` sur le nom nu
  refuse et sort en 1 **sans rien supprimer** — puis sur les vrais dépôts après
  réinstallation du plugin. Consigné en #145, #138 et #139 fermées.

**Fichiers :**
- `cmd/jigger-git/main.go` — `desambiguer`, `qualifier`, `trouver`, `decrire`, `retenir`,
  `estClone`, `depots`
- `cmd/jigger-git/main_test.go` — 9 cas

**Décisions / notes :**
- **La piste 1 de #138 a changé de forme** : la sortie de `list` est un protocole à champs
  fixes, on ne peut pas y ajouter une colonne de chemin. C'est le `name` qui est qualifié —
  ce qui répond mieux, le nom affiché devenant directement utilisable par `uninstall`.
- **Inclure la racine dans la qualification** est le point que l'issue n'avait pas vu : sans
  elle, le clone posé à la racine garderait le nom nu et resterait visé en silence.
- **Un de mes tests était faux et l'implémentation l'a démenti** : `/a/app` et `/b/app` se
  qualifient déjà en `a/app` et `b/app`. Le repli sur l'absolu ne sert que si les
  **racines** portent le même nom de base ; le test a été refait sur ce vrai cas.
- Version cible **v0.18.0** : la v0.17.0 est publiée depuis ce matin.

**Commits :** `c649de2` Plugin git : deux clones du même nom ne se confondent plus

## 15:48:39 — Demande

> fais la 0.17.1

## 15:53:27 — Demande

> redéploie le site

## 15:55:44 — Demande

> ajoute la règle de permission pour le script de déploiement

## 14:05 — Réalisé : v0.17.1 publiée, règle de permission, et une course découverte

**Demandes :** prépare la 0.17.1 ; redéploie le site ; ajoute la règle de permission pour
le script de déploiement.

**Actions :**
- **v0.17.1 préparée et publiée.** Croisement commits ↔ issues : deux journaux non
  consignés (→ #146) et #145 rebasculée de `v0.18.0` sur `v0.17.1`. Bump des seize
  mentions, section `CHANGELOG`, quatre pièges du `forge.md` levés, tag posé après le
  commit du CHANGELOG.
- **La pipeline 60 a d'abord échoué** sur `github:` en `422`. Diagnostic : le miroir
  GitLab → GitHub est **asynchrone** et n'avait pas encore répliqué le tag ; GitHub refuse
  de créer une release sur un tag qu'il ignore. Ni le jeton, ni une panne — une **course**,
  que la v0.17.0 avait gagnée six heures plus tôt. Synchronisation forcée
  (`remote_mirrors/2/sync`), job relancé → succès, pipeline 60 au vert. Consigné en #147.
- **Règle de permission** pour `website/deploy-proxmox.sh` ajoutée dans
  `.claude/settings.local.json`.

**Fichiers :**
- `CHANGELOG.md`, `main.go` et les huit fichiers de bannières — bump 0.17.1
- `.claude/settings.local.json` — trois formes exactes d'invocation du script (hors dépôt)

**Décisions / notes :**
- **Le fichier local et non le `settings.json` du projet** : ce script pousse vers deux
  machines du homelab, et une règle commitée l'accorderait à quiconque clone — miroir
  GitHub public compris. Trois formes exactes plutôt qu'un joker.
- **La chaîne de release n'était pas fiable, elle avait eu de la chance.** Le job `github:`
  part dans la seconde qui suit le tag ; le miroir, lui, s'exécute quand il s'exécute. La
  piste retenue dans #147 est de faire attendre le tag au script, avec plafond et échec
  explicite — la doctrine du fichier étant déjà de s'arrêter plutôt que de faire semblant.
- Le `forge.md` compte quatre pièges de publication ; celui-ci en est un **cinquième**.
- **Le site n'est pas redéployé** : le classificateur refusait encore le script au moment
  de la demande. La règle de permission vient d'être posée et prendra effet à la prochaine
  session.

**Commits :** `72ab070` Version 0.17.1 · tag `v0.17.1` · `4fb8ee3` et `c649de2` (correctifs)

## 16:35:19 — Demande

> fais #147

## 14:30 — Réalisé : le script de publication attend le miroir (#147)

**Demande :** fais #147.

**Actions :**
- Bloc d'attente ajouté **en tête** de `tools/publier-github.sh` : sondage de
  `GET /repos/:depot/git/refs/tags/:tag` jusqu'à réponse, plafond d'une minute réglable
  par `JIGGER_ATTENTE_TAG`, échec explicite au-delà avec la commande de synchronisation
  forcée toute prête.
- Aucun harnais de test n'existe pour ce script — éprouvé à la main sur les **trois**
  chemins, avec une copie tronquée juste après le bloc d'attente pour ne rien publier :
  tag présent (0,4 s, sortie 0), tag absent plafond 0 (échec immédiat, sortie 1), tag
  absent plafond 10 s (boucle d'attente puis échec).
- `.claude/forge.md` passe à **cinq pièges**. Consigné en #148, #147 fermée.

**Fichiers :**
- `tools/publier-github.sh` — le bloc d'attente
- `.claude/forge.md` — cinquième piège de publication

**Décisions / notes :**
- **Le contrôle est en tête, pas juste avant la création de la release.** Sans le tag,
  rien de ce qui suit n'a de sens, et l'erreur doit nommer sa cause plutôt que de sortir
  d'un `curl` trois étapes plus loin.
- **Plafond court et échec bruyant** : au-delà d'une minute ce n'est plus un décalage de
  réplication mais un miroir en panne. Même doctrine que le jeton absent.
- Le `forge.md` retient qu'un `update_status: finished` sans erreur ne dit **pas** que la
  dernière poussée est répliquée, seulement que la dernière *exécution* s'est terminée —
  c'est `last_successful_update_at` qui tranche. C'est ce qui a permis de distinguer
  « miroir en retard » de « miroir en panne » pendant le diagnostic.
- Le tableau des issues ouvertes est de nouveau **vide**.

**Commits :** `8edafc3` Le script de publication attend que le tag soit arrivé sur le miroir

## 16:53:23 — Demande

> le plugin git ne fonctionne pas bien.  La liste des verbes proposés n'a rien à voir avec les commandes git

## 16:58:43 — Demande

> je ne veux pas qu'on ré-écrive un `git`.  LE but premier de jigger est d'intercepter une commande et de présenter, dans la popup, les commandes, options et paramètres qui sont disponible pour cette commande interceptée.  La raison de vivre des plugin, c'est d'implémenter un helper convivial qui apporte des fonctionalités supplémentaires (souvent visuelles) à une commande existante

## 17:02:45 — Demande

> retire git des plugins livrés, et fais l'ADR

## 15:50 — Réalisé : le plugin git retiré, et l'ADR-0009

**Demandes :** le plugin git ne fonctionne pas bien, les verbes proposés n'ont rien à voir
avec git ; puis « retire git des plugins livrés, et fais l'ADR ».

**Actions :**
- **Diagnostic.** Le plugin ne rendait pas `git` commode, il lui substituait un vocabulaire :
  six verbes dont aucun n'existe dans git, et une ligne complétée qui se lit comme du git
  tout en exécutant `jigger-git run install …`. Vérifié dans le code que ce n'était pas une
  implémentation bâclée mais **la seule forme que le descripteur sait exprimer** —
  `Options()` rend `nil` (`discovery.go:315`), un seul `Pool` parmi trois valeurs, deux
  caches pour toute la machine.
- **Retrait** de `packaging/plugins/git/config.json`, section de documentation remplacée par
  la raison du retrait dans les deux langues, exemples d'installation reformulés. → #149
- **ADR-0009** rédigée. → #150
- **Copie locale retirée** (`~/.config/jigger/plugins/git/`) et `warm` rejoué :
  `plugin-commands` est vide, `git` n'est plus surveillé.

**Fichiers :**
- `packaging/plugins/git/config.json` — supprimé
- `docs/plugins.md`, `docs/fr/plugins.md` — raison du retrait, exemples reformulés
- `docs/adr/0009-viviers-de-plugin-par-verbe.md` — créé

**Décisions / notes :**
- **Trois mesures ont rouvert la règle d'or du plan** (« un plugin ne peut pas être un
  sous-processus appelé par `render` ») : `render` **est déjà** un sous-processus par frappe
  (`jigger.plugin.zsh:253`), une frappe coûte **11 ms**, une réponse git contextuelle **7 ms**.
  La règle interdisait la lenteur ; elle a été lue comme interdisant le fork.
- **L'option tentante était de nommer plusieurs viviers tous réchauffés** — presque aucun
  changement, règle respectée à la lettre — et elle est fausse par construction : une branche
  d'il y a une minute resterait absente jusqu'au prochain `warm`.
- `cmd/jigger-git` est **conservé** : sa descente et ses gardes serviront à la réécriture.
  Décision à contester si le dépôt préfère ne pas garder de code non référencé.
- ADR numérotée et formatée à la main : pas d'index dans ce dépôt, format maison stable sur
  huit fichiers, le script du skill en aurait imposé un autre.
- **Erreur d'énumération de ma part** : j'ai annoncé deux fichiers à supprimer dans la copie
  locale, il y en avait trois — `connus.json` (3,4 ko), la table des URL apprises, est parti
  avec. Reconstructible par parcours des clones, mais il aurait fallu le nommer.

**Commits :** `ce0c998` retrait du plugin · `7c6ec06` ADR-0009

## 17:17:57 — Demande

> ok, on continue

## 16:40 — Réalisé : le descripteur sait décrire un helper (étape 2 de l'ADR-0009)

**Demande :** « ok, on continue ».

**Actions :**
- Étape 2 du plan : extension du protocole, en TDD — 9 cas dans `plugin`, 4 dans `complete`,
  écrits avant l'implémentation.
- `Verb.Options` et `Config.Pools` ajoutés ; type `Vivier` avec ses deux régimes ;
  validation qui refuse un vivier non déclaré **en nommant le nom fautif**.
- `pm.Viviers`, contrat optionnel sur le modèle de `Bindings`/`Elevateur`/`Exhaustif` ;
  branché dans la seule branche « paquet » de `completeWith`.
- Protocole du régime `direct` tranché ici, l'ADR le laissant à l'implémentation : binaire
  **du plugin** avec les arguments déclarés, une ligne par candidat (`nom` ou
  `nom<TAB>badge`), délai de 200 ms réglable par `JIGGER_DELAI_VIVIER`, silence en cas
  d'échec ou de dépassement.
- Documentation dans les deux langues. Consigné en #151.

**Fichiers :**
- `internal/plugin/discovery.go`, `internal/plugin/protocol.go`, `internal/pm/viviers.go`,
  `internal/complete/complete.go` et les tests
- `docs/plugins.md`, `docs/fr/plugins.md`

**Décisions / notes :**
- **Le binaire interrogé est toujours celui du plugin**, le vivier ne déclarant que des
  arguments. Un descripteur ne doit pas pouvoir faire lancer n'importe quel programme à
  chaque frappe — contrainte 4 du plan, que le régime `direct` rend plus sensible encore.
- **Le délai n'était pas tenu, et le défaut préexistait.** `c.Wait()` attend la fermeture du
  tuyau ; un plugin qui est un script laisse ses enfants le tenir ouvert. Un `sleep` a fait
  attendre 5 s un délai réglé sur 50 ms. Réclamation en arrière-plan ; le paquet passe de
  5 s à 0,08 s. Le réchauffement avait le même défaut, masqué par son délai de 30 s.
- **`res.cat` ne suivait pas le vivier** : `InsertItem` aurait cherché le candidat dans une
  table qui ne l'a jamais contenu. Trouvé en relisant, pas par un test — il faudrait en
  écrire un.
- Les trois valeurs historiques de `pool` restent valides sans bloc `pools` : contrat
  public depuis la 0.16.0, tenu par un test.

**Commits :** `31167c9` Le descripteur d'un plugin sait décrire un helper de commande

## 20:45:39 — Demande

> étape 3

## 17:30 — Réalisé : le plugin git renaît en helper (étape 3 de l'ADR-0009)

**Demande :** « étape 3 ».

**Actions :**
- Descripteur `packaging/plugins/git/config.json` réécrit : **17 verbes git réels**, quatre
  viviers `direct` (branches, distants, étiquettes, fichiers modifiés) et des options par
  verbe.
- `cmd/jigger-git` supprimé — 950 lignes.
- Éprouvé en configuration jetable, puis chez l'utilisateur après `make install`.
- Documentation réécrite dans les deux langues. Consigné en #152.

**Fichiers :**
- `packaging/plugins/git/config.json` — recréé, tout autre
- `cmd/jigger-git/` — supprimé
- `internal/plugin/discovery.go` — l'ordre du vivier n'est plus écrasé
- `internal/facade/executer.go`, `internal/plugin/discovery_test.go` — commentaires corrigés
- `docs/plugins.md`, `docs/fr/plugins.md`

**Décisions / notes :**
- **Le helper n'a pas de binaire, et c'est la découverte de l'étape.** Le binaire d'un vivier
  est celui du plugin ; le binaire d'un helper git **est git**. Le descripteur déclare donc
  `cmd: "git"`, et le vrai git exécute les verbes *et* peuple les viviers. Le plugin tient
  en un fichier JSON et s'installe sans rien compiler — ce qui rend inutiles les 950 lignes
  que j'avais gardées « pour la réécriture ».
- **L'ordre du vivier était écrasé.** `Candidats()` retriait alphabétiquement et jetait le
  `--sort=-creatordate` du descripteur : `v0.1.2` sortait avant `v0.17.1`. L'ordre du
  plugin est désormais préservé — il l'a choisi, souvent par pertinence.
- **Deux commentaires étaient devenus faux**, donnant `jigger-git` comme binaire d'un plugin
  `git`. Corrigés : un helper fait coïncider le mot et le binaire, un gestionnaire tiers non.
- **Piège d'installation rencontré** : le binaire de `~/.local/bin` datait d'avant
  l'extension, son `validate()` refusait `pool: "branches"` et écartait le descripteur **en
  silence** (« descripteur invalide : skip »). Symptôme trompeur — `git ⇥` proposait des
  formules Homebrew. `make install` a tout réglé. Un descripteur trop récent pour le binaire
  installé est indiscernable d'un descripteur absent : cela mériterait un mot sur la sortie
  d'erreur.
- Contre-épreuve du vivier contextuel : un fichier créé apparaît dans `git add ⇥` et
  disparaît dès qu'il est retiré.

**Commits :** `3bd82e0` Le plugin git renaît en helper, et n'a plus de binaire

## 20:55:35 — Demande

> fais la 0.18.0

## 21:01:15 — Demande

> redéploie le site

## 21:20 — Réalisé : v0.18.0 publiée, site redéployé, et un raisonnement démenti

**Demandes :** fais la 0.18.0 ; redéploie le site.

**Actions :**
- **v0.18.0 publiée.** Croisement commits ↔ issues : cinq journaux non consignés (→ #153).
  Bump des seize mentions, section `CHANGELOG` où **#149 et #152 sont fondues en une seule
  entrée** — le plugin `git` a été retiré puis réécrit dans la même version, personne n'a vu
  l'entre-deux, les notes racontent donc un remplacement.
- **Site redéployé** en `0.18.0` — il servait encore `0.17.0`, deux versions de retard. La
  **règle de permission a joué** : le script s'est lancé sans intervention.
- **Pipeline 61 en échec**, puis verte après synchro forcée du miroir et relance.

**Fichiers :**
- `CHANGELOG.md`, `main.go` et les huit fichiers de bannières
- `.claude/forge.md` — correction du cinquième piège

**Décisions / notes :**
- **Mon raisonnement de #148 était faux, et la v0.18.0 l'a démontré.** Le correctif a fait
  exactement ce qui était spécifié — attendre, annoncer, échouer en nommant sa cause — mais
  j'avais posé qu'au-delà d'une minute le miroir est *en panne*. Mesures : dernière synchro
  réussie à `18:59:48`, tag poussé à `18:59:50` — **deux secondes trop tard** — et miroir
  toujours pas rejoué **vingt-cinq minutes** plus tard, état `finished`, sans erreur.
- **Le miroir ne repart pas tout seul** quand une poussée arrive juste après son passage.
  Attendre ne peut donc pas suffire, *quelle que soit la durée du plafond*. La piste 3 de
  #147 — déclencher le miroir depuis la CI — que j'avais écartée d'un « la piste 1 obtient
  le même résultat sans rien demander », est la seule qui traite la cause. → #154
- `forge.md` corrigé (#155) : une attente expirée veut dire qu'il faut **réveiller** le
  miroir, pas chercher une panne. On tranche en comparant `last_successful_update_at` à la
  date du tag.
- Le rattrapage — synchro forcée puis relance — a fonctionné **deux fois**, v0.17.1 et
  v0.18.0. C'est reproductible, donc documenté.

**Commits :** `495639e` Version 0.18.0 · tag `v0.18.0` · `32ad6d4` correction du forge.md

## 21:08:32 — Demande

> je veux tester la dernière version en local

## 21:13:15 — Demande

> ça marche mais on n'a pas vraiment de bénéfice à utiliser ce plugin.  Soit on pousse l'automatisation plus loin, soit on stoppe

## 22:10 — Réalisé : le helper affiche du contexte, ou il ne sert à rien

**Demande :** « ça marche mais on n'a pas vraiment de bénéfice à utiliser ce plugin. Soit on
pousse l'automatisation plus loin, soit on stoppe. »

**Actions :**
- **Analyse du reproche, qui était juste.** zsh complète déjà branches, fichiers, distants et
  options de git. Le plugin livrait la **parité de complétion**, pas un helper. Constat
  mesuré : le popup sait afficher une seconde colonne (`ssh ⇥` y met l'adresse des hôtes),
  et mes viviers la laissaient vide en ne rendant que des noms nus.
- Vivier `direct` étendu à `nom<TAB>badge<TAB>contexte`, même convention que le cache des
  installés. Descripteur enrichi avec les formats de git. Aucun code git écrit.
- Cas du retard vérifié sur un dépôt fabriqué : `vieille  [behind 3] 0 seconds ago`.
- Documentation dans les deux langues. Consigné en #157.

**Fichiers :**
- `internal/plugin/discovery.go` — troisième champ, posé dans `Versions`
- `packaging/plugins/git/config.json` — formats de contexte, puis lisibilité rendue
- `docs/plugins.md`, `docs/fr/plugins.md`

**Décisions / notes :**
- **Deux limites assumées et dites** : les distants gardent des noms nus (`git remote -v`
  met l'URL au champ du badge, indéplaçable sans post-traitement donc sans binaire) et les
  fichiers aussi (`git status` ne sait pas se formater). **Un helper sans binaire est borné
  par ce que la commande assistée sait produire** — c'est le prix de la propriété « aucun
  exécutable à écrire ».
- **Un contexte trop long chasse le nom** au lieu d'être tronqué : cadre débordé, nom
  disparu. Les natifs n'y mettaient que des versions courtes ; un descripteur tiers y met ce
  qu'il veut. → #156, format des étiquettes raccourci en attendant.
- **`json.dumps` avait reformaté le descripteur** en 297 lignes. Rendu à ses 58 lignes
  alignées : c'est le seul exemple complet du format qu'un auteur de plugin ait sous les yeux.
- Le popup affichait déjà `Versions` pour `ssh` — la capacité existait, elle n'était pas
  utilisée. Chercher ce qui existe avant d'ajouter aurait fait gagner un tour.

**Commits :** `d06f748` le contexte · `698cf51` lisibilité du descripteur

## 21:24:56 — Demande

> fais #156

## 22:45 — Réalisé : le contexte cède avant le nom (#156)

**Demande :** fais #156.

**Actions :**
- Correctif écrit en TDD : deux cas d'abord — le nom survit à quatre largeurs et deux
  états, et un contexte court n'est pas mutilé — le premier échouant sur le défaut.
- Le contexte est borné à la **moitié de la ligne** avant tout calcul, dans
  `internal/ui/frame.go`.
- **Format riche des étiquettes restauré** dans le descripteur livré : la troncature le rend
  de nouveau utilisable. Consigné en #158, #156 fermée.

**Fichiers :**
- `internal/ui/frame.go` et son test
- `packaging/plugins/git/config.json` — date **et** début du sujet
- `docs/plugins.md`, `docs/fr/plugins.md` — la troncature dite plutôt que la consigne

**Décisions / notes :**
- **La règle : le contexte cède avant le nom, jamais l'inverse.** Le nom est ce qui sera
  inséré ; le contexte n'est qu'une aide à choisir.
- Le calcul ne bornait **que le nom**, d'après la partie droite réelle. Le commentaire du
  code explique bien pourquoi — un identifiant winget suivi d'une version longue débordait
  la ligne — mais la protection était asymétrique.
- **Le champ n'avait jamais eu à se défendre** : les natifs n'y mettent que des versions et
  des adresses. C'est l'ADR-0009 qui l'expose à un descripteur tiers.
- **Effet de bord heureux, vérifié puis adopté** : le contournement de #157 — étiquettes
  réduites à la date — n'a plus lieu d'être. Corriger la cause a rendu la fonctionnalité
  meilleure que le contournement ne l'avait laissée.
- La seconde question de #156 — répartir autrement quand les noms sont courts et le contexte
  long — est **laissée sans réponse et dite comme telle** : elle demanderait de mesurer tous
  les noms de la page avant de dessiner.

**Commits :** `4680032` Le contexte cède avant le nom, et non l'inverse

## 21:29:44 — Demande

> fais #154

## 23:15 — Réalisé : le script réveille le miroir (#154)

**Demande :** fais #154.

**Actions :**
- `reveiller_miroir()` ajoutée à `tools/publier-github.sh`, appelée **avant** la boucle
  d'attente : `POST remote_mirrors/N/sync`, avec `CI_JOB_TOKEN` puis `GITLAB_API_TOKEN`.
- Éprouvée sur les trois chemins en extrayant la fonction du script : aucun jeton, jeton
  d'API valide, jeton refusé.
- `.claude/forge.md` complété. Consigné en #159, #154 fermée.

**Fichiers :**
- `tools/publier-github.sh` — le réveil
- `.claude/forge.md` — ordre des jetons et ce que le journal affichera

**Décisions / notes :**
- **La question du jeton n'est pas tranchée à l'aveugle, elle est déléguée à la mesure.** Le
  droit de `CI_JOB_TOKEN` sur cet endpoint n'est pas documenté de façon fiable ; le script
  l'essaie et imprime la réponse de l'API. Deux releases ont déjà échoué parce qu'un
  raisonnement plausible avait remplacé une mesure — cette fois la mesure est faite par la
  CI, pas devinée.
- **Aucun jeton n'est requis** : sans jeton qui marche, on se contente d'attendre, soit
  exactement le comportement précédent. C'est ce qui permet de livrer sans bloquer sur une
  variable que personne ne sait encore nécessaire.
- Reste à décider **sur pièces** : si le jeton de job est refusé, il faudra une variable
  masquée de portée `api`, capable d'écrire sur le projet. C'est le coût que #147 avait
  refusé de payer.
- Le tableau des issues ouvertes est **vide**.

**Commits :** `8cab1ec` Le script réveille le miroir avant de l'attendre

