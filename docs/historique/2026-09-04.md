# Historique — 2026-09-04

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 08:16:18 — Demande

> J'aurais voulu qu'il y ait les animations (GIF) directement dans la doc web.  Ce qu'on va faire, c'est ajouter une section qui guide pas à pas l'utilisateur.  On va partir de l'installation, jusqu'à l'utilisation complète de jigger.  Que ça soit par l'appel de `jigger` directement ou par l'utilisation du package manager de l'os avec interception et affichage de la popup.


## 08:35 — Réalisé : les animations rendues visibles, et une page « parcours »

**Demande :** avoir les animations directement dans la doc web, et ajouter une section
qui guide pas à pas — de l'installation à l'usage complet, aussi bien par l'appel direct
de `jigger` que par l'interception du gestionnaire natif.

**Le diagnostic, qui a changé la première demande.** La machine a « Réduire les
animations » actif (`prefers-reduced-motion`). `activerDemos()` sortait alors
immédiatement : aucune vidéo n'était même chargée, `readyState` à 0 sur les onze
démonstrations. L'utilisateur n'avait donc **jamais vu une seule animation** du site —
rien que des images fixes, sans que rien ne le signale ni n'offre de recours. Ce n'était
pas un problème de format MP4/GIF : le site s'interdisait de démarrer quoi que ce soit
chez lui. Passer au GIF l'aurait contourné, au prix de 0,9 → 2,1 Mo et de toute
possibilité de mettre en pause (WCAG 2.2.2). Arbitrage retenu après confirmation : un
contrôle de lecture explicite.

**Actions :**
- **Bouton de lecture sur chaque démonstration**, pour tout le monde — une boucle sans
  moyen de l'arrêter est un défaut d'accessibilité. Il couvre le cadre entier, s'efface
  pendant la lecture, revient au survol ou au clavier ; libellé bilingue passant par
  `aria-label` et non par `data-i18n`, puisqu'il change avec l'état autant qu'avec la
  langue. `setLang` le reprend.
- Le réglage système n'est pas contourné mais **arbitré** : rien ne démarre seul chez qui
  a demandé moins d'animations, et le premier « lire » retient le choix dans
  `jigger-anim` puis lance tout ce qui est visible — une fois pour le site, pas une fois
  par démonstration et par page. Vérifié en navigateur : un clic, trois vidéos lancées,
  préférence persistée.
- **Nouvelle page `parcours.html`**, quatrième du site : onze étapes dans l'ordre. 1 à 3
  l'installation en condensé (renvoi à `utiliser.html` pour le détail, plutôt qu'un
  doublon) ; 4 à 7 la voie « vos propres commandes, interceptées » ; 8 et 9 la voie
  « une seule syntaxe, `jg` » ; 10 SSH ; 11 l'appel direct du binaire.
- **L'étape 11 comble un manque réel** : `jigger complete`, `render`, `pick`, `prompt`,
  `warm` et `--select` n'étaient documentés nulle part sur le site, alors que le greffon
  n'est qu'un client et que ces commandes sont le seul recours sans shell interactif.
- En-tête des quatre pages étendu (contrôle 4 : il doit rester identique partout),
  `PAGES` du vérificateur étendu, `deploy-proxmox.sh` complété — sans quoi la page
  n'aurait jamais atteint la ligne.
- 74 clés françaises écrites. `verifier.sh --reseau` : **109 contrôles au vert**.

**Fichiers :**
- `website/app.js` — contrôle de lecture, préférence `jigger-anim`, 74 clés `tour.*`
- `website/styles.css` — style du bouton, `prefers-reduced-motion` respecté jusque dans
  ses propres transitions
- `website/parcours.html` — la page, 11 étapes, 10 blocs par système
- `website/{index,utiliser,ssh}.html` — le lien de navigation
- `website/verifier.sh`, `website/deploy-proxmox.sh`, `website/README.md`

**Décisions / notes :**
- **La demande « mettre des GIF » reposait sur une prémisse fausse**, et le dire valait
  mieux que l'exécuter : le format n'était pas en cause. Le vrai défaut — tout le contenu
  illustré invisible, en silence, pour une catégorie de visiteurs — était plus grave que
  ce qui était signalé, et le GIF l'aurait masqué sans le corriger.
- **Incohérence rattrapée à la relecture** : l'anglais annonçait « Ten steps » quand la
  page en comptait onze ; le français disait « Onze ». Corrigé dans les quatre
  occurrences (description, og, twitter, chapô).
- Six affirmations « trois pages » devenues fausses corrigées dans `README.md`,
  `verifier.sh` et `app.js`, plus le compte de médias (dix-huit → vingt).
- **Omarchy avait été illustré entre-temps.** La page a été écrite avec dix blocs
  omarchy annonçant « pas encore d'enregistrement » ; `main` avait entre-temps reçu les
  quatre captures (b7dd4e6). Quatre de ces blocs étaient donc **faux** au moment de la
  fusion — corrigés : les étapes 4, 6, 8 et 10 montrent l'enregistrement.
- **Les quatre blocs qui restent sans capture** — installer et mettre à jour, sur macOS
  et sur Arch — ne disent plus « pas encore » mais pourquoi ils n'existeront pas : les
  filmer demande d'installer pour de vrai, ce que seule la machine virtuelle Windows
  fait. C'était l'intention annoncée par le commit des captures Omarchy, appliquée aux
  deux blocs Arch et oubliée sur les deux blocs macOS d'`utiliser.html` — alignés.

## 08:42:41 — Demande

> commit et push


## 08:45 — Réalisé : fusion, publication

**Demande :** « commit et push » — le travail de la session était déjà commité et poussé ;
restait le journal.

**Actions :**
- Fusion de `main` (13 commits arrivés pendant la construction : captures Omarchy,
  empreinte des actifs au déploiement, deux correctifs de défilement, `color-scheme`).
  Deux conflits, tranchés en faveur du travail arrivé : `deploy-proxmox.sh` garde son
  mécanisme d'empreinte — `parcours.html` s'inscrit dans la boucle **et** dans l'archive,
  sans quoi elle serait partie sans style ou pas du tout — et le `README` garde son
  décompte de médias.
- `verifier.sh --reseau` : **126 contrôles au vert**. `go build ./...` au vert.
- Publié : la page, les vidéos Omarchy et le bouton de lecture sont en ligne, empreintes
  posées (`styles.css?v=1368b970`).

**Décisions / notes :**
- **Une ligne de l'entrée de 08:35 était devenue fausse** (« Omarchy reste sans
  capture ») : écrite avant la fusion, démentie par elle. Corrigée ici plutôt que laissée
  — un journal qui garde une affirmation fausse vaut moins qu'un journal plus court.
## 11:40:28 — Demande

> L'étape suivante est de faire connaître jigger

## 11:49:28 — Demande

> continue


## Réalisé : le module PowerShell ne venait de nulle part

**Demande :** poursuivre après la refonte du site.

**Actions :** comblé le seul défaut que le chantier du site avait révélé sans réparer.
Le guide, le manuel d'installation et les deux READMEs demandaient tous
`Import-Module C:\path\to\jigger\shell\jigger.psm1` sans dire d'où venait le fichier —
un lecteur Windows qui suivait le guide n'avait pas le module. Le clonage est ajouté aux
quatre documents, dans les deux langues, et le chemin fictif disparaît partout.

**Fichiers :** `docs/getting-started.md`, `docs/fr/getting-started.md`, `README.md`,
`README.fr.md`. Commit `c1a8b5c`, issue #123.

**Note d'incident.** Ce commit est parti **sur `main`** alors que je le croyais sur
`feat/site-refonte`. La copie de travail avait changé de branche entre-temps — une autre
session y a travaillé, la MR !43 a été fusionnée et le site publié. Le résultat est le
bon endroit (la branche du site était déjà fusionnée), mais la décision ne venait pas de
moi. C'est exactement le coût annoncé de la décision R5 du chantier précédent : travailler
dans la copie principale plutôt que dans un worktree dédié. À refaire, worktree.
## 12:12:51 — Demande

> laisse l'autre session s'en charger

