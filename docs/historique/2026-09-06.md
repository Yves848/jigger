# Historique — 2026-09-06

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 08:20:28 — Demande

> Réflexion faite, un plugin `git` nèst pas une bonne idée.  On va concentrer jigger sur sa tâche initiale.  Aider au package management

## 08:33:59 — Demande

> on retire tout ce qui touche à la tentative de plugin `git`

# 2026-09-06

## 00:55 — Réalisé : le plugin git et son mécanisme retirés (ADR-0010)

**Demandes :** « un plugin `git` n'est pas une bonne idée, on concentre jigger sur sa tâche
initiale, aider au package management » ; puis « on retire tout ce qui touche à la tentative
de plugin `git` ».

**Actions :**
- Descripteur livré et copie locale retirés, caches réchauffés : `git` n'est plus surveillé.
- `internal/pm`, `internal/plugin` et `internal/complete` ramenés à leur état d'avant
  l'extension de l'ADR-0009 ; `internal/pm/viviers.go` supprimé.
- Documentation réécrite dans les deux langues autour du **périmètre**, en gardant l'histoire
  des deux retraits.
- **ADR-0010** écrite, **ADR-0009** basculée en « remplacée » avec son texte intact.
- Consigné en #164.

**Fichiers :**
- `packaging/plugins/git/` — supprimé
- `internal/pm`, `internal/plugin`, `internal/complete` — avant extension
- `internal/facade/executer.go`, `internal/plugin/discovery_test.go` — exemples génériques
- `docs/plugins.md`, `docs/fr/plugins.md`, `docs/adr/0009-…`, `docs/adr/0010-…`

**Décisions / notes :**
- **Le retrait n'a aucune cause technique**, et c'est ce qui rendait l'ADR nécessaire : le
  helper marchait. Ce sont trois constats d'usage qui l'emportent — le shell complète déjà
  git ; un helper sans binaire est borné par ce que la commande sait produire (deux verbes
  sur dix-sept sans contexte) ; et le coût, un sous-processus tiers par frappe, ne diminuait
  pas.
- **Le périmètre n'était écrit nulle part** — c'est ce qui a permis d'écrire ce plugin deux
  fois sans que rien ne s'y oppose. L'ADR-0010 le pose.
- **Ce qui est gardé délibérément** : la troncature du rendu (elle protège tous les
  gestionnaires et a ses tests), la découverte, l'exécution native, le silence sur un verbe
  inconnu, l'armement du mot. Rien de tout cela n'était propre au helper.
- **L'ADR-0009 garde son texte.** Un ADR accepté ne se réécrit pas : le registre vaut par ce
  qu'il conserve des raisonnements qui se sont révélés faux.
- J'avais défendu la position inverse deux heures plus tôt ; l'ADR le dit plutôt que de
  présenter la conclusion comme allant de soi.

**Commits :** `e6e4969` retrait du plugin · `635b0fb` retrait du mécanisme · `885f3ee` ADR-0010

## 08:43:51 — Demande

> on la sort et on la publie.  Tout de suite.  Pas besoin de question supplémentaire

## 08:50:43 — Demande

> le résultat de la pipeline

## 08:55 — Réalisé : v0.21.0 publiée, et le miroir enfin dompté

**Demande :** « on la sort et on la publie, tout de suite ».

**Actions :**
- **v0.21.0 publiée** : bump, section `CHANGELOG`, contrôles, tag. Site redéployé et vérifié
  en `0.21.0` dans la foulée.
- **Pipeline 65 verte du premier coup**, `github:` compris — et dans le **pire cas**.

**Décisions / notes :**
- **Le cas était celui qui faisait échouer les précédentes.** Tag poussé 93 secondes après un
  passage du miroir, donc en pleine fenêtre de blocage : exactement ce qui a coulé la v0.18.0
  et la v0.20.0.
- **Le mécanisme a travaillé, et la trace le prouve** : absence du tag constatée, dernier
  démarrage du miroir lu, 75 secondes d'insistance, miroir démarré à `06:51:16` — soit
  **5 min 13 après le passage précédent**, confirmant la fenêtre de cinq minutes une
  quatrième fois — puis publication.
- **Quatrième tentative, première éprouvée dans le cas qu'elle vise.** Les trois précédentes
  avaient échoué pour un motif unique : prendre la réponse d'une API pour la preuve de
  l'effet cherché. Celle-ci observe l'effet.
- Numéro **mineur** et non correctif : le lot retire une capacité livrée.
- Vérifié après coup : 5 archives sur chaque forge, 40 lignes de notes, miroir en phase.

**Commits :** `1c47eaa` Version 0.21.0 · tag `v0.21.0`

## 08:54:01 — Demande

> publier sur scoop et brew.  Puis, nettoyer les version locale et installer via brew

## 09:01:54 — Demande

> ok, ça marche

## 09:30 — Réalisé : taps publiés, poste basculé sur la version livrée

**Demande :** publier sur scoop et brew, puis nettoyer les versions locales et installer via
brew.

**Actions :**
- **Homebrew** (`yves/cocktails`) : formule 0.15.0 → 0.21.0, condensat calculé sur l'archive
  téléchargée. Vérifié que les trois chemins installés existent dans l'archive publiée et que
  les deux assertions du bloc `test` passent sur le binaire 0.21.0.
- **Scoop** (`scoop-jigger`) : manifeste régénéré par `bin/maj-manifeste.sh`, qui lit le
  condensat dans le `SHA256SUMS` publié, valide le JSON et vérifie l'accès sans
  authentification.
- **Poste** : `brew upgrade jigger` → 0.21.0 ; `~/.local/bin/jigger` retiré ; `~/.zshrc`
  basculé sur le greffon livré, sans `JIGGER_BIN` ni chemin vers le dépôt.

**Fichiers :**
- `~/git/homebrew-cocktails/Formula/jigger.rb`, `~/git/scoop-jigger/bucket/jigger.json`
- `~/git/config/config/zsh/.zshrc`

**Décisions / notes :**
- **Deux incidents de clones périmés, attrapés à temps.** Les taps dataient de la 0.11.0
  quand les distants étaient en 0.15.0 : régénérer le manifeste scoop depuis un générateur
  périmé aurait **annulé en silence** le commit « Le bucket nomme pacman ». C'est la poussée
  refusée qui l'a révélé — j'avais édité sans fetcher.
- Leçon appliquée dans la foulée : le dépôt `config` a été **fetché avant** d'être édité, et
  il était 12 commits en retard sur le fichier utilisé tous les jours.
- **Renversement assumé du réglage zsh** : le poste utilise désormais ce que reçoit un
  utilisateur, au prix qu'un chantier sur jigger ne s'y voie plus immédiatement. La ligne pour
  repasser au greffon en cours d'écriture est laissée en commentaire.

## 09:54:40 — Demande

> mets un rappel pour l'expiration du jeton

## 10:00:52 — Demande

> commit et push

## 09:55 — Réalisé : rappel posé pour l'expiration du jeton

**Demande :** mettre un rappel pour l'expiration du jeton.

**Actions :**
- Rappel macOS créé, échéance **1ᵉʳ décembre 2026 à 9 h** — un mois d'avance, le
  renouvellement demandant de créer un jeton, de remplacer la variable CI et de vérifier.

**Décisions / notes :**
- Le corps du rappel est **autosuffisant** : ce qui casse (le job `github:` ne réveillera
  plus le miroir, les releases échoueront environ une fois sur deux, **sans message explicite
  sur la cause**), comment renouveler, et où lire le détail. Dans un an personne ne se
  souviendra que ce jeton existe ; le symptôme sera un `422` de GitHub, celui-là même qui a
  fait tourner en rond quatre fois le 5 septembre.
- **Le rappel vit hors du dépôt**, donc hors de portée de quiconque reprendrait le projet.
  Une vérification par la CI — le job `miroir` tourne déjà chaque jour et sait ouvrir une
  issue — serait plus solide. Proposée, pas faite.

## 10:15:48 — Demande

> ajoute la vérification dans le job miroir

## 10:20 — Réalisé : le garde-fou surveille l'échéance des jetons

**Demande :** ajouter la vérification dans le job miroir.

**Actions :**
- `JetonsAlarmants` et sa surveillance branchées dans `tools/miroir`, avec une issue
  `garde-fou::jeton` propre. Six cas de test posés avant le code.
- Fonctions d'issue généralisées pour porter deux garde-fous sans dupliquer la logique.
- Consigné en #166.

**Décisions / notes :**
- **La vérification a trouvé quelque chose au premier passage** : `garde-fou-miroir` expire
  le **16 septembre, dans neuf jours**, sans que rien ne le signale. Elle aurait coupé le
  garde-fou lui-même.
- **`GARDE_FOU_TOKEN` est refusé sur `/access_tokens`** (401, mesuré). La lecture passe donc
  par `GITLAB_API_TOKEN`, et **le jeton surveille sa propre échéance** — le jour où il meurt,
  l'échec de lecture est le signal. Chemin éprouvé.
- **Le code de sortie ne dépend pas des alertes** : une échéance à trois semaines ferait
  rougir la planification chaque jour, ce qui apprend à ne plus la regarder. L'issue est le
  canal.
- Le job CI n'a pas eu à changer : les variables du projet sont accessibles à tous les jobs.

**Commits :** `8c54cb4` Le garde-fou surveille aussi l'échéance des jetons

## 11:59:32 — Demande

> vérifier les issues ouvertes

## 12:02:06 — Demande

> avant de commencer : est-ce qu'une VM parallels avec arch ARM aiderait pour développer et tester le module pacman ?

## 12:04:47 — Demande

> est-ce qu'une VM arch X86 serait plus utile ? PAr exemple, dans un lxc sur le serveur proxmox ?

## 12:07:36 — Demande

> pour les captures d'écran, il n'est pas envisageable d'avoir un accès SSH vers le LXC et de faire les capture ici ,

## 12:27:19 — Demande

> monte le LXC Arch

## 12:54:17 — Demande

> corrige docs/captures.md

## 11:00 — Réalisé : le LXC Arch monté, et la doc des captures corrigée

**Demandes :** une VM Arch aiderait-elle ? plutôt un LXC x86 sur Proxmox ? monte-le ; corrige
`docs/captures.md`.

**Actions :**
- **LXC 118 `arch-jigger`** créé sur pve : Arch x86_64, `192.168.50.18`, 2 cœurs, 2 Go,
  20 Go sur `nvme`, non privilégié, `nesting=1`, démarrage auto. Outillé en go, yay, vhs,
  chromium, tmux, ffmpeg et MesloLGL Nerd Font ; utilisateur `builder` avec un sudo **qui
  demande un mot de passe**, condition pour reproduire #168.
- **Quatre usages éprouvés** : `make test-all` passe, `makepkg` construit yay depuis l'AUR
  (fakeroot compris), VHS enregistre sans affichage, et l'accès ssh depuis le Mac fonctionne.
- `docs/captures.md` corrigé. Consigné en #170.

**Décisions / notes :**
- **Le choix x86 est tranché par un fait, pas par une préférence** : les deux `PKGBUILD` de
  `packaging/aur` sont `arch=('x86_64')` et `jigger-bin` télécharge
  `jigger_*_linux_amd64.tar.gz`. Or la CI ne produit **aucune** archive linux/arm64 : une VM
  ARM testerait une configuration qui n'est pas livrée.
- **Quatre frictions réglées** : Landlock absent du noyau vu par un conteneur non privilégié
  (`DisableSandbox` dans `pacman.conf`), trousseau du gabarit non initialisé
  (`pacman-key --init`), Chromium de go-rod sans `libnss3` (`ROD_BROWSER_BIN`), et une
  adresse que j'avais crue libre.
- **Mon erreur d'adressage** : `.17` déduite libre des seules configurations de conteneurs,
  en oubliant le DHCP. L'`arp` montrait une MAC étrangère à Proxmox. Déplacé en `.18`, et
  **vérifié par la MAC** que c'est bien le conteneur qui répond — la vérification qui manquait.
- **Mon erreur d'analyse** : j'ai annoncé que la façade perdait ses verbes d'écriture sur Arch,
  « bien plus gros qu'un test capricieux ». C'était faux. `internal/pacman/verbs.go` n'expose
  délibérément que la lecture quand yay est absent — c'est l'ADR-0007, « pacman lit, yay
  pilote ». Il manquait yay au conteneur, pas un correctif à jigger.
- Les deux risques annoncés — `makepkg` et Chromium en conteneur non privilégié — sont levés
  **par la mesure**.

**Commits :** `825bc87` correction de `docs/captures.md`

## 12:58:31 — Demande

> attaquer #169


## 14:20 — Réalisé : #169, scoop mesuré plutôt que supposé

**Demande :** traiter #169 — recensement du défaut de droits sur les quatre gestionnaires,
et suite pour scoop.

**Actions :**
- Recensement de l'issue vérifié contre le code : seul `internal/winget/droits.go`
  implémente `pm.Elevateur`, `internal/scoop/` n'avait pas de `droits.go`, et
  `internal/elevate/elevate_other.go` écrit déjà pourquoi le chemin Unix devra anticiper
  plutôt que constater.
- Mesure sur la VM Windows 11 (102, `192.168.50.100`) via l'agent QEMU. L'agent tourne en
  `NT AUTHORITY\SYSTEM` **élevé** : inutilisable tel quel pour mesurer un défaut de droits.
  Contourné par une tâche planifiée en `RunLevel Limited`, identité confirmée par `whoami`
  dans la tâche elle-même.
- Quatre mesures : `scoop install -g` → 0, `scoop install` → 0, sous-commande inconnue → 1,
  et un témoin `cmd /c exit 3` → 3.
- Cause remontée jusqu'au lanceur `bin/scoop.ps1`, puis **prédiction posée avant
  vérification** (la branche `default` doit, elle, rendre 1) — confirmée.
- VM nettoyée : trois tâches planifiées désenregistrées, fichiers retirés, aucune racine
  `C:\ProgramData\scoop` créée.

**Fichiers :**
- `internal/scoop/droits.go` — créé, et ne contient que la mesure et ce qu'elle interdit
  d'écrire. Placé là où la question se reposera.

**Décisions / notes :**
- **Pas de `pm.Elevateur` pour scoop**, à l'inverse de ce que l'issue pariait. Rien à
  constater (tous les échecs rendent 0), pas de contre-cas (rien ne refuse l'élévation),
  et la façade n'émet de toute façon jamais `-g`.
- Le témoin `exit 3` était la pièce nécessaire : sans lui, un `0` ne distingue pas le
  comportement de scoop d'un dispositif de mesure faux.
- **Retombée hors périmètre**, ouverte en #171 : le code observé étant toujours 0, la
  façade prend **tout** échec de scoop pour une réussite sous Windows. L'utilisateur voit
  le message — le verbe est relayé — mais `jg` rend 0.
- Quatre commandes refusées par le classifieur en cours de route (base64 encodé, scan de
  port, `scp` puis pipe vers la VM). Contourné en restant en clair et en réduisant la
  portée, jamais en cherchant à passer outre.

**Commits :** `4402702` scoop ne peut pas constater ses droits : la mesure tranche
## 14:37:27 — Demande

> faire #167


## 15:40 — Réalisé : #167, le popup élève les opérations pacman

**Demande :** traiter #167 — une opération pacman complétée par le popup part sans
élévation et pacman la refuse.

**Actions :**
- Conception (a) de l'issue retenue : préfixer `sudo `, jamais demander le mot de passe.
- `_jigger_pacman_root` — prédicat **distinct** de `_jigger_pacman_mutant`, parce que les
  deux tables divergent sur `-Sc`, `-Sw` et `-Fy`.
- `_jigger_besoin_sudo` — analyse de ligne sans rien évaluer (`${(z)…}`), avec trois refus :
  `yay`/`paru`, ligne déjà élevée, et `pacman` qui n'est pas la première commande.
- `_jigger_elever` câblé sur les deux chemins qui **lancent** : `_jigger_accept` (⏎) et le
  code 10 de `_jigger_widget`.
- Réglage `JIGGER_SUDO` déclaré par pacman dans son `init`, comme `pacman_ttl` et `aur_ttl`.
- Les vingt opérations de la table **mesurées sur le conteneur Arch en utilisateur non
  root** : concordance totale avec le prédicat.
- 40 assertions ajoutées à la suite zsh ; `go test ./...` et `make test-shell` passent.

**Fichiers :**
- `shell/jigger.plugin.zsh` — les trois fonctions, le défaut `JIGGER_SUDO`, les deux câblages
- `internal/pacman/pacman.go` — déclaration du réglage
- `internal/i18n/catalogue.go` — libellé `cfg.sudo` en deux langues
- `tests/zpty.zsh` — le bloc d'assertions
- `docs/getting-started.md`, `docs/fr/getting-started.md` — la ligne du tableau des réglages

**Décisions / notes :**
- **⏎ et non ⇥**, la question laissée ouverte par l'issue. L'argument décisif n'y était pas :
  ⏎ n'appelle `_jigger_insert` que s'il reste un candidat, donc un `pacman -Syu` frappé en
  entier aurait échappé à un préfixe posé à l'insertion.
- `-Sup` est le cas qui aurait pu surprendre — un `u` mais rien que des URI imprimées. Le
  test du `p` le range à gauche, et la mesure sur Arch le confirme.
- **Limite assumée :** les options longues (`--sync`) ne sont pas lues ; la ligne part alors
  sans préfixe, comme avant.
- **Reste à trancher :** faut-il un ADR ? La décision « le chemin popup anticipe par préfixe »
  complète l'ADR-0004 (« élévation constatée ») et diverge de ce que `elevate_other.go`
  prévoyait (`sudo -v` avant de lancer). Non écrit sans accord.

**Commits :** `6770392` le popup pose sudo sur une opération pacman qui exige root
