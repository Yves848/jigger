# Historique — 2026-09-17

> Journal des demandes de la journée. Les entrées « Demande » sont consignées
> automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill
> `historiser`.

## 19:40 — Demande

> resume

## 20:0x — Réalisé : la MR !69 fusionnée, et le guide du dépôt remis à jour

**Demande :** reprendre le travail interrompu. Il vivait ici, pas dans `cocktails` :
la branche `docs/architecture-execution` était poussée, sa MR ouverte depuis le
9 septembre, et le dernier commit s'appelait `wip` — il ne portait que la demande
captée `> oui, retire ai-migration-kit`, restée à moitié faite.

**Actions :**
- Suite complète passée avant la fusion (`make test-all` : « tout passe »), puis
  MR !69 fusionnée sur `main` (`14b3498`). Les issues #199 et #200 étaient déjà
  fermées par la fusion.
- Fin du « retire ai-migration-kit » : entrée de marketplace et plugin désactivé
  retirés de `~/.claude/settings.json` (hors dépôt), et les trois renvois du guide
  du dépôt corrigés — les skills de cycle de vie viennent maintenant du kit
  `tagout`, et `gitlab-changelog` a été remplacé par `forge-cycle` le 14 septembre.
- Déclaration de la porte des tests de `forge-cycle` : sans `.claude/verifier.toml`,
  le verbe `fusionner` fusionne sans qu'aucune suite verte n'ait été constatée. La
  fusion du jour a dû être vérifiée à la main faute de ce fichier.
- Double installation de jigger (point laissé ouvert le 14 août) : **plus rien à
  trancher**. `~/.local/bin/jigger` n'existe plus, et `~/.local/bin` est passé
  *après* `/opt/homebrew/bin` dans le `PATH` — la copie de dev ne peut plus masquer
  le binaire Homebrew (0.22.1) en silence, même si un `make install` la recrée.

**Fichiers :**
- `.claude/forge.md` — `ai-migration-kit` → `tagout`, `gitlab-changelog` → `forge-cycle`
- `.claude/verifier.toml` — nouveau : `make test-all`, motif de succès « tout passe »

**Commits :** `14b3498` (fusion MR !69), `7732783`
