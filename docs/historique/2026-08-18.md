# Historique — 2026-08-18

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 08:35:14 — Demande

> remets github partout

## 21:41:48 — Demande

> construire et publier

## 21:45:24 — Demande

> c'est bizarre, la version actuelle ne prend pas en compte les modifications concernant le 'enter' qui doit complèter la ligne et exécuter directement l'instruction

## 21:50:57 — Demande

> c'est bon dans un terminal neuf


## 21:51 — Réalisé : binaire recompilé, et le ⏎ qui manquait était un shell trop vieux

**Demande :** compiler et installer le binaire, puis comprendre pourquoi le ⏎ de A-20 —
compléter la ligne *et* l'exécuter en une frappe — restait sans effet.

**Actions :**
- `./build.zsh --deploy` : `jigger 0.12.0` compilé et installé dans `~/.local/bin/jigger`,
  greffon zsh déjà présent dans le `~/.zshrc`.
- Enquête sur le ⏎ absent, sans rien modifier : `ba45bc7` (A-20) est bien sur `main` et
  reste le dernier commit à toucher `shell/jigger.plugin.zsh` ; `./tests/zpty.zsh --suite`
  passe en entier, y compris « ⏎ complète la dernière partie, et exécute dans la même
  frappe » ; dans un zsh interactif neuf, `bindkey "^M"` rend bien `_jigger_accept`.
- Relevé des shells interactifs vivants : deux sur trois (démarrés les 12 et 15 août)
  ont en mémoire le greffon d'avant A-20 (17 août 16:22).
- Vérifié qu'un re-source à chaud est sans danger : `_jigger_bound_widget` refuse de
  relever un widget `_jigger_*`, donc pas de boucle sur soi-même — le repli retombe sur
  `.accept-line`.

**Fichiers :** aucun fichier du dépôt modifié — le diagnostic n'a rien changé au code.

**Décisions / notes :** cause racine, confirmée par l'utilisateur dans un terminal neuf :
le greffon n'est sourcé qu'au démarrage du shell, donc `^M` était resté l'`accept-line` de
zsh dans les sessions ouvertes avant A-20. La recompilation a rendu le symptôme plus
trompeur qu'il ne l'était : le binaire, lui, est appelé à chaque frappe, si bien que ces
vieux shells affichent depuis le pied à quatre pastilles — **↩ exécuter** comprise — pour
une touche que jigger n'y tient pas. Le cadre annonce ce que le clavier ignore. À garder
en tête pour la prochaine version publiée : c'est le genre d'écart que verra aussi
quelqu'un qui met à jour jigger sans rouvrir son terminal.
