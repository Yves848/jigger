# Historique — 2026-08-30

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 12:35 — Réalisé : A-25 vérifiée de bout en bout, close

**Demande :** tâche 5 du plan du sélecteur SSH — prouver que l'ensemble (parseur, deux
tâches précédentes, enregistrement, greffons) fonctionne assemblé, puis consigner. Exécutée
comme tâche déléguée, hors de la session interactive habituelle : ce journal ne porte donc
pas d'entrées « Demande » horodatées par le hook, seulement ce qui a été constaté.

**Actions :**

- **Preuve 1 — le rendu, sans shell.** `jigger render --line "ssh arch" --cols 80` dessine
  le cadre attendu, avec `archlight` (le seul hôte du `~/.ssh/config` réel de la machine à
  commencer par « arch ») et son adresse à droite :

  ```
  count=1  sel=0  exec=0  left=ssh archlight
  ╭──────────────────────────────────────────────────────────╮
  │❯ ssh                                        jigger 0.12.0│
  │  •  archlight                            192.168.50.207  │
  │                                                          │
  │   ⇥  insert   ↩  execute   ↓  browse   ^G  close         │
  ╰──────────────────────────────────────────────────────────╯
  ```

- **Preuve 2 — la complétion sur les trois commandes.** `jigger complete "ssh "`,
  `"scp "` et `"sftp "` rendent exactement les mêmes 21 hôtes (issus de
  `~/.ssh/config.d/homelab.conf`, lu par `Include`), preuve que les trois fournisseurs
  partagent le même catalogue.
- **Preuve 3 — non-régression sur brew.** `jigger complete "brew ins"` rend toujours
  `install` (sous-commande) ; `jigger complete "brew install fire"` rend toujours les
  formules (`firefox`, `firebase-cli`…). La règle générale ajoutée à
  `internal/complete` (un fournisseur sans sous-commande propose son catalogue au premier
  mot) n'a rien changé pour un fournisseur qui en déclare.
- **`make test`** (et `go test ./... -count=1` pour écarter le cache) : tous les paquets
  passent, aucun test existant modifié (`git status` propre après coup).
- **A-25** déplacée d'« À faire » vers « Fait » dans `docs/ameliorations.md`, avec sa
  date, ses documents et le résumé des deux décisions ci-dessous.

**Un écart trouvé en vérifiant, entre le plan et le binaire réel — pas un défaut
d'intégration :** le brief de la tâche 5 et `docs/plans/2026-08-30-selecteur-ssh.md`
appellent la complétion avec `jigger complete --line "..."`. Ce drapeau n'existe que pour
`render` ; `complete` prend sa ligne en argument **positionnel** (`arg(2)` dans `main.go`),
comme le montrent `README.md`, `docs/getting-started.md` et les journaux précédents
(`complete "jg "`, `complete "brew install fire"`). Tapée telle quelle, la commande du
plan ne produit **aucune sortie**, pour aucune des cinq lignes testées — vérifié
littéralement avant de creuser. Avec la syntaxe établie du dépôt, les trois preuves
rendent exactement ce qui était attendu. Le plan sera à corriger s'il ressert ; le code,
lui, n'a rien à réparer.

**Fichiers modifiés par cette tâche :** `docs/ameliorations.md`, `docs/historique/2026-08-30.md`.
Aucun fichier de code touché — la vérification n'écrit rien dans le dépôt au-delà de la
consignation.

**Décisions / notes — les deux qui coûtent, portées par les tâches précédentes et
reprises ici parce que la vérification de bout en bout est l'endroit où leurs
conséquences se voient :**

- **`ssh`, `scp` et `sftp` entrent dans le défaut de `JIGGER_COMMANDS`, pas dans la liste
  toujours-armée** (`shell/jigger.psm1`, tâche 4). Conséquence assumée : qui a épinglé
  `winget,scoop` dans son profil ne verra pas le sélecteur tant qu'il n'aura pas allongé
  son réglage. Le précédent A-1 avait tranché l'inverse pour la façade, parce que `jigger`
  et `jg` sont les commandes *de jigger* ; `ssh` est une commande **tierce**, et
  `JIGGER_COMMANDS` existe pour que l'utilisateur choisisse ce qui l'intercepte.
- **La mémoïsation du catalogue, prévue par le plan, a été retirée** (`internal/ssh/manager.go`,
  commit `a0811a3`) : le greffon relance `jigger render` à chaque frappe, donc une variable
  de paquet ne survit jamais — elle protégeait un scénario qui ne se produit pas.

**Ce qui reste ouvert :**

- **L'essai en vrai shell** (`exec zsh`, taper `ssh arch` sans valider, constater la popup
  qui suit la frappe, ⇥ qui insère, ⎋ qui ferme) — la seule étape du plan qui exige une
  frappe humaine, donc la seule qui n'a pas été faite ici. C'est la seule preuve qui
  montre le greffon fonctionner pour de vrai ; les trois preuves ci-dessus montrent le
  binaire correct, pas le greffon monté.
- **Le commit de cette tâche n'a pas été poussé, et aucune merge request n'a été
  ouverte** — décision du contrôleur de cette tâche déléguée, `jigger` étant un projet
  publié avec un miroir GitHub. Poussée et MR restent à faire par Yves, comme le demande
  le `CONTRIBUTING.md`.
- Le plan (`docs/plans/2026-08-30-selecteur-ssh.md`) garde sa syntaxe `complete --line`
  incorrecte pour les étapes 2 et 3 de la tâche 5 : à corriger si le document ressert.
