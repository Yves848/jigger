# Historique — 2026-08-17

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 12:35:06 — Demande

> commit et push


## 12:35 — Réalisé : A-19 et A-11 closes, et déploiement local

Séance à cheval sur la nuit du 16 : le détail des demandes est dans
[2026-08-16.md](2026-08-16.md).

**Actions :**
- **A-19** : jeu de raccourcis arrêté — `⇥` coche, `^A` coche tout ce que le filtre laisse,
  `PgPréc`/`PgSuiv` paginent, `^R` bascule en regex. Porté par un principe explicite :
  *jigger ne prend une touche que tant qu'il a le clavier, et la rend sinon.*
- **A-11** : `^R` sur les trois surfaces, popup vivant compris, avec délégation à la
  recherche inverse de zsh hors ligne surveillée. Filtre à deux modes dans
  `internal/complete/filtre.go`, drapeau `render --regex`, état dans les deux greffons.
- **Déploiement local** : binaire installé, `~/.zshrc` remis au propre (doublon retiré,
  greffon du dépôt, `JIGGER_BIN` posé), sauvegarde faite avant modification.

**Fichiers :** `internal/complete/filtre.go`, `internal/ui/{liste,tableau,picker}.go`,
`main.go`, `shell/jigger.plugin.zsh`, `shell/jigger.psm1`, les deux guides,
`docs/ameliorations.md`

**Défauts trouvés en implémentant**, tous par une vérification et non par une relecture :

- `^B`/`^F` avaient été volés au champ de saisie par A-10 — le curseur ne s'y déplaçait plus.
- La **clé de cache du cadre ne portait pas le mode**, dans les deux greffons : une bascule
  ne redessinait rien, la ligne n'ayant pas changé. C'est le symptôme qu'Yves a signalé.
- La condition d'activation, calquée sur « il y a des candidats », empêchait de basculer
  quand la liste était vide — c'est-à-dire quand c'est le plus utile.
- **La version requise par les greffons était restée à 0.9.0** alors qu'ils passent
  désormais `--regex`, que ce binaire refuse : le popup se serait évanoui sans un mot.
  Relevée à 0.11.0, garde-fou vérifié à l'œuvre.
- **Le greffon zsh n'avait pas `JIGGER_BIN`**, que le module PowerShell a depuis toujours.
  Sans lui, un binaire fraîchement compilé ne peut jamais gagner : Homebrew place son `bin`
  avant `~/.local/bin`.

**Décisions / notes :** trois de mes modifications de fichiers ont **échoué en silence**
faute d'ancre exacte, et j'ai cru deux fois que le code ne marchait pas alors qu'il n'était
pas là. Vérifier chaque insertion, une par une, est ce qui a débloqué la séance.

Reste à éprouver sur le Dell XPS : la bascule `^R` sous PowerShell, jamais vue tourner
dans un vrai PSReadLine.

**Commits :** `df6d7bb`, `2bfdbde`, `e128581` sur `feat/raccourcis-selection` (MR !23)
## 12:37:26 — Demande

> il n'y a pas de MR en attente

## 12:40:39 — Demande

> oui

## 12:41:45 — Demande

> publie la v0.11.0


## 12:55 — Réalisé : publication de la v0.11.0

**Demande :** publier la v0.11.0.

**Actions :**
- Notes de version agrégées depuis les issues, en fusionnant ce qui relève d'un même
  chantier plutôt qu'en concaténant. Les quatre issues étiquetées `v0.10.1` — version
  jamais publiée — ont été **réétiquetées v0.11.0** avant l'agrégation, sans quoi leur
  travail aurait disparu des notes.
- CHANGELOG commité et poussé **avant** le tag : un tag est un instantané, il doit contenir
  ses propres notes.
- Tag `v0.11.0` poussé, pipeline vert sur les quatre étapes, release créée avec ses cinq
  liens. Téléchargement anonyme vérifié.
- Formule Homebrew et bucket scoop alignés ; pour scoop, le condensat annoncé a été
  recalculé sur le fichier réellement servi — identiques.

**Fichiers :** `CHANGELOG.md`, `~/git/homebrew-cocktails/Formula/jigger.rb`,
`~/git/scoop-jigger/bucket/jigger.json`

**Décisions / notes :** la v0.11.0 change le contrat entre greffon et binaire — les
greffons passent `render --regex`, que les binaires antérieurs refusent. Ils exigent donc
0.11.0 et se désactivent en le disant. Il faut mettre à jour **les deux ensemble** :
`brew upgrade jigger` le fait, un `git pull` du dépôt seul, non.
## 12:52:49 — Demande

> on va passer à la A-15 puis enchaîner avec la A-15

## 13:05:37 — Demande

> ok, ça tient

## 13:06:28 — Demande

> ok, écris l'ADR et la spec, puis implémente

## 18:53:48 — Demande

> vérifier la cohérence des branches.  Il est probable que des modifications effectuées via mon PC ne se trouvent pas sur ce mac

## 19:16:11 — Demande

> tu peux rebase la branche courante à partir de main ?

## 19:17:16 — Demande

> oui, force-push

## 19:19:16 — Demande

> crée la MR

## 19:20:32 — Demande

> fusionne la MR

## 19:21:07 — Demande

> récupère la branche facade-powershell

## 19:23:17 — Demande

> je veux que toutes les modifications apportées aujourd'hui soient sur main.  Pour le reste, on peut toujours repartir sur une branche "fraiche"

## 19:37:55 — Demande

> oui, il faut hitoriser


## 19:38 — Réalisé : cohérence des branches entre le PC et le Mac, tout ramené sur `main`

**Demande :** vérifier que rien de ce qui a été fait depuis le PC ne manque sur le Mac, puis
faire en sorte que tout le travail du jour soit sur `main`.

**Actions :**
- Inventaire complet après `fetch --prune` : positions locales/distantes, tags, stashes,
  worktrees. Deux écarts réels — `main` en retard de 10 commits (deux fusions faites depuis
  le PC), et `origin/feat/facade-powershell` **absente de ce Mac**, avec 3 commits postérieurs
  à sa fusion précédente (A-20, A-15, tableau des améliorations).
- Vérifié que les 16 branches locales `[gone]` ne portent **aucun fichier absent de `main`** :
  celles qui montraient des commits « hors main » avaient été fusionnées en squash, d'où des
  SHA différents pour un contenu identique.
- `main` avancé en fast-forward, `feat/configuration` rebasée dessus (3 commits, sans conflit),
  force-push après accord explicite.
- SHA réécrits par le rebase corrigés dans les issues #63-65 — en-tête **et** marqueur
  `<!-- gitlab-changelog sha=… -->` : sans ça, ces trois commits seraient ressortis « sans
  issue » à la préparation de la v0.12.0.
- `feat/facade-powershell` récupérée. Ses 3 commits n'avaient jamais été consignés (le PC
  n'a pas fait tourner le skill) : issues #66, #67, #68 créées à partir des messages de commit.
- Fusion d'essai de `main` dans la branche en worktree jetable avant de créer la MR :
  aucun conflit, `go build ./...`, `GOOS=windows go build ./...` et `go test ./...` passent.
- MR !26 (`a2dca8e`) et MR !27 (`32d496c`) fusionnées. `git log --branches --remotes --not main
  --since=2026-08-17` ne renvoie plus rien.

**Fichiers :** aucun fichier de code touché — travail d'intégration git et de consignation
GitLab. Seul ce journal change.

**Décisions / notes :**
- `feat/facade-powershell` **n'a pas été rebasée**, contrairement à `feat/configuration` :
  ses commits viennent du PC, et réécrire leur historique ici aurait imposé un
  `git reset --hard` là-bas. Fusion par commit de merge, donc.
- Les branches distantes fusionnées ne sont pas supprimées — cela demande un accord explicite.
- **Reste en attente :** 13 branches locales `[gone]` à supprimer (`/clean_gone`), et 3 montées
  en worktree (`feat/facade-phase-1`, `feat/i18n`, `fix/scoop-parsers`) dont deux portent des
  captures de prompts non commitées (`docs/historique/2026-08-15.md`, `2026-08-16.md`) absentes
  de `main` : à récupérer ou à jeter sciemment avant de retirer les worktrees.
## 19:41:27 — Demande

> nettoie les branches locales et supprime les distantes fusionnées

## 19:46:03 — Demande

> on peut publier la 0.12.0


## 19:55 — Réalisé : nettoyage des branches, puis publication de la v0.12.0

**Demande :** supprimer les branches locales et les distantes fusionnées, puis publier la
0.12.0.

**Actions :**
- Récupéré, avant de retirer les worktrees, les captures de prompts qu'ils gardaient non
  commitées : une du 15 août (`feat/facade-phase-1`), cinq du 16 (`feat/i18n`), aucune sur
  `main`. Commit `e9b2f60`, issue #70, MR !29.
- Supprimé 23 branches locales, 10 distantes (toutes vérifiées `--merged main`) et les 3
  worktrees. Il ne reste que `main`, des deux côtés.
- Croisé les commits depuis `v0.11.0` avec les issues consignées : **trois commits venus du
  PC n'en avaient aucune**. Consignés rétroactivement — l'alias `jg` (#71), les tests i18n
  (#72), les scripts de build (#73) — à partir de leurs messages de commit.
- Publié la v0.12.0 : CHANGELOG, `main.go` en 0.12.0, les deux captures du README qui
  affichaient encore 0.11.0. MR !30, tag `v0.12.0` sur `2c32bd1`, release GitLab.

**Fichiers :**
- `CHANGELOG.md` — section v0.12.0, onze issues agrégées, cinq internes écartées
- `main.go`, `README.md`, `README.fr.md` — la version affichée
- `docs/historique/2026-08-15.md`, `2026-08-16.md` — les captures récupérées

**Décisions / notes :**
- **Le plancher des greffons reste à 0.11.0.** `config --export` est appelé de façon
  tolérante des deux côtés (`2>/dev/null` en zsh, `try/catch` en PowerShell) : un binaire
  plus ancien laisse simplement les défauts s'appliquer. Rien n'oblige à le relever.
- La release a été créée à la main avant que la chaîne CI ne tourne ; le job `release` sait
  la compléter au lieu d'échouer (`release-cli update`), c'est prévu dans `.gitlab-ci.yml`.
- **Vérifié :** `make test-all` — go test sur les onze paquets puis la suite zsh dans un
  vrai pseudo-terminal — « tout passe ».

**Commits :** `1905296` Version 0.12.0, `e9b2f60` captures récupérées.
