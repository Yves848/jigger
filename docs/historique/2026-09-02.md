# Historique — 2026-09-02

> **Journée à cheval sur l'installation du hook.** `UserPromptSubmit` n'était pas installé
> sur cette machine (Arch) : il l'a été à 12:22, dans le dépôt `claude-skills`, et il s'est
> rechargé seul — le fichier de réglages était déjà sous surveillance, aucun redémarrage
> n'a été nécessaire.
>
> Conséquence sur la lecture de ce fichier : les demandes d'avant 12:25 sont **reformulées
> à la main** dans le « **Demande :** » de chaque entrée « Réalisé » ; celles d'après sont
> **capturées telles quelles** par le hook, en sections « Demande » horodatées. Le hook
> ajoutant toujours en fin de fichier, ces dernières se trouvent après les entrées
> « Réalisé » du matin : les horodatages font foi, pas l'ordre des sections.

## 07:44 — Réalisé : publication de la v0.13.0

**Demande :** vérifier si le dépôt est à jour, puis pousser le commit en attente et poser
le tag `v0.13.0`.

**Actions :**
- Constat de départ : arbre propre, `main` en avance d'**un** commit sur `origin/main`
  (`e741520`, le CHANGELOG de la v0.13.0), aucun tag sur `HEAD`.
- Croisement `v0.12.0..HEAD` : 32 commits, tous couverts — par leur `sha=` pour huit
  d'entre eux, par la branche `feat/selecteur-ssh` pour les issues #82/#83/#84, le reste
  étant des commits de fusion. Le commit du CHANGELOG ne se consigne pas, par
  construction.
- Push, puis tag annoté `v0.13.0` posé **sur le commit du CHANGELOG** et poussé — l'ordre
  compte : un tag est un instantané, et qui récupère `v0.13.0` doit y trouver le changelog
  de `v0.13.0`.
- Pipeline #50 suivi jusqu'au bout : `verifier`, `construire`, `publier`, `release`, tous
  au vert. La release a donc été créée **par la CI**, pas à la main.
- Miroir GitHub vérifié après coup : `v0.13.0` y est arrivée.

**Décisions / notes :**
- **`gl.py` était bloqué avant même de commencer**, sur
  `[SSL: CERTIFICATE_VERIFY_FAILED] … Missing Authority Key Identifier`. Ce n'était ni le
  jeton ni le serveur : depuis Python 3.13, `ssl.create_default_context()` active
  `VERIFY_X509_STRICT`, qui refuse un certificat d'autorité sans *Authority Key
  Identifier* — contrôle que `git` et `curl` ne font pas, d'où un dépôt qui pousse très
  bien pendant que l'API se dit injoignable. Contourné sur le moment par un
  `sitecustomize.py` de scratchpad, puis **corrigé à la source dans le dépôt
  `claude-skills`** (issue #1, MR !1, publiée en v0.1.0) : mode strict conservé par défaut,
  désarmé seulement après le code d'erreur 85, une fois par session, sur ce seul drapeau.
- La release n'a **pas** été créée par l'API : le job `release` du `.gitlab-ci.yml` la
  produit lui-même à partir du CHANGELOG, avec les cinq liens d'archives. La créer à la
  main aurait doublonné.

**Commits :** `e741520` Version 0.13.0 : le CHANGELOG rattrape le sélecteur SSH (poussé),
tag `v0.13.0`.

## 11:40 — Réalisé : état du miroir GitHub et du garde-fou

**Demande :** vérifier que le miroir GitHub est toujours à jour, puis lancer le garde-fou.

**Actions :**
- Comparaison GitLab / GitHub : tête de `main` identique (`e741520`), **18 tags de chaque
  côté**, aucun absent, aucun en trop, aucun pointant ailleurs. `v0.13.0` vise le même
  commit des deux côtés.
- Garde-fou `tools/miroir` **non exécuté** : Go n'est pas installé sur cette machine
  (`go.mod` demande 1.26.5 ; ni pacman ni mise ne le fournissent ici). Ce n'est
  vraisemblablement pas la machine de compilation.
- Vérification que le relais planifié existe réellement, l'issue #78 l'ayant laissé « à
  faire, hors dépôt » : planification `0 8 * * *` Europe/Paris **active**, variable
  `GARDE_FOU_TOKEN` masquée et non protégée, passages au vert les 30, 31 août, 1er et
  2 septembre. Aucune issue `garde-fou::miroir` n'a jamais été ouverte.

**Décisions / notes :**
- **`api.github.com` répond `API rate limit exceeded`** depuis cette IP sur les appels non
  authentifiés. La comparaison a donc été faite au `git ls-remote`, qui n'est pas plafonné
  et lit les références brutes. À retenir : le garde-fou, qui interroge l'API GitHub,
  rendrait vraisemblablement **2** dans ces conditions — « il n'a pas pu travailler », et
  non un feu vert. Le troisième code de sortie prend ici tout son sens.
- **Angle mort trouvé, un cran en amont d'A-24 :** les passages planifiés des **28 et
  29 août** ont échoué en `stuck_or_timeout_failure` — durée nulle, trace vide, aucun
  runner ne les a pris. Ni écart du miroir ni défaut du garde-fou, mais ces deux jours-là
  personne ne surveillait et **rien ne l'a dit**. Un job qui ne démarre pas ne peut pas
  rendre le code 2 que le garde-fou réserve à ce cas.

## 12:05 — Réalisé : notification par courriel sur pipeline en échec

**Demande :** être prévenu quand un pipeline planifié échoue, et quand il revient au vert.

**Actions :**
- Réglages de notification du projet passés de `global` à `custom`, avec
  `failed_pipeline` puis `fixed_pipeline` à `true`. Relus après écriture.
- Le réglage global du compte (`participating`) et les autres événements du projet sont
  inchangés.

**Décisions / notes :**
- **GitLab ne sait pas filtrer sur « pipeline *planifié* en échec ».** `failed_pipeline`
  couvre tous les pipelines du projet, push et tags compris. Accepté : le volume est
  négligeable sur ce dépôt, et un pipeline de push cassé mérite aussi un courriel.
- `fixed_pipeline` a été ajouté dans la foulée : sans lui, un courriel d'échec laisse la
  question ouverte et fait retourner voir l'interface.
- Réglage **personnel**, attaché au compte. Pour quelque chose de projet, avec
  destinataires explicites, il faudrait l'intégration *Pipeline status emails* — non
  configurée à ce jour.

## 12:25:45 — Demande

> fusionne la MR et supprime la branche

## 12:27:20 — Demande

> publie la v0.3.0

## 12:29:22 — Demande

> consigne cette session dans l'historique de claude-skills

## 12:30:58 — Demande

> pousse-le avec son issue

## 12:35:27 — Demande

> redémarre pour activer le hook

## 12:39:07 — Demande

> corrige l'avertissement et commite

## 12:40:05 — Demande

> pousse-le avec son issue

## 12:56:12 — Demande

> cette machine est sous Arch (Omarchy).  On va ajouter le module "PacMan / Yay" à jigger

## 13:51:48 — Demande

> commit ça

## 13:56:48 — Demande

> oui, lance /historiser

## 12:56 — Réalisé : le module pacman / yay

**Demande :** ajouter à jigger le module pacman / yay, cette machine étant sous Arch
(Omarchy).

**Actions :**
- Quatre arbitrages soumis avant d'écrire une ligne, tous tranchés sur la recommandation :
  périmètre complet (complétion + façade + prompt), yay seul pilote, l'AUR au catalogue de
  yay, et spec + ADR avant le code.
- Environnement mesuré avant de concevoir : `pacman -Sl` 166 ms / 15 604 lignes,
  `pacman -Qq` 10 ms, `readdir` de `/var/lib/pacman/local` sous la milliseconde,
  `checkupdates` 510 ms (réseau), `yay -Slq aur` 799 ms / 118 582 noms.
- Conception (`docs/specs/2026-09-02-pacman-design.md`) et ADR-0007 rédigés, puis le
  module : `internal/pacman` (7 fichiers, 4 de test), le câblage dans `managers`, `pm`,
  `ui`, `prompt`, `i18n` et `main.go`, le greffon zsh, deux segments de prompt et les
  quatre documents d'accueil.
- Go n'était pas installé sur la machine : `mise use -g go@latest` (1.27.0) après accord,
  ce qui a permis de compiler, tester et **éprouver le binaire en vrai** plutôt que de
  livrer du code non compilé.

**Fichiers :**
- `internal/pacman/` — le module : deux fournisseurs, catalogue à deux étages, verbes,
  analyseurs, état du prompt.
- `internal/complete/complete.go` — casse des sous-commandes, `subBrut` pour le titre.
- `internal/pm/pm.go` — `BadgeRepo`, `BadgeAUR`, `NewCatalogDe`.
- `internal/managers/`, `internal/ui/frame.go`, `internal/prompt/status.go`,
  `internal/i18n/catalogue.go`, `main.go` — câblage.
- `shell/jigger.plugin.zsh` — `JIGGER_COMMANDS`, détection des opérations mutantes,
  nommage des variables de prompt.
- `shell/oh-my-posh/pacman.segment.json`, `shell/starship/pacman.toml` — blocs de prompt.
- `docs/specs/2026-09-02-pacman-design.md`, `docs/adr/0007-pacman-lit-yay-pilote.md`,
  `docs/ameliorations.md` (A-16), les deux README, les deux getting-started, `CHANGELOG.md`.

**Décisions / notes :**
- **Les opérations de pacman sont des drapeaux**, et le moteur route sur
  `HasPrefix(word, "-")` : elles tombaient du mauvais côté du test. Réglé **sans toucher au
  moteur** — le fournisseur déclare la même liste dans `Subcommands()` et dans
  `Options("")`. Deux asymétries préexistantes de `complete` sont sorties au passage : la
  branche des sous-commandes ne minusculait pas (contrairement à celle des options), et le
  cadre annonçait `pacman -rns`, qui n'est pas une commande valide.
- **ADR-0007 — pacman lit, yay pilote.** Les deux mots de commande donnent sur une seule
  base alpm, situation inédite ici : `Router` lance tous les gestionnaires capables d'un
  verbe sans nom à résoudre, donc `jg list` aurait tout listé deux fois et `jg install fd`
  aurait ouvert le popup de désambiguïsation à chaque installation.
- **Le budget de la frappe a failli être crevé.** La première rédaction fusionnait le
  catalogue à chaque `render` : 45 ms, pour un budget déclaré de « quelques ms »
  (`complete_test.go:332`). `render` étant appelé en substitution de commande dans le
  widget zle, c'est de la latence de frappe directe. Le coût a été bissecté au banc
  (lecture 10 ms, allocation 1,8, boucle dépôts 2,0, boucle AUR 8,3, tri 3,0) avant de
  déplacer la fusion dans `warm`. Résultat : `yay` 70 → **28 ms**, `pacman` 13 → **7 ms**,
  `warm --all` 1 058 → **710 ms**.
- **Le bloc de prompt exportait `JIGGER_BREW_*` en dur** : sur Arch, les chiffres de pacman
  sortaient sous un nom faux. Les noms se décident maintenant au chargement du greffon.
- Vérifié sur données réelles, pas seulement en test : `yay -S 1password` insère
  `omarchy/1password` (121 noms sont portés par un dépôt *et* l'AUR, presque tous du dépôt
  `omarchy`) ; `jg outdated` répond « nothing to report » en code 0 malgré le code 1 que
  pacman rend quand il ne trouve rien ; la détection des commandes mutantes du greffon a
  été éprouvée sur dix-neuf lignes, `git commit -m 'pacman -Syu'` compris.
- **Reste ouvert :** les 28 ms de `yay` sont la lecture de 2,1 Mo et le remplissage d'une
  table de 134 000 entrées. Les faire tomber encore demanderait un badge par défaut sur
  `pm.Catalog` — un changement du type partagé par les quatre gestionnaires pour une
  dizaine de millisecondes. Écarté sciemment, et noté dans la conception.

**Commits :** sur la branche `feat/module-pacman`, poussée —
`ca4b016` pacman lit, yay pilote : l'ADR-0007 tranche avant le code ·
`0226ffc` Le module pacman / yay : premier gestionnaire Linux de jigger ·
`c1b4f24` Le greffon, les segments de prompt et les quatre documents d'accueil.
Issues #88, #89, #90 · MR !36 (`Closes #88, #89, #90`).

## 13:59:25 — Demande

> fusionne la MR

## 14:01:37 — Demande

> publie la v0.14.0

## 14:12:15 — Demande

> installer jigger sur ce laptop omarchy

## 14:18:44 — Demande

> corrige les deux

## 14:28:11 — Demande

> ok, enchaîne

## 14:06 — Réalisé : l'infographie du fonctionnement, et le site remis à niveau

**Demande :** réaliser une infographie qui montre clairement les flux entre les
gestionnaires de paquets et jigger — puis l'ajouter au site, et mettre à jour la copie
restée en arrière.

**Actions :**
- Lecture du dépôt avant de dessiner : les deux README, `internal/managers`,
  `internal/pacman/verbs.go`, la table des verbes, les modes d'accès de chaque
  gestionnaire. Le schéma ne dit rien qui ne soit vérifié dans le code ou les documents.
- **Artefact autonome** d'abord (dark, palette du site, IBM Plex) : le schéma en trois
  bandes, un second schéma sur le routage, la table des douze verbes vers quatre
  dialectes, et une section « ce que jigger ne fait pas ».
- **Intégration au site** ensuite, en section `#fonctionnement` entre « Une syntaxe » et
  « Ce qu'il garantit », plus une entrée de nav. Version allégée d'une ligne par bloc et
  par vignette : sur une page web, moins dense se lit mieux.
- **Mise à niveau de la copie** : métadonnées (`<title>`, `og:`, `twitter:`), hero,
  section façade, section prompt, et deux paragraphes neufs — le sélecteur SSH et la
  nuance pacman / yay.
- Contrôles : `./verifier.sh` au vert (89 clés, parité complète, ancres résolues), et
  rendu vérifié dans les deux langues sous Chrome headless — l'extension Chrome n'étant
  pas connectée, la capture s'est faite en ligne de commande, comme pour `og.png`.

**Fichiers :**
- `website/index.html` — la section `#fonctionnement` (SVG écrit à la main), l'entrée de
  nav, les métadonnées, le paragraphe SSH, le paragraphe pacman / yay, et les libellés
  passés de trois gestionnaires à quatre.
- `website/styles.css` — le bloc `.dia-*`.
- `website/app.js` — 40 clés françaises neuves (37 `dia.*`, plus `nav.how`, `popup.ssh`,
  `facade.arch`), sept clés existantes réécrites, et le couple `TITLE` mis à jour. Le
  dictionnaire passe de 49 à 89 entrées.
- `website/README.md` — une section qui documente les contraintes du schéma.
- `CHANGELOG.md` — première section `[Unreleased]` de ce dépôt.

**Décisions / notes :**
- **Le SVG est stylé par classes, jamais par attribut `style`.** La CSP du vhost pose
  `style-src 'self'`, qui interdit aussi le style en ligne — les attributs de présentation
  (`fill`, `stroke`) ne sont pas concernés, mais passer par `styles.css` accroche en plus
  le schéma aux variables de palette existantes.
- **Chaque libellé du schéma porte un `data-i18n`.** Le remplacement d'`innerHTML` marche
  sur les éléments `<text>` d'un SVG, et sur son `<title>` qui sert de description
  accessible : vérifié au `--dump-dom`. `verifier.sh` compte donc ces trente-sept clés comme
  les autres, sans rien changer au script.
- **Une erreur sémantique corrigée en cours de route :** le canal vert était dessiné en
  pointillés jusqu'au shell. Or seule l'interrogation des gestionnaires est asynchrone ; la
  relecture de la ligne de cache par le hook est immédiate. Le trait plein en haut, les
  pointillés en bas — et la légende le dit.
- **Le site était resté à la v0.12.0 sur le fond** : ni pacman, ni yay, ni le sélecteur
  SSH, et « trois gestionnaires » partout. C'est la copie qui avait pris du retard, pas le
  schéma ; d'où la passe de mise à niveau, faite dans la foulée plutôt que laissée en
  contradiction avec le dessin juste au-dessus.
- **Les captures étaient restées à `jigger 0.10.0`, et `og.png` à `0.9.0`.** Ce sont de
  vraies sorties, que le README du site impose de **rejouer** plutôt que de retoucher, et
  rejouer celle d'Homebrew demande un Mac : laissées en l'état depuis cette machine
  Windows plutôt que faussées. Rafraîchies dans l'intervalle par la session de 14:55
  (`9cdc351`), qui les a reprises en 0.14.1 et leur a ajouté un cadre Arch.
- **`[Unreleased]` est une première ici.** Les dix-sept versions précédentes ont toutes vu
  leur bloc écrit au moment de la coupe, dans un commit « Version X ». La section suit le
  format Keep a Changelog déclaré en tête de fichier, et se fondra dans le bloc de la
  prochaine version ; à supprimer si la coupe reprend l'habitude d'écrire d'un coup.

**Commits :** `ab0e3f0` Le site montre comment jigger fonctionne, et rattrape la v0.14.0,
sur la branche `feat/schema-fonctionnement`, fusionnée dans `main`.

**Note de fusion :** `origin/main` avait avancé de **huit commits** pendant cette session —
la v0.14.1 et les rattrapages du poste Omarchy, poussés depuis une autre machine. La
fusion s'est donc faite sur le `main` à jour, avec deux conflits d'ajouts concurrents,
`CHANGELOG.md` et ce fichier, résolus en gardant les deux côtés. `website/index.html` a
fusionné tout seul : les quatre lignes touchées de l'autre côté sont les versions dans les
captures, que cette session n'avait justement pas touchées.

## 14:28 — Réalisé : jigger installé sur le poste Omarchy, et les deux retards que ça a révélés

**Demande :** installer jigger sur le portable Omarchy, puis corriger les deux écarts
constatés au passage — la suite `zpty` en échec sous Arch, et `getting-started.md` resté
muet sur pacman / yay.

**Actions :**

*Installation du poste (dépôt `config`, hors de ce dépôt-ci)*
- `make install` → `~/.local/bin/jigger` 0.14.0 ; `which -a jigger` n'en voit qu'un,
  `~/.local/bin` est déjà dans le `PATH` par l'`env-bootstrap` d'Omarchy.
- `source` du greffon ajouté dans `config/zsh/.zshrc.linux`, à côté de `parc`, avec la
  même garde `[[ -r … ]]`. Le commentaire qui disait « jigger n'a PAS de pendant sur ce
  poste… son greffon ne parle que de Homebrew » a été remplacé : il datait d'avant la
  0.14.0.
- Vérifié dans un vrai `zsh -i` : `_jigger_insert` définie, `^I` lié à `_jigger_widget`,
  `JIGGER_COMMANDS=brew pacman yay ssh scp sftp`. Les deux `can't change option: zle` du
  démarrage sont antérieurs à jigger — reproduits greffon neutralisé.
- Bloc de prompt **non activé** : `~/.config/starship.toml` appartient à Omarchy sur ce
  poste et le composant `starship` du dépôt `config` en est exclu (son ADR 0012).

*Correction 1 — la suite `zpty` sous Arch*
- Symptôme : 2 assertions sur 15 en échec, « `jf=10` absent », alors qu'aucune régression
  n'avait eu lieu. Le rc du bloc de prompt posait `PS1='jf=${JIGGER_BREW_FORMULAE}'`
  **avant** le `source`, donc un nom choisi d'avance — quand le greffon, lui, décide des
  noms au chargement selon la machine (`_jigger_pm` / `_jigger_c1`, cf. le module pacman).
- Le `PS1` passe après le `source` et se construit sur le choix du greffon, lu par
  indirection : `${(P)${_jigger_test_compteur}:-vide}`. Même règle que pour la langue en
  tête de la suite — on assère la **concordance**, jamais une valeur.
- Le nettoyage de l'environnement hérité couvre désormais les deux familles de variables :
  sous Arch, on ne purgeait pas celle qu'on allait lire.
- Au passage : `print -r -- "\n$failed…"` sortait un `\n` littéral (`-r` n'interprète pas
  les échappements). Remis à la forme de la branche voisine.
- Résultat : **42 `ok`, 0 `FAIL`**, « tout passe ». `go test ./...` vert.

*Correction 2 — les deux `getting-started.md`*
- 16 retouches par langue, EN et FR tenus en parallèle : ligne « Arch Linux » au tableau
  d'en-tête, prérequis, section d'installation propre à Arch, `source` depuis un clone,
  exemples `pacman -S` / `-R` / `-Q --` / `yay -S`, correction `extra/rustup`, pastilles
  ◆ dépôt / ▣ AUR, arbitrage « pacman lit, yay pilote » avec renvoi à l'ADR-0007,
  `JIGGER_CACHE_DIR` sous Linux, ligne de prompt Arch, `pacman.segment.json` et
  `pacman.toml`, noms `JIGGER_PACMAN_*`, symptôme « `jg install` sans yay ».
- Tous les liens relatifs des deux fichiers résolvent ; le nombre de titres reste
  identique entre les deux langues.

**Fichiers :**
- `tests/zpty.zsh` — `PS1` du rc de prompt construit après le `source`, sur `_jigger_pm` /
  `_jigger_c1` ; `unset` des deux familles de variables ; message d'échec sans `\n` littéral.
- `docs/getting-started.md` — pacman / yay documentés de bout en bout.
- `docs/fr/getting-started.md` — idem, en français.
- (hors dépôt) `~/git/config/config/zsh/.zshrc.linux` — le `source` du greffon.

**Décisions / notes :**
- **Deux fichiers de segment existaient déjà** (`shell/oh-my-posh/pacman.segment.json`,
  `shell/starship/pacman.toml`), livrés avec le module pacman mais jamais cités par le
  document d'accueil. C'est ce genre d'écart que la relecture a cherché : le README avait
  suivi la 0.14.0, `getting-started.md` non.
- **Aucun paquet jigger sous Arch**, ni dépôt ni AUR : la route est Go ou les sources.
  C'est écrit noir sur blanc au § 2 plutôt que laissé à deviner — le lecteur d'Arch
  tombait jusqu'ici sur une section « macOS et Linux — par Homebrew » qui ne le concernait
  pas.
- Le `\n` littéral du message d'échec n'était pas dans la demande : signalé à
  l'utilisateur, corrigé faute d'objection, réversible en une ligne.

## 14:36:43 — Demande

> fusionne

## 14:39:18 — Demande

> ajoute la permission

## 14:44:11 — Demande

> publie la v0.14.1

## 14:46:39 — Demande

> oui, refais les captures

## 14:55 — Réalisé : v0.14.1 publiée, puis les captures rafraîchies

**Demande :** publier la v0.14.1, puis refaire les captures restées à d'anciennes
versions.

**Actions :**

*La release*
- `CHANGELOG.md` : une entrée sous `### Fixed`, #93 seule — #92 et #94 sont internes.
- `main.go` → 0.14.1, et les deux captures des README qui affichaient la 0.14.0.
- Commit `52eb7bd`, tag `v0.14.1` posé **après** le commit du CHANGELOG (l'ordre du skill :
  qui récupère le tag doit y trouver son changelog), release GitLab avec notes en français.
- `make install` derrière : le binaire du poste et le greffon du dépôt ne doivent pas
  diverger, c'est la panne que le § 9 du guide décrit.

*Les captures*
- Bannières passées en 0.14.1 : les deux guides (× 2 cadres + la ligne `--version`),
  `website/index.html` (× 2), `website/og.html` (qui partait de la 0.9.0).
- Ajout au § 5 des deux guides d'un cadre réellement capturé ici, `yay -S visual-studio`.

**Fichiers :**
- `CHANGELOG.md`, `main.go`, `README.md`, `README.fr.md` — la release.
- `docs/getting-started.md`, `docs/fr/getting-started.md`, `website/index.html`,
  `website/og.html` — les captures.

**Décisions / notes :**
- **Le refus de « retoucher le numéro » a été tranché par une mesure**, pas par une
  préférence. Il tenait aux v0.13.0, v0.14.0 et v0.14.1 tant qu'on ignorait si le cadre
  avait bougé depuis la 0.10.0. Rendu à `--cols 60`, `jigger render` produit aujourd'hui
  un cadre identique à l'octet : bordure, ligne de titre, colonne de pastilles, ligne
  vide, pied. Seule la bannière était fausse — donc seule elle a changé.
- **Deux cadres restent non recapturables sur ce poste**, et c'est un fait de la machine,
  pas un oubli : « brew install fire » veut un Homebrew (ici le rendu dit « building the
  Homebrew catalog… »), et « jg » veut les douze verbes alors que la façade n'en déclare
  que huit sous yay (ADR-0007) — la capture contredirait sa propre légende. Leurs lignes
  de candidats restent celles du Mac. La géométrie, elle, est vérifiée juste.
- **Le cadre français a été rendu sous `JIGGER_LANG=fr`**, pas fabriqué en substituant
  les libellés anglais : le pied français est plus court d'aplomb que le pied anglais, et
  la première tentative avait élargi le cadre de six colonnes. Mesuré, puis corrigé.
- **Un défaut trouvé au passage, non corrigé :** `runDemo` (`main.go:695`) ne teste que
  `runtime.GOOS == "windows"`. Sur cette machine Arch, `jigger demo` affiche donc un cadre
  « brew install » avec des formules Homebrew — un gestionnaire qui n'y est pas. Le module
  pacman l'a rendu faux sans le toucher. Signalé à l'utilisateur.

## 14:54:09 — Demande

> corrige le demo

## 15:10 — Réalisé : `jigger demo` cesse d'annoncer brew sous Arch

**Demande :** corriger le défaut de `runDemo` signalé au chantier précédent.

**Actions :**
- `runDemo` demande le gestionnaire à `managers.Default()` au lieu de tester
  `runtime.GOOS`. La table des aperçus sort dans `apercu(cmd)`, avec une branche pacman
  (titre « pacman -S », pastilles ◆ dépôt / ▣ AUR, versions au format alpm).
- Trois tests dans `main_test.go`, éprouvés par mutation.
- `runtime` retiré des imports : la branche corrigée en était le dernier usage.
- `make install` derrière, pour que le binaire du poste dise la même chose que le dépôt.

**Fichiers :**
- `main.go` — `runDemo` + `apercu`, et l'import `runtime` en moins.
- `main_test.go` — `TestApercuSuitLeGestionnaire`, `TestApercuReplieSurBrew`,
  `TestDemoMontreLeGestionnaireDeLaMachine`.

**Décisions / notes :**
- **La règle existait déjà, écrite ailleurs.** `managers.Default()` répond exactement à la
  question que `runDemo` se posait — quel gestionnaire cette machine parle-t-elle. Le
  défaut n'était pas un oubli de branche pacman, c'était une règle recopiée à côté de celle
  qui fait autorité : la copie a cessé d'être vraie quand l'originale a changé, en silence.
  Le correctif supprime la copie plutôt que de la compléter.
- **Le titre pacman est « pacman -S », pas « pacman install ».** Chez pacman l'opération
  est un drapeau ; un aperçu qui montrerait une grammaire que le popup ne montre pas
  mentirait deux fois. Même raison que le point de grammaire relevé à la conception du
  module.
- **`apercu` est découpée pour être éprouvable**, pas par goût du découpage : la sortie de
  `runDemo` dépend de ce qui est installé sur la machine qui lance les tests, celle
  d'`apercu` non. C'est la leçon du harnais `zpty` de ce matin, appliquée avant d'avoir mal.
- **Les tests assèrent la concordance, jamais une valeur.** Le titre commence par le mot de
  commande, les pastilles appartiennent au gestionnaire nommé, `runDemo` annonce bien
  `managers.Default()` — vrai sur les trois plateformes, depuis n'importe laquelle. Vérifié
  par mutation : en rétablissant le défaut d'origine, les trois tombent, avec le bon
  message.

## 14:59:30 — Demande

> publie la v0.14.2

## 15:10:21 — Demande

> enchaine

## 15:35 — Réalisé : la v0.15.0, et la garde qui empêchera le prochain retard

**Demande :** publier la v0.14.2, puis enchaîner sur le contrôle des bannières.

**Actions :**

*La release, qui a changé de numéro*
- En rassemblant la matière, un commit de la période s'est révélé **sans issue** :
  `ab0e3f0`, la section `#fonctionnement` du site et son schéma SVG, fait hors du circuit
  de consignation — mais avec son CHANGELOG déjà écrit sous `## [Unreleased]`, deux
  entrées sous « Added ».
- Question posée à l'utilisateur avant de taguer, l'arbitrage lui revenant : **v0.15.0**
  retenue. `ab0e3f0` consigné rétroactivement en #99 puis fermé (son commit était déjà sur
  `main`, aucune MR pour le faire), et #95 à #98 réétiquetées de v0.14.2 en v0.15.0.
- `CHANGELOG.md` : `[Unreleased]` devient `[v0.15.0]`, les trois entrées du site gagnent
  leur `(#99)`, et une section `### Fixed` accueille #95 et #97.
- `main.go` → 0.15.0 et les **treize** bannières avec lui. Commit `b96cb94`, tag
  `v0.15.0`, release GitLab.

*La garde*
- `TestLesBannieresSuiventLaVersion` dans `main_test.go` : lit `version` et exige que les
  treize bannières des six fichiers l'annoncent. Éprouvé par mutation — main.go passé en
  0.16.0 sans toucher aux documents, les treize remontent, chacune avec son fichier.

**Fichiers :**
- `CHANGELOG.md`, `main.go`, les deux README, les deux guides, `website/index.html`,
  `website/og.html` — la release.
- `main_test.go` — la garde.

**Décisions / notes :**
- **Le numéro ne m'appartenait pas.** J'avais annoncé une v0.14.2 sur une information
  incomplète : je ne connaissais pas encore `ab0e3f0`. Un tag public ne se reprend pas, et
  la règle du skill est explicite — le plus fort incrément du lot décide, un ajout impose
  le mineur. Question posée avant l'irréversible, pas après.
- **Un commit sans issue ne se passe pas sous silence.** Une release qui tairait un pan de
  travail est pire qu'une release incomplète assumée : d'où #99, écrite après coup, avec le
  sha dans son commentaire pour que les regroupements futurs le retrouvent.
- **La garde vient d'un constat, pas d'une précaution.** Le retard des bannières était
  connu et signalé dans les notes de trois releases de suite ; il a duré parce qu'il ne
  faisait échouer rien du tout. Et le geste venait de s'élargir de deux bannières à treize
  — c'est quand un geste dépasse la mémoire qu'on le confie à la machine.
- **Effet de bord assumé :** toute release doit maintenant repasser sur les treize
  bannières. Le test le rappellera ; il ne le fera pas à la place de qui publie, ce qui est
  volontaire — poser le numéro reste une décision.
