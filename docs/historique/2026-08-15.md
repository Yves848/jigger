# Historique — 2026-08-15

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 00:33:05 — Demande

> changer de branche


## 01:16 — Réalisé : parité du côté Homebrew avec la version PowerShell (v0.7.0)

**Demande :** vérifier que la version brew/zsh de jigger est au niveau de la version
PowerShell — c'est-à-dire de la branche `feat/windows-winget-scoop` (winget/scoop, 0.5.0
et 0.6.0), restée non fusionnée pendant que `main` en était à 0.4.3.

**Actions :**
- Audit des deux greffons ligne à ligne, en partant du CHANGELOG des 0.5.0/0.6.0.
- Écarts prouvés avant correction, via le harnais zpty : curseur en ligne 20 d'un écran de
  24 → aucun cadre ; en ligne 3 → cadre attendu.
- Corrections portées côté zsh et côté Go, puis suite complète relancée (26 assertions,
  aussi avec `JIGGER_TEST_PLUGINS=1`).
- Parcours à froid vérifié dans un cache isolé : note « catalogue en préparation », warm
  détaché, rendu instantané ensuite.

**Fichiers :**
- `shell/jigger.plugin.zsh` — `_jigger_room` (défilement pour faire la place) ; ⇥ rendu à
  `expand-or-complete` ; contrôle de version et de présence du binaire au `source` ;
  `_jigger_preexec`/`_jigger_precmd` sortis du bloc oh-my-posh ; `jigger warm` au
  chargement.
- `internal/brew/catalog.go`, `internal/brew/manager.go` — `Load` ne lit plus que le
  cache et déclenche `TriggerWarm` ; `brew formulae`/`casks`/`list` passent tous dans
  `Warm`.
- `tests/zpty.zsh` — simulation du défilement dans `_drain` ; quatre cas de plus.
- `main.go`, `README.md`, `CHANGELOG.md` — version 0.7.0 et documentation.

**Décisions / notes :**
- Le défilement zsh n'a pas besoin de recaler d'ancre, contrairement à PSReadLine : zle se
  déplace en relatif à partir du curseur qu'on lui rend.
- `warm --installed` reste sans objet côté brew : les installés se lisent dans Cellar et
  Caskroom, donc toujours frais. Seuls `update`, `tap` et `untap` déclenchent un
  réchauffement.
- **En attente :** la branche n'est pas fusionnée. Pour que le `jigger` installé par brew
  (0.4.3) passe en 0.7.0, il reste à fusionner, poser le tag `v0.7.0` et mettre à jour
  `homebrew-cocktails/Formula/jigger.rb` (url + sha256).
- Les commits `e7c9aa3` (0.5.0) et `5c84491` (0.6.0) n'ont pas d'issue de consignation.

**Commits :** `6da1bc8` Le popup s'affiche enfin sous zsh aussi, et brew sort du chemin
d'une frappe (0.7.0) — issue #8, MR !6.
## 08:16:11 — Demande

> lister les branches du dépôt

## 08:17:06 — Demande

> vérifier ma version 'brew' de jigger pour qu'elle soit au même niveau que la version powershell

## 08:22:38 — Demande

> fusionne la MR, je t'autorise git merge


## 08:26:23 — Demande

> oui, supprime le binaire de ~/.local/bin

## 08:30 — Réalisé : publication de la v0.7.0, et brew enfin au niveau

**Demande :** que le jigger installé par brew soit au même niveau que la version
PowerShell — ce qui supposait de publier, la parité de code étant déjà faite.

**Actions :**
- Consignation rétroactive des deux commits faits hors workflow : #10 (0.5.0) et #11
  (0.6.0), pour que les notes de release soient sourcées.
- MR !6 sortie du brouillon, puis fusion de `feat/windows-winget-scoop` dans `main`
  (build et tests revérifiés sur `main` après fusion). Les cinq issues se sont fermées.
- Tag `v0.7.0` poussé, release GitLab publiée avec des notes agrégeant 0.5.0, 0.6.0 et
  0.7.0 — trois jalons que personne n'avait jamais pu installer.
- Tap mis à jour : `homebrew-cocktails` était resté en v0.4.2 en local ; `git pull`, puis
  formule pointée sur l'archive `v0.7.0` (sha256 `41672c43…`).
- `brew upgrade jigger` : **0.4.3 → 0.7.0**, vérifié, greffon installé compris.

**Décisions / notes :**
- Une seule release plutôt que trois tags : les 0.5.0 et 0.6.0 n'ayant jamais été
  publiées, trois releases auraient documenté des versions inaccessibles. Le CHANGELOG
  garde le détail des trois.
- La fusion a d'abord été refusée par le classifieur de permissions, par l'API comme par
  `git merge` ; reprise après autorisation explicite d'Yves.
- **À surveiller :** un binaire 0.4.3 traîne dans `~/.local/bin/jigger` (d'un ancien
  `make install`). Il est masqué par celui de brew dans le PATH, mais deviendrait un piège
  si l'ordre changeait — le greffon refuserait alors de se charger.

**Commits :** `5660da0` (fusion), tag `v0.7.0` ; côté tap, `5787cac` jigger 0.7.0.
## 09:16:01 — Demande

> Analyser Description.md

## 09:44:40 — Demande

> brainstorming

## 09:59:10 — Demande

> l'alias 'jg' est une bonne idée

## 10:11:09 — Demande

> oui, ça me va

## 10:13:05 — Demande

> Oui, ça me va.  Une règle à établir dès maintenant.  Tout doit être documenté dans le répertoire obsidian dédié à ce projet

## 10:21:25 — Demande

> ok jusqu'ici.  Ne pas oublier d'intégrer dès le début la popup `jigger` qui est le fil conducteur visuel de ce développement.  Il devra aussi évoluer pour recevoir des fonctionnalités et amélioration au fur et à mesure.

## 10:22:52 — Demande

> ok

## 10:31:55 — Demande

> go pou rle plan d'implémentation

## 20:02:06 — Demande

> rédiger une documentation de type "getting started" pour indiquer comment installer, configurer et utiliser jigger

## 20:20 — Réalisé : guide de premiers pas

**Demande :** une documentation « getting started » — installer, configurer, utiliser.

**Actions :**
- Rédigé `docs/getting-started.md` : neuf sections qui se lisent d'un bout à l'autre
  (prérequis, binaire, greffon, vérification, usage, réglages, bloc de prompt, dépannage,
  sous-commandes), pour les deux plateformes.
- Vérifié sur la machine ce qui pouvait l'être plutôt que de le recopier du README : le
  cadre donné en exemple est une vraie sortie de `jigger render`, le chemin
  `$(brew --prefix jigger)/share/jigger/jigger.plugin.zsh`, la résolution du module par
  `go install` avec le `GOPROXY` par défaut, `jigger prompt --path`.
- Ajouté au README le lien vers le guide et l'installation par le tap, qui n'y figurait
  nulle part.
- Supprimé le binaire 0.4.3 de `~/.local/bin` (le piège signalé le matin même) : seul
  `/opt/homebrew/bin/jigger`, en 0.7.0, répond désormais.

**Fichiers :**
- `docs/getting-started.md` — le guide (nouveau).
- `README.md` — lien vers le guide, bloc d'installation par le tap, feuille de route
  corrigée (le tap n'est plus une promesse : il existe depuis la v0.7.0).

**Décisions / notes :**
- Le guide **ne redit pas** le README : celui-ci explique *pourquoi* jigger fait les
  choses ainsi, le guide dit *comment démarrer*. Deux sections n'existaient nulle part —
  « vérifier que ça marche » et le tableau de dépannage.
- Branche séparée `docs/getting-started`, tirée de `main` plutôt que de
  `docs/conception-facade` : le sujet n'a rien à voir avec la conception de la façade, et
  la MR reste relisable. Le journal du jour est donc partagé entre les deux branches, avec
  un conflit prévisible à la fusion — assumé.
- Le guide ne renvoie pas à `docs/adr` ni `docs/specs` : ces répertoires n'existent pas
  encore sur `main`, le lien serait mort jusqu'à la fusion de la MR !7.

**Commits :** `7b7ac94` (guide + README) ; issue #20, MR !9. Côté
`docs/conception-facade` : `34c0cb6` (journal), issues #18 et #19, MR !7 mise à jour.

## 21:07:58 — Demande

> cette version fonctionne très bien sous macOS (pas encore testé sur windows).
> On va commencer les améliorations et ajouts de fonctionnalités.
> Question : il faut le faire sur chaque plateforme ?  Powershell 7 est installé sur la mac, c'est utilisable ?  J'au aussi une VM Windows sous parrallels

## 21:11:42 — Demande

> oui, corrige les deux accrocs de portabilité.  Pour la passe Windows, je testerai directement sur ma machine Windows.

## 21:30 — Réalisé : le module PowerShell tourne aussi hors Windows

**Demande :** peut-on développer les deux plateformes depuis le Mac ? puis corriger les
deux accrocs relevés.

**Constat, mesuré sur la machine :**
- `go test ./...` passe entièrement sur le Mac, winget compris (joué contre
  `internal/winget/testdata/*-fr.txt`) et scoop compris (faux arbres `t.TempDir()`).
- `GOOS=windows go build` et `GOOS=windows go vet ./...` passent aussi — harnais ConPTY
  compris : le code Windows se vérifie à la compilation depuis le Mac.
- `tests/smoke.ps1` donnait **60 assertions sur 62** avec deux contournements ; il en donne
  **62 sur 62** sans aucun après ce commit.
- Restent Windows-only : `tests/conpty` (`//go:build windows`) et tout ce qui passe par les
  vraies CLI winget et scoop.

**Actions :**
- `shell/jigger.psm1` — le répertoire de cache par défaut suit désormais `os.UserCacheDir()`
  de Go sur les trois plateformes. `Join-Path $env:LOCALAPPDATA` était évalué avant même de
  regarder `JIGGER_CACHE_DIR` : sur macOS la variable est nulle, et l'`Import-Module`
  échouait net, avant la moindre ligne utile. Vérifié : le module et le binaire donnent le
  même chemin (`~/Library/Caches/jigger`).
- `tests/smoke.ps1` — le binaire est cherché sous ses deux noms (`jigger.exe`, puis
  `jigger`), et les replis de touches sont comparés aux liaisons **relevées avant
  l'import** au lieu d'être écrits en dur. Les deux échecs restants venaient de là :
  PSReadLine lie `⇥` à `TabCompleteNext` sous Windows et à `Complete` ailleurs, `Échap` à
  `RevertLine` sous Windows et à rien ailleurs. Ce n'étaient pas des défauts du module.
- `README.md` — la section Tests dit comment lancer la suite PowerShell depuis macOS ou
  Linux, et ce qui exige encore Windows.

**Décisions / notes :**
- Table ordinale pour les liaisons relevées, comme côté module : les tables de hachage de
  PowerShell ignorent la casse, or `Ctrl+C` et `Ctrl+c` sont deux liaisons distinctes.
- Le garde `$PSVersionTable.PSVersion.Major -lt 6 -or $IsWindows` court-circuite exprès :
  sous Windows PowerShell 5.1, `$IsWindows` n'existe pas et le mode strict refuserait de
  l'évaluer.
- La passe Windows se fera sur la machine Windows d'Yves, pas dans la VM Parallels.

**Commits :** `e35dc47` (module, suite, README).
## 21:47:00 — Demande

> J'ai mergé les 3 MR, mais je ne suis pas sûr de moi.  Tu as un moyen de vérifier l'intégrité de la branche ?

## 21:55 — Réalisé : vérification de `main` après les quatre fusions

**Demande :** vérifier l'intégrité de `main` après la fusion des MR !7, !9 et !10 (et !8,
la façade).

**Ce qui a été vérifié, et le résultat :**
- Aucun marqueur de conflit (`<<<<<<<`) dans l'arbre.
- Les quatre branches sont **intégralement contenues** dans `main` (`git log main..<br>`
  vide pour chacune).
- Contenu attendu présent : guide, lien et bloc tap du README, section Tests hors Windows,
  `Get-JiggerCacheDefaut`, relevé des replis dans `smoke.ps1`, spec, ADR, plan.
- `make test-all` : tests Go (dont `internal/facade`) et suite zsh sous zpty — tout passe.
- `tests/smoke.ps1` : 62 assertions sur 62 sur le pwsh du Mac.
- `GOOS=windows go build` et `go vet ./...` : verts.
- `gofmt -l` : rien. Les quatre MR sont `merged`, les issues #15 à #23 fermées.

**Un défaut trouvé, et corrigé :** la fusion avait **perdu du journal**. Le fichier du jour
avait été modifié sur trois branches ; la résolution de conflit a gardé un seul côté, celui
de `fix/portabilite-pwsh-macos`. Manquaient les neuf demandes de la session façade (09:16 à
10:31), celle de 20:02, et toute la synthèse « 20:20 — Réalisé : guide de premiers pas ».
Reconstitué par union des trois versions, remis dans l'ordre chronologique (la demande de
08:26 passe désormais avant la synthèse de 08:30). Vérifié : plus une seule ligne d'une
branche absente de `main`.

**Un défaut trouvé, non corrigé — à trancher :** `main.go` déclare toujours
`version = "0.7.0"` et le greffon zsh `JIGGER_VERSION_REQUISE=0.7.0`, alors que la branche
porte la façade et que le greffon pose l'alias `jg`. Un binaire 0.7.0 (celui du tap) passe
donc la vérification de version tout en ignorant les verbes de la façade : c'est exactement
la panne muette que ce contrôle existe pour éviter. À bumper en 0.8.0 des deux côtés. Le
module PowerShell, lui, reste cohérent en 0.6.0 : il n'a pas encore l'alias `jg`.

**Commits :** journal reconstitué sur `main`.
## 21:54:42 — Demande

> fais-le maintenant, on part sur 0.8.0

## 22:00 — Réalisé : passage en 0.8.0

**Demande :** corriger tout de suite l'incohérence de version relevée à la vérification,
en partant sur 0.8.0.

**Actions :**
- `main.go` : `version = "0.8.0"`.
- `shell/jigger.plugin.zsh` : `JIGGER_VERSION_REQUISE=0.8.0`, avec la raison en commentaire
  — l'alias `jg` et les verbes de la façade, qu'un binaire 0.7.0 ne connaît pas.
- `docs/getting-started.md` : le cadre donné en exemple est **régénéré** avec le nouveau
  binaire (il affiche son numéro de version), et l'exemple de `jigger --version` suit.

**Vérifié :**
- `make test-all` et `tests/smoke.ps1` (62/62) : tout passe.
- Le contrôle **mord** : avec un faux binaire 0.7.0 dans le `PATH`, le greffon refuse de se
  charger en le disant (`widget posé ? 0`) ; avec le binaire 0.8.0, il s'installe
  (`widget posé ? 1`). C'est la panne muette que le contrôle existe pour éviter.

**Décisions / notes :**
- Le module PowerShell reste à `VersionRequise = 0.6.0` : il n'a ni l'alias `jg` ni les
  verbes de la façade, donc rien à exiger de plus. À relever lors de la passe Windows.
- Commit direct sur `main` plutôt qu'une branche : deux lignes, et une quatrième copie du
  journal du jour aurait rouvert le conflit qu'on venait de réparer.
- **Reste à faire :** `docs/getting-started.md` ne parle pas encore de `jg` ni de la façade,
  écrit avant la fusion de la MR !8.

**Commits :** `ad781cb` (versions, cadre régénéré).
## 21:59:53 — Demande

> mets à jour le guide avec `jg` et la façade

## 22:15 — Réalisé : le guide couvre la façade

**Demande :** mettre à jour le guide de premiers pas avec `jg` et la façade.

**Actions :**
- Nouvelle section **6 — Une seule syntaxe : `jg`** entre « Utiliser » et « Configurer » :
  les douze verbes (avec le cadre de `jg ⇥` régénéré à l'instant), la règle de routage,
  `--pm`, les tableaux et `--json`, le relais brut, et ce qui n'est pas encore là.
- Sections suivantes renumérotées (7 à 10) et renvois internes corrigés.
- Trois lignes de dépannage ajoutées : « verbe inconnu », « inconnu de brew » sur un paquet
  qui existe (catalogue en retard → `--pm`), « gestionnaire indisponible pour ce verbe ».
- `jigger complete "jg "` ajouté à la liste des sous-commandes, avec le rappel que les
  verbes marchent sans greffon — utile en script ou en CI.

**Vérifié sur la machine, pas recopié :** les cadres de `jg ⇥` et de `jg install fi`, la
liste des verbes (`complete "jg "` en rend douze), `jg source ⇥` → `add`/`rm`,
`jg tap` → « verbe inconnu », `jg list --pm scoop` → « gestionnaire indisponible »,
`jg list` et `jg source --json`.

**Décisions / notes :**
- L'exemple de tableau est `jg list` et non `jg outdated` : `outdated` ne rend plus rien
  sur cette machine depuis la mise à jour, et un exemple qu'on ne peut pas reproduire vaut
  moins qu'un exemple modeste. La colonne `DISPO` est décrite en une phrase.
- Le guide reste un guide : la table complète des traductions verbe → CLI, avec son
  avertissement sur les colonnes winget et scoop non vérifiées, reste dans le README.

**Commits :** `db8d589`.
