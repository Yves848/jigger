# Historique — 2026-08-15

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 00:33:05 — Demande

> changer de branche


## 01:16 — Réalisé : parité du côté Homebrew avec la version PowerShell (v0.7.0)

**Demande :** vérifier que la version brew/zsh de jigger est au niveau de la version
PowerShell — c'est-à-dire de la branche `feat/windows-winget-scoop` (winget/scoop, 0.5.0
et 0.6.0), restée non fusionnée pendant que `main` en était à 0.4.3.

**Actions :**
- Audit des deux greffons ligne à ligne, en partant du CHANGELOG des 0.5.0/0.6.0.
- Écarts prouvés avant correction, via le harnais zpty : curseur en ligne 20 d'un écran de
  24 → aucun cadre ; en ligne 3 → cadre attendu.
- Corrections portées côté zsh et côté Go, puis suite complète relancée (26 assertions,
  aussi avec `JIGGER_TEST_PLUGINS=1`).
- Parcours à froid vérifié dans un cache isolé : note « catalogue en préparation », warm
  détaché, rendu instantané ensuite.

**Fichiers :**
- `shell/jigger.plugin.zsh` — `_jigger_room` (défilement pour faire la place) ; ⇥ rendu à
  `expand-or-complete` ; contrôle de version et de présence du binaire au `source` ;
  `_jigger_preexec`/`_jigger_precmd` sortis du bloc oh-my-posh ; `jigger warm` au
  chargement.
- `internal/brew/catalog.go`, `internal/brew/manager.go` — `Load` ne lit plus que le
  cache et déclenche `TriggerWarm` ; `brew formulae`/`casks`/`list` passent tous dans
  `Warm`.
- `tests/zpty.zsh` — simulation du défilement dans `_drain` ; quatre cas de plus.
- `main.go`, `README.md`, `CHANGELOG.md` — version 0.7.0 et documentation.

**Décisions / notes :**
- Le défilement zsh n'a pas besoin de recaler d'ancre, contrairement à PSReadLine : zle se
  déplace en relatif à partir du curseur qu'on lui rend.
- `warm --installed` reste sans objet côté brew : les installés se lisent dans Cellar et
  Caskroom, donc toujours frais. Seuls `update`, `tap` et `untap` déclenchent un
  réchauffement.
- **En attente :** la branche n'est pas fusionnée. Pour que le `jigger` installé par brew
  (0.4.3) passe en 0.7.0, il reste à fusionner, poser le tag `v0.7.0` et mettre à jour
  `homebrew-cocktails/Formula/jigger.rb` (url + sha256).
- Les commits `e7c9aa3` (0.5.0) et `5c84491` (0.6.0) n'ont pas d'issue de consignation.

**Commits :** `6da1bc8` Le popup s'affiche enfin sous zsh aussi, et brew sort du chemin
d'une frappe (0.7.0) — issue #8, MR !6.
## 08:16:11 — Demande

> lister les branches du dépôt

## 08:17:06 — Demande

> vérifier ma version 'brew' de jigger pour qu'elle soit au même niveau que la version powershell

## 08:22:38 — Demande

> fusionne la MR, je t'autorise git merge


## 08:26:23 — Demande

> oui, supprime le binaire de ~/.local/bin

## 08:30 — Réalisé : publication de la v0.7.0, et brew enfin au niveau

**Demande :** que le jigger installé par brew soit au même niveau que la version
PowerShell — ce qui supposait de publier, la parité de code étant déjà faite.

**Actions :**
- Consignation rétroactive des deux commits faits hors workflow : #10 (0.5.0) et #11
  (0.6.0), pour que les notes de release soient sourcées.
- MR !6 sortie du brouillon, puis fusion de `feat/windows-winget-scoop` dans `main`
  (build et tests revérifiés sur `main` après fusion). Les cinq issues se sont fermées.
- Tag `v0.7.0` poussé, release GitLab publiée avec des notes agrégeant 0.5.0, 0.6.0 et
  0.7.0 — trois jalons que personne n'avait jamais pu installer.
- Tap mis à jour : `homebrew-cocktails` était resté en v0.4.2 en local ; `git pull`, puis
  formule pointée sur l'archive `v0.7.0` (sha256 `41672c43…`).
- `brew upgrade jigger` : **0.4.3 → 0.7.0**, vérifié, greffon installé compris.

**Décisions / notes :**
- Une seule release plutôt que trois tags : les 0.5.0 et 0.6.0 n'ayant jamais été
  publiées, trois releases auraient documenté des versions inaccessibles. Le CHANGELOG
  garde le détail des trois.
- La fusion a d'abord été refusée par le classifieur de permissions, par l'API comme par
  `git merge` ; reprise après autorisation explicite d'Yves.
- **À surveiller :** un binaire 0.4.3 traîne dans `~/.local/bin/jigger` (d'un ancien
  `make install`). Il est masqué par celui de brew dans le PATH, mais deviendrait un piège
  si l'ordre changeait — le greffon refuserait alors de se charger.

**Commits :** `5660da0` (fusion), tag `v0.7.0` ; côté tap, `5787cac` jigger 0.7.0.
## 09:16:01 — Demande

> Analyser Description.md

## 09:44:40 — Demande

> brainstorming

## 09:59:10 — Demande

> l'alias 'jg' est une bonne idée

## 10:11:09 — Demande

> oui, ça me va

## 10:13:05 — Demande

> Oui, ça me va.  Une règle à établir dès maintenant.  Tout doit être documenté dans le répertoire obsidian dédié à ce projet

## 10:21:25 — Demande

> ok jusqu'ici.  Ne pas oublier d'intégrer dès le début la popup `jigger` qui est le fil conducteur visuel de ce développement.  Il devra aussi évoluer pour recevoir des fonctionnalités et amélioration au fur et à mesure.

## 10:22:52 — Demande

> ok

## 10:31:55 — Demande

> go pou rle plan d'implémentation

## 20:02:06 — Demande

> rédiger une documentation de type "getting started" pour indiquer comment installer, configurer et utiliser jigger

## 20:20 — Réalisé : guide de premiers pas

**Demande :** une documentation « getting started » — installer, configurer, utiliser.

**Actions :**
- Rédigé `docs/getting-started.md` : neuf sections qui se lisent d'un bout à l'autre
  (prérequis, binaire, greffon, vérification, usage, réglages, bloc de prompt, dépannage,
  sous-commandes), pour les deux plateformes.
- Vérifié sur la machine ce qui pouvait l'être plutôt que de le recopier du README : le
  cadre donné en exemple est une vraie sortie de `jigger render`, le chemin
  `$(brew --prefix jigger)/share/jigger/jigger.plugin.zsh`, la résolution du module par
  `go install` avec le `GOPROXY` par défaut, `jigger prompt --path`.
- Ajouté au README le lien vers le guide et l'installation par le tap, qui n'y figurait
  nulle part.
- Supprimé le binaire 0.4.3 de `~/.local/bin` (le piège signalé le matin même) : seul
  `/opt/homebrew/bin/jigger`, en 0.7.0, répond désormais.

**Fichiers :**
- `docs/getting-started.md` — le guide (nouveau).
- `README.md` — lien vers le guide, bloc d'installation par le tap, feuille de route
  corrigée (le tap n'est plus une promesse : il existe depuis la v0.7.0).

**Décisions / notes :**
- Le guide **ne redit pas** le README : celui-ci explique *pourquoi* jigger fait les
  choses ainsi, le guide dit *comment démarrer*. Deux sections n'existaient nulle part —
  « vérifier que ça marche » et le tableau de dépannage.
- Branche séparée `docs/getting-started`, tirée de `main` plutôt que de
  `docs/conception-facade` : le sujet n'a rien à voir avec la conception de la façade, et
  la MR reste relisable. Le journal du jour est donc partagé entre les deux branches, avec
  un conflit prévisible à la fusion — assumé.
- Le guide ne renvoie pas à `docs/adr` ni `docs/specs` : ces répertoires n'existent pas
  encore sur `main`, le lien serait mort jusqu'à la fusion de la MR !7.

**Commits :** `7b7ac94` (guide + README) ; issue #20, MR !9. Côté
`docs/conception-facade` : `34c0cb6` (journal), issues #18 et #19, MR !7 mise à jour.

## 21:07:58 — Demande

> cette version fonctionne très bien sous macOS (pas encore testé sur windows).
> On va commencer les améliorations et ajouts de fonctionnalités.
> Question : il faut le faire sur chaque plateforme ?  Powershell 7 est installé sur la mac, c'est utilisable ?  J'au aussi une VM Windows sous parrallels

## 21:11:42 — Demande

> oui, corrige les deux accrocs de portabilité.  Pour la passe Windows, je testerai directement sur ma machine Windows.

## 21:30 — Réalisé : le module PowerShell tourne aussi hors Windows

**Demande :** peut-on développer les deux plateformes depuis le Mac ? puis corriger les
deux accrocs relevés.

**Constat, mesuré sur la machine :**
- `go test ./...` passe entièrement sur le Mac, winget compris (joué contre
  `internal/winget/testdata/*-fr.txt`) et scoop compris (faux arbres `t.TempDir()`).
- `GOOS=windows go build` et `GOOS=windows go vet ./...` passent aussi — harnais ConPTY
  compris : le code Windows se vérifie à la compilation depuis le Mac.
- `tests/smoke.ps1` donnait **60 assertions sur 62** avec deux contournements ; il en donne
  **62 sur 62** sans aucun après ce commit.
- Restent Windows-only : `tests/conpty` (`//go:build windows`) et tout ce qui passe par les
  vraies CLI winget et scoop.

**Actions :**
- `shell/jigger.psm1` — le répertoire de cache par défaut suit désormais `os.UserCacheDir()`
  de Go sur les trois plateformes. `Join-Path $env:LOCALAPPDATA` était évalué avant même de
  regarder `JIGGER_CACHE_DIR` : sur macOS la variable est nulle, et l'`Import-Module`
  échouait net, avant la moindre ligne utile. Vérifié : le module et le binaire donnent le
  même chemin (`~/Library/Caches/jigger`).
- `tests/smoke.ps1` — le binaire est cherché sous ses deux noms (`jigger.exe`, puis
  `jigger`), et les replis de touches sont comparés aux liaisons **relevées avant
  l'import** au lieu d'être écrits en dur. Les deux échecs restants venaient de là :
  PSReadLine lie `⇥` à `TabCompleteNext` sous Windows et à `Complete` ailleurs, `Échap` à
  `RevertLine` sous Windows et à rien ailleurs. Ce n'étaient pas des défauts du module.
- `README.md` — la section Tests dit comment lancer la suite PowerShell depuis macOS ou
  Linux, et ce qui exige encore Windows.

**Décisions / notes :**
- Table ordinale pour les liaisons relevées, comme côté module : les tables de hachage de
  PowerShell ignorent la casse, or `Ctrl+C` et `Ctrl+c` sont deux liaisons distinctes.
- Le garde `$PSVersionTable.PSVersion.Major -lt 6 -or $IsWindows` court-circuite exprès :
  sous Windows PowerShell 5.1, `$IsWindows` n'existe pas et le mode strict refuserait de
  l'évaluer.
- La passe Windows se fera sur la machine Windows d'Yves, pas dans la VM Parallels.

**Commits :** `e35dc47` (module, suite, README).
## 21:47:00 — Demande

> J'ai mergé les 3 MR, mais je ne suis pas sûr de moi.  Tu as un moyen de vérifier l'intégrité de la branche ?

## 21:55 — Réalisé : vérification de `main` après les quatre fusions

**Demande :** vérifier l'intégrité de `main` après la fusion des MR !7, !9 et !10 (et !8,
la façade).

**Ce qui a été vérifié, et le résultat :**
- Aucun marqueur de conflit (`<<<<<<<`) dans l'arbre.
- Les quatre branches sont **intégralement contenues** dans `main` (`git log main..<br>`
  vide pour chacune).
- Contenu attendu présent : guide, lien et bloc tap du README, section Tests hors Windows,
  `Get-JiggerCacheDefaut`, relevé des replis dans `smoke.ps1`, spec, ADR, plan.
- `make test-all` : tests Go (dont `internal/facade`) et suite zsh sous zpty — tout passe.
- `tests/smoke.ps1` : 62 assertions sur 62 sur le pwsh du Mac.
- `GOOS=windows go build` et `go vet ./...` : verts.
- `gofmt -l` : rien. Les quatre MR sont `merged`, les issues #15 à #23 fermées.

**Un défaut trouvé, et corrigé :** la fusion avait **perdu du journal**. Le fichier du jour
avait été modifié sur trois branches ; la résolution de conflit a gardé un seul côté, celui
de `fix/portabilite-pwsh-macos`. Manquaient les neuf demandes de la session façade (09:16 à
10:31), celle de 20:02, et toute la synthèse « 20:20 — Réalisé : guide de premiers pas ».
Reconstitué par union des trois versions, remis dans l'ordre chronologique (la demande de
08:26 passe désormais avant la synthèse de 08:30). Vérifié : plus une seule ligne d'une
branche absente de `main`.

**Un défaut trouvé, non corrigé — à trancher :** `main.go` déclare toujours
`version = "0.7.0"` et le greffon zsh `JIGGER_VERSION_REQUISE=0.7.0`, alors que la branche
porte la façade et que le greffon pose l'alias `jg`. Un binaire 0.7.0 (celui du tap) passe
donc la vérification de version tout en ignorant les verbes de la façade : c'est exactement
la panne muette que ce contrôle existe pour éviter. À bumper en 0.8.0 des deux côtés. Le
module PowerShell, lui, reste cohérent en 0.6.0 : il n'a pas encore l'alias `jg`.

**Commits :** journal reconstitué sur `main`.
## 21:54:42 — Demande

> fais-le maintenant, on part sur 0.8.0

## 22:00 — Réalisé : passage en 0.8.0

**Demande :** corriger tout de suite l'incohérence de version relevée à la vérification,
en partant sur 0.8.0.

**Actions :**
- `main.go` : `version = "0.8.0"`.
- `shell/jigger.plugin.zsh` : `JIGGER_VERSION_REQUISE=0.8.0`, avec la raison en commentaire
  — l'alias `jg` et les verbes de la façade, qu'un binaire 0.7.0 ne connaît pas.
- `docs/getting-started.md` : le cadre donné en exemple est **régénéré** avec le nouveau
  binaire (il affiche son numéro de version), et l'exemple de `jigger --version` suit.

**Vérifié :**
- `make test-all` et `tests/smoke.ps1` (62/62) : tout passe.
- Le contrôle **mord** : avec un faux binaire 0.7.0 dans le `PATH`, le greffon refuse de se
  charger en le disant (`widget posé ? 0`) ; avec le binaire 0.8.0, il s'installe
  (`widget posé ? 1`). C'est la panne muette que le contrôle existe pour éviter.

**Décisions / notes :**
- Le module PowerShell reste à `VersionRequise = 0.6.0` : il n'a ni l'alias `jg` ni les
  verbes de la façade, donc rien à exiger de plus. À relever lors de la passe Windows.
- Commit direct sur `main` plutôt qu'une branche : deux lignes, et une quatrième copie du
  journal du jour aurait rouvert le conflit qu'on venait de réparer.
- **Reste à faire :** `docs/getting-started.md` ne parle pas encore de `jg` ni de la façade,
  écrit avant la fusion de la MR !8.

**Commits :** `ad781cb` (versions, cadre régénéré).
## 21:59:53 — Demande

> mets à jour le guide avec `jg` et la façade

## 22:15 — Réalisé : le guide couvre la façade

**Demande :** mettre à jour le guide de premiers pas avec `jg` et la façade.

**Actions :**
- Nouvelle section **6 — Une seule syntaxe : `jg`** entre « Utiliser » et « Configurer » :
  les douze verbes (avec le cadre de `jg ⇥` régénéré à l'instant), la règle de routage,
  `--pm`, les tableaux et `--json`, le relais brut, et ce qui n'est pas encore là.
- Sections suivantes renumérotées (7 à 10) et renvois internes corrigés.
- Trois lignes de dépannage ajoutées : « verbe inconnu », « inconnu de brew » sur un paquet
  qui existe (catalogue en retard → `--pm`), « gestionnaire indisponible pour ce verbe ».
- `jigger complete "jg "` ajouté à la liste des sous-commandes, avec le rappel que les
  verbes marchent sans greffon — utile en script ou en CI.

**Vérifié sur la machine, pas recopié :** les cadres de `jg ⇥` et de `jg install fi`, la
liste des verbes (`complete "jg "` en rend douze), `jg source ⇥` → `add`/`rm`,
`jg tap` → « verbe inconnu », `jg list --pm scoop` → « gestionnaire indisponible »,
`jg list` et `jg source --json`.

**Décisions / notes :**
- L'exemple de tableau est `jg list` et non `jg outdated` : `outdated` ne rend plus rien
  sur cette machine depuis la mise à jour, et un exemple qu'on ne peut pas reproduire vaut
  moins qu'un exemple modeste. La colonne `DISPO` est décrite en une phrase.
- Le guide reste un guide : la table complète des traductions verbe → CLI, avec son
  avertissement sur les colonnes winget et scoop non vérifiées, reste dans le README.

**Commits :** `db8d589`.
## 22:08:46 — Demande

> publie la v0.8.0

## 22:20 — Réalisé : publication de la v0.8.0

**Demande :** publier la v0.8.0.

**Actions, dans l'ordre qui compte :**
- Croisement des commits depuis `v0.7.0` avec les issues consignées ; les quatre issues
  encore étiquetées `v0.7.1` (#15, #16, #18, #19) réétiquetées `v0.8.0`.
- `CHANGELOG.md` : section v0.8.0 (Ajouté / Corrigé / Ce qui n'est pas encore là), agrégée
  depuis les issues #17, #20, #22, #25 et #27 — les `type::interne` écartées. Commit
  `9cfdd28`, poussé **avant** le tag : ce qu'un tag ne contient pas au moment où on le pose,
  il ne le contiendra jamais.
- Tag `v0.8.0` posé et poussé, après `go build` et `go test ./...`.
- [Release GitLab v0.8.0](https://gitlab.yg-devworks.com/yves/jigger/-/releases/v0.8.0)
  publiée, notes tirées du CHANGELOG (lien du guide rendu absolu, une page de release
  n'ayant pas de chemin relatif).
- Tap : formule pointée sur l'archive `v0.8.0`, sha256 `6cb5f289…` — vérifié **stable sur
  deux téléchargements**, l'archive GitLab étant générée à la volée. Commit `3d16b78`.
  Les caveats mentionnent désormais `jg`.
- `brew upgrade jigger` : **0.7.0 → 0.8.0**.

**Vérifié après coup :** le greffon livré par brew est bien celui qui exige 0.8.0 et pose
l'alias `jg` ; dans un zsh neuf, le widget s'installe (`widget: 1`), l'alias est là, et
`_jigger_commands` vaut `brew jigger jg`. `jigger complete "jg "` rend les douze verbes.

**Décisions / notes :**
- Le test de la formule (`jigger complete "inst"` → `install`) a été rejoué contre le
  binaire 0.8.0 **avant** de publier la formule : la façade aurait pu changer l'aiguillage
  d'une ligne sans premier mot. Elle ne l'a pas fait.
- Les commits d'implémentation de la façade ne sont pas consignés un par un : l'issue #17
  couvre la branche entière. C'est le seul écart au workflow sur cette release.

**Commits :** `9cfdd28` (CHANGELOG), tag `v0.8.0` ; côté tap, `3d16b78`.
## 22:12 — Demande

> comme pour oh-my-posh, il faut un segment pour starship

## 22:13:16 — Demande

> testé sous windows sur mon dell XPS, ça fonctionne pour les 2 : Winget et Scoop

## 22:16:25 — Demande

> jigger source et jigger list ne produisent que les résultats liés à winget

## 22:18:51 — Demande

> jigger list --pm scoop 2>&1 répond "rien à signaler"

## 22:25:04 — Demande

> jigger fonctionne bien avec scoop.  La popup apparaît bien pour la commande scoop

## 22:26:28 — Demande

> je dois le faire sur le XPS, ça ira ?

## 22:30 — Réalisé : un segment starship, et pourquoi il ne s'affichait pas

**Demande :** doter starship du même bloc de prompt qu'oh-my-posh.

**Actions :**
- `shell/starship/brew.toml` et `shell/starship/windows.toml` : trois modules `env_var`
  par plateforme, sur les mêmes variables, les mêmes émojis et les mêmes couleurs que les
  segments oh-my-posh. `symbol` et `style` séparés du `format`, pour que les deux choses
  qu'on retouche soient chacune sur leur ligne.
- Le § « Bloc oh-my-posh » du README devient « Bloc de prompt » et couvre les deux
  (étapes 2a/2b) ; ancre corrigée dans le guide, `$comment` des deux segments oh-my-posh
  repointés sur le nouveau titre.
- Le bloc ne s'affichait toujours pas dans le terminal. **Diagnostic, pas de tâtonnement :**
  `$env:JIGGER_PROMPT` n'était nulle part dans le `$PROFILE` — le hook du module est
  désactivé par défaut, il n'exportait donc aucune variable — et `~/.config/starship.toml`
  n'existait pas du tout, starship tournant sur ses seuls défauts.
- Corrigé hors dépôt jigger : réglage posé avant l'`Import-Module` dans
  `D:\config\powershell`, et `~/.config/starship.toml` créé depuis `windows.toml`.

**Vérifié — starship 1.24.2 installé sur la machine, rien recopié de la documentation :**
- Rendu dans les cinq états : tout affiché, version seule, rien du tout, et le cas
  scoop-seul des deux fichiers.
- `${env_var}` avec accolades : `$env_var` suivi d'une lettre est avalé comme un nom de
  module inconnu — piège trouvé à l'essai, pas deviné.
- `$all` par défaut contient bien `$env_var` (`starship print-config`) : rien à ajouter au
  `format`.
- La chaîne complète, hook compris : `Update-JiggerPrompt` exporte les quatre variables,
  et starship rend `🪟 1.29.280  📦 45  🥄 1`.
- L'init mis en cache par le profil relance le binaire à chaque prompt : `starship.toml`
  est relu à chaque fois, aucune purge de cache nécessaire.

**Décisions / notes :**
- **Écart assumé avec le segment oh-my-posh**, noté dans `windows.toml` : celui-ci suspend
  tout le bloc à `JIGGER_WINGET_VERSION`, si bien qu'une machine qui n'a que scoop
  n'affiche rien. Les trois modules starship étant indépendants, elle affiche sa cuillère
  seule. C'est mieux, et c'est délibéré.
- Aucune condition à écrire côté starship : un module dont la variable n'est pas définie
  ne s'affiche pas. La règle « un compteur à zéro n'est pas exporté » fait tout le travail,
  là où oh-my-posh demandait un `{{ if }}`.
- Le dépôt `D:\config` ignorait le bloc jigger du profil : il vivait dans le fichier de
  travail sans avoir jamais été commité, alors que ce dépôt est censé être la source de
  vérité. Il y entre en entier, pas seulement la ligne `JIGGER_PROMPT`.
- `~/.config/starship.toml` reste **hors dépôt**, suivi nulle part. À déplacer dans
  `D:\config` avec un lien symbolique, comme le profil pwsh, si on veut le versionner.

**Commits :** `ac9653a` ; côté configuration, `562593c` dans `D:\config`.

## 22:30:00 — Demande

> Crées la branche ici et ajoute un fichier .ps1 avec le code qui va bien pour générer les traces que tu utiliseras ici.  Attention, j'ai généré du code pour starship sur le XPS.  Il va falloir fetch/pull avant

## 22:35 — Réalisé : outil de capture pour la passe Windows

**Demande :** créer la branche ici et y ajouter un script `.ps1` qui génère les traces à
exploiter ensuite sur le Mac.

**Diagnostic établi avec Yves, depuis le XPS :** `jigger source` et `jigger list` ne rendent
que winget. `jigger list --pm scoop` répond « rien à signaler » — donc ni la détection
(sinon « gestionnaire indisponible ») ni l'exécution (aucune ligne sur stderr) : ce sont bien
les analyseurs de `internal/scoop/parse.go`, écrits à l'aveugle. Le chemin natif n'est pas en
cause : le popup sur `scoop …` lit le disque et ne lance jamais scoop.

**Actions :**
- Branche `fix/scoop-parsers`, tirée de `main` après `git fetch` (rien de neuf sur origin :
  `origin/main` est resté à `1457cc8`).
- `tests/captures-scoop.ps1` : capture `scoop list`, `bucket list` et `search git` dans
  `internal/scoop/testdata/` sous les noms qu'attend `parse_test.go`, plus `scoop help`,
  `scoop update --help` et les deux aides winget dans `tests/captures/`, avec un fichier de
  contexte (versions, système).
- `.gitattributes` : les captures scoop rejoignent la règle winget, aucune normalisation des
  fins de ligne.
- Issue #30, MR !11 (en brouillon tant que les captures manquent).

**Décisions / notes :**
- **La capture se fait derrière un tuyau**, pas depuis une console : `Format-Table` s'accorde
  à la largeur de sortie, et jigger ne voit jamais la version « console ». Une capture prise
  dans un terminal large aurait produit un jeu d'essai que jigger ne rencontrera jamais —
  c'est-à-dire le même défaut qu'on répare, sous une autre forme.
- La commande est résolue en **exécutable** (`Get-Command -CommandType Application`), comme
  `exec.Command` : si scoop n'existait que comme fonction PowerShell, le script le dirait, et
  ce serait un diagnostic à part entière.
- Script éprouvé sur le Mac dans les deux chemins : scoop absent (message, code 1) et faux
  scoop rendant des tableaux (fichiers écrits, contexte, résumé).
- À faire ensuite, une fois les captures poussées : réécrire les trois analyseurs, faire
  **échouer** un analyseur qui ne retrouve pas la structure du tableau, vérifier les tables de
  verbes contre les aides, puis lever les réserves du README, du guide et des notes v0.8.0.

**Commits :** `4cd675d`.## 22:55 — Demande

> il faut merger la modif starship et la pousser sur le dépôt pour récupérer sur le mac

## 23:00 — Réalisé : le segment starship sur `main`

**Demande :** merger la branche et la pousser, pour récupérer le segment sur le Mac.

**Actions :**
- Le distant avait bougé depuis le début de la séquence — quatre commits (CHANGELOG
  v0.8.0, journal, réécriture du guide) et le tag `v0.8.0`. `main` fast-forwardé,
  `feat/segment-starship` rebasée dessus : pas de conflit, mais la section du bloc de
  prompt était passée du § 7 au § 8 en amont.
- Un commit ajouté au passage : le § 3 du guide ne nommait qu'oh-my-posh pour la
  contrainte d'ordre sous PowerShell, alors que la raison vaut identiquement pour
  starship — `prompt` est le seul « precmd » disponible, et jigger enveloppe celui qui est
  en place.
- Merge `--no-ff` puis poussée. `go test ./...` avant.

**Décisions / notes :**
- Branche plutôt que commit direct sur `main` : c'est un ajout de fonctionnalité, pas les
  deux lignes du passage en 0.8.0.
- `brew upgrade jigger` ne suffira pas à récupérer le fichier sur le Mac : le tap installe
  le greffon zsh sous `share/`, pas les segments — ni ceux d'oh-my-posh, ni les nouveaux.
  Il faut le dépôt cloné. À verser à la passe suivante si on veut que le tap les livre.
- Sur le Mac, le second geste est celui qui a manqué ici : `JIGGER_PROMPT=1` dans
  `~/.zshrc`, **avant** le `source` du greffon.

**Commits :** `992e923`, merge `f644981`, poussé sur `origin/main`.

## 23:01 — Demande

> oui, fais le changelog et le journal

## 23:08:08 — Demande

> vérifie la branche

## 23:10 — Réalisé : CHANGELOG et journal

**Demande :** consigner le segment starship au CHANGELOG et au journal.

**Actions :**
- `CHANGELOG.md` : section **« Non publié »** (Ajouté / Modifié) en tête.
- `docs/historique/2026-08-15.md` : les trois entrées ci-dessus.

**Décisions / notes :**
- **« Non publié » plutôt qu'une v0.8.1**, contrairement au précédent de la v0.4.2 (un
  changement de segment seul, qui avait bumpé `main.go`) : la v0.8.0 vient d'être publiée
  — tag, release GitLab, formule du tap, `brew upgrade` — et écrire un numéro que rien ne
  tague ferait mentir le fichier. Le format Keep a Changelog, que le fichier déclare
  suivre, prévoit exactement cette section. Le bump de `main.go` reste ce qu'il a toujours
  été ici : son propre commit, au moment de publier.
- Rien ne change dans le binaire : la version qu'il annonce n'a pas à bouger pour deux
  fichiers de configuration et de la documentation.
- Les entrées « Demande » de cette séquence n'ont pas été consignées par le hook ; leurs
  heures sont reconstituées d'après les commits, à la minute.

**Commits :** celui qui porte ce fichier.
## 23:20 — Réalisé : vérification de `main` après le segment starship

**Demande :** vérifier la branche après le travail fait sur le XPS.

**État :** `origin/main` avait avancé de quatre commits (segment starship, doc, CHANGELOG) ;
`fix/scoop-parsers` n'a rien reçu — les captures scoop ne sont pas encore poussées.

**Contrôles, tous verts :**
- Aucun marqueur de conflit ; `feat/segment-starship` intégralement contenue dans `main`.
- Journal intact : les sections reconstituées ce soir (09:16, 20:02, et la synthèse « guide
  de premiers pas ») ont survécu aux fusions du XPS.
- `make test-all`, `tests/smoke.ps1` (62/62), `GOOS=windows go build` et `go vet`, `gofmt` :
  rien à signaler.
- Ancres cohérentes : le § devenu « Bloc de prompt » est bien celui que vise le guide, plus
  aucune référence à l'ancienne ancre.
- CHANGELOG en `## [Non publié]` : aucune version annoncée, donc aucun tag à poser et
  `main.go` reste en 0.8.0 à juste titre.

**Vérification fonctionnelle du segment**, starship 1.26.0 étant installé sur le Mac : les
deux fichiers ont été rendus pour de vrai (`starship prompt`, `STARSHIP_CONFIG` pointé
dessus). `brew.toml` avec les trois variables donne `🍺 6.0.17 🧪 7 📦 2` ; avec la seule
version, les compteurs disparaissent ; sans rien, le bloc entier s'efface. `windows.toml`
donne `🪟 1.29.280 📦 48 🥄 1`, et `🥄 1` seul quand seule la variable scoop est posée —
exactement ce que le CHANGELOG met en avant. Aucun `format` n'a été ajouté : le `$all` par
défaut contient bien `$env_var`.

**Piège rencontré, à retenir :** le premier essai a été faussé par mon propre environnement,
qui exporte déjà `JIGGER_BREW_*`. Les modules semblaient ne jamais disparaître. Il faut
`env -u` pour juger de l'absence d'une variable.

**Un manque, non corrigé :** le travail starship n'est **pas consigné** — ni issue, ni MR, ni
branche distante ; quatre commits sont arrivés sur `main` hors du workflow habituel. Le
contenu n'est pas perdu (la section « Non publié » du CHANGELOG le porte), mais la
traçabilité l'est. Consignation rétroactive proposée à Yves, comme cela avait été fait pour
les 0.5.0 et 0.6.0.

**Commits :** aucun côté code.
## 23:51:01 — Demande

> oui, consigne-les rétroactivement

## 23:55 — Réalisé : consignation rétroactive du segment starship

**Demande :** consigner rétroactivement les commits starship arrivés sur `main` hors
workflow.

**Actions :**
- Issues #33 (`type::ajout`, `ac9653a`), #34 (`type::modification`, `992e923`) et #35
  (`type::interne`, `68ca4a4`), créées puis fermées aussitôt — elles sont sur `main`, donc
  sans MR possible. Chacune porte la mention qu'elle est rétroactive : sans cela, l'absence
  de MR ressemblerait plus tard à une perte de trace qu'à un choix. Le commit de fusion
  `f644981` n'a pas d'issue : il ne porte aucun contenu propre.
- #29 à #32 réétiquetées de `v0.8.1` en `v0.9.0` : le lot contient un `type::ajout`, donc
  l'incrément est mineur, et les sept issues d'après la v0.8.0 forment un groupe cohérent.
- Vérifié : tout commit hors fusion depuis le tag `v0.8.0` a désormais son issue.

**Décisions / notes :**
- Relevé en rédigeant #33, et consigné là où on le relira au bon moment : **le tap ne livre
  pas `shell/starship`**. La formule installe le greffon zsh et `shell/oh-my-posh`, et
  l'archive `v0.8.0` ne contient pas encore le répertoire starship. La ligne
  `pkgshare.install "shell/starship"` est à ajouter **au prochain tag** — l'ajouter avant
  casserait la compilation de la formule actuelle.

**Commits :** aucun côté code.
