# Historique — 2026-09-07

> Journal des demandes de la journée. Les entrées « Demande » sont consignées automatiquement (hook) ; les sections « Réalisé » sont ajoutées par le skill `historiser`.

## 00:06:09 — Demande

> d'autres améliorations sympas ?

## 00:08:44 — Demande

> 3, 4 et 5

## 00:23:53 — Demande

> commit push PR and merge

## 00:20 — Réalisé : notifications, titre d'onglet, seconde passe sur la barre d'état

**Demande :** appliquer trois des améliorations proposées — canal de notification, titre
d'onglet automatique, et enrichissement de la barre d'état.

**Actions :**
- `preferredNotifChannel: "ghostty"`, valeur vérifiée : l'union fermée
  `auto · iterm2 · terminal_bell · iterm2_with_bell · kitty · ghostty · notifications_disabled`
  apparaît deux fois dans le binaire. Les événements associés couvrent
  `permission_prompt`, `idle_prompt`, `agent_needs_input`, `agent_completed` et
  `quota_auto_resume_fired` — ce dernier prévient de la réouverture d'un quota épuisé.
- `terminalTitleFromRename: true` : le titre de l'onglet suit le nom de la session.
- Barre d'état, seconde passe : compte à rebours de remise à zéro des quotas (issu du
  `resets_at` jusque-là inexploité), marqueur `wt` pour les worktrees rattachés, style de
  sortie affiché seulement s'il n'est plus `default`. Neuf cas de test, dont la charge
  vide et un worktree monté sur un dépôt jetable.

**Fichiers :**
- `~/.claude/statusline.sh` — seconde version (la précédente conservée en `.v1`)
- `~/.claude/settings.json` — `preferredNotifChannel`, `terminalTitleFromRename`

**Décisions / notes :** le compte à rebours n'apparaît qu'au-delà de 60 % de quota
consommé — en dessous il occuperait de la place pour annoncer qu'il n'y a rien à
surveiller. Trois paliers, parce que le premier essai affichait `↻113h20` pour le quota
hebdomadaire : `4j16` au-delà de deux jours, `2h13` au-delà d'une heure, `45min` sinon.
Une durée qui demande un calcul mental ne sert à rien dans une barre d'état.

Le worktree se détecte en comparant `git rev-parse --git-dir` et `--git-common-dir`, qui
ne diffèrent que dans un worktree rattaché : c'est le seul test fiable.

Deux propositions restent en attente : les raccourcis AZERTY dans `keybindings.json`
(`ctrl+]` est inatteignable sur un clavier français Apple) et le recul de l'auto-compaction
via `autoCompactWindow`. La fouille du binaire a par ailleurs livré une table fiable des
clés de réglages, dont `language` (interface en français), `vimInsertModeRemaps`
(pertinent avec `editorMode: normal` sur AZERTY), `spinnerVerbs`, `showTurnDuration`,
`showMessageTimestamps` et `terminalProgressBarEnabled` — trouvées, volontairement pas
appliquées.

**Note :** ce travail porte sur `~/.claude/`, qui n'est pas versionné. Seul le journal est
commité ici.
## 18:05:15 — Demande

> supprime la branche et versionne ~/.claude

## 18:09:48 — Demande

> /login

## 18:32:50 — Demande

> sors le brouillon et fusionne

## 18:38:03 — Demande

> la popup n'apparait plus pour la commande SSH

## 18:49:12 — Demande

> le camp développement, et ouvre l'issue

## 18:54:35 — Demande

> [Image #5] Il y a un plugin de zsh qui parasite.  Ca fonctionne, mais c'est perturbant


## 19:15 — Réalisé : la suggestion de zsh-autosuggestions ne survit plus à l'insertion

**Demande :** un greffon zsh parasite l'affichage — ça fonctionne, mais c'est perturbant.
Capture à l'appui : `ssh aquariumdebian13`.

**Actions :**
- Cause trouvée par lecture du code plutôt que par tâtonnement : le greffon ne mentionne
  `POSTDISPLAY` **nulle part**. `_jigger_insert` écrit `LBUFFER` directement, et
  zsh-autosuggestions ne recalcule sa proposition qu'aux widgets qu'il enveloppe — les
  nôtres n'en sont pas. La suggestion calculée pour `ssh ` survit donc à l'insertion.
- `POSTDISPLAY=''` aux deux endroits qui réécrivent `LBUFFER` : `_jigger_insert`, point
  unique de ⇥ et ⏎, et le chemin du sélecteur plein écran.
- Cas ajouté à `tests/zpty.zsh --suite`, avec zsh-autosuggestions et une histoire d'une
  seule ligne.

**Fichiers :**
- `shell/jigger.plugin.zsh` — `POSTDISPLAY=''` aux deux sites
- `tests/zpty.zsh` — `_jigger_suggest_rc` et le cas « suggestion périmée »

**Décisions / notes :** `POSTDISPLAY` plutôt que `ZSH_AUTOSUGGEST_CLEAR_WIDGETS`. Le
tableau est l'intégration « officielle », mais il doit être garni **avant**
l'initialisation de l'autre greffon — une contrainte d'ordre que le `.zshrc` d'un
utilisateur ne peut pas garantir. La variable zle, elle, existe toujours dans un widget,
et l'effacer ne coûte rien quand aucun greffon ne l'utilise.

Le test a été éprouvé **dans les deux sens**, ce qui est le seul moyen de savoir qu'il
sert à quelque chose : sans le correctif il échoue en nommant la collision
(`aquariumdebian13`, la chaîne même de la capture), avec le correctif il passe. Un test de
non-régression qu'on n'a jamais vu rougir n'est qu'une ligne de plus.

Deux assertions et non une : la première vérifie que la suggestion est bien **là** avant
l'insertion. Sans elle, une machine sans zsh-autosuggestions — ou une histoire vide —
ferait passer le cas sans rien éprouver.

**Trouvé en chemin, sans rapport :** `TestExportNEmetQueLeFichier` échoue quand
`JIGGER_ROWS` est posé dans l'environnement, ce qui est le cas du `.zshrc` de ce poste. Le
code est juste, c'est l'isolation du test qui manque — et le sujet même de ce test est la
préséance de l'environnement sur le fichier. Consigné en #186, non corrigé ici.
## 19:17:58 — Demande

> remettre la situation en ordre. Commit et push + publication et installation.  Un seul binaire sur la machine locale, ce sera celui de homebrew.


## 19:30 — Réalisé : la v0.22.0 est publiée, et il ne reste qu'un jigger sur la machine

**Demande :** remettre la situation en ordre — commit, push, publication, installation.
Un seul binaire local, celui de Homebrew.

**Actions :**
- Section `v0.22.0` du `CHANGELOG`, écrite **avant** le tag : la CI y lit les notes de
  version au moment du tag et refuse de publier si la section manque. Extraction simulée
  avec l'`awk` du pipeline avant de commiter — 65 lignes, non vide.
- Labels de version réalignés sur `v0.22.0` (#176, #184, #185, #186 portaient `v0.21.1`).
- Tag `v0.22.0` poussé, pipeline vert, release publiée avec ses cinq archives.
- Formule du tap mise à jour, empreinte calculée sur l'archive téléchargée et vérifiée sur
  **deux tirages** — les archives GitLab sont produites à la volée.
- `brew upgrade jigger` : 0.21.0 → 0.22.0, formule reliée. Puis, **et seulement ensuite**,
  suppression de `~/.local/bin/jigger` et de l'artefact du dépôt.

**Fichiers :**
- `CHANGELOG.md` — section `v0.22.0`
- `~/git/homebrew-cocktails/Formula/jigger.rb` — url et sha256

**Décisions / notes :** l'ordre des gestes n'était pas indifférent. Installer d'abord,
supprimer ensuite : l'ordre inverse aurait rouvert la fenêtre où aucun jigger n'est dans le
PATH — celle-là même qui désarme le greffon pour toute la vie du shell (#184), et qui a
coûté le diagnostic de la soirée.

Trois vérifications que la convention du tap impose et qui ont toutes tenu : les trois
chemins installés par la formule existent dans l'archive publiée, les « twelve verbs » des
`caveats` sont toujours douze (comptés sur `jigger complete "jg "`), et les trois
assertions du bloc `test` passent sur le binaire installé.

Le camp est désormais **publié** des deux côtés, greffon compris. Le prix est assumé : un
chantier ne se voit plus dans ce shell sans publier. En échange, ce qui est publié est ce
qu'on utilise tous les jours — la seule façon fiable de s'apercevoir qu'une version l'est
mal.

**Commits :** `2ca683f` CHANGELOG : la v0.22.0 · tag `v0.22.0`
## 19:34:21 — Demande

> corrige #186 et #184


## 19:50 — Réalisé : les deux garde-fous cessent de mordre en silence

**Demande :** corriger #186 et #184.

**Actions :**
- **#186** — `neutraliseEnvironnement(t)` parcourt `Declares` et blanchit chaque variable
  déclarée pour la durée du test. Posée dans les deux **aides partagées** du paquet
  (`exporteEtRelit`, `ecrireConfig`) plutôt qu'au cas par cas : le prochain réglage ajouté
  au catalogue sera couvert sans que personne y pense.
- **#184** — les deux vérifications d'installation (binaire joignable, version suffisante)
  passent du chargement à l'usage. Le greffon se charge et s'arme toujours ; il consulte
  `_jigger_utilisable` aux deux seuls endroits qui lancent le binaire.
- Six assertions neuves dans la suite `zpty`, trois pour chaque comportement.

**Fichiers :**
- `internal/config/environnement_test.go` — l'aide (nouveau)
- `internal/config/export_test.go`, `internal/config/duree_test.go` — un appel chacun
- `shell/jigger.plugin.zsh` — `_jigger_utilisable`, `_jigger_plainte`, et le
  déclenchement aux deux points d'appel
- `tests/zpty.zsh` — les trois cas de #184

**Décisions / notes :** trois granularités de mémorisation, chacune répondant à un coût
précis. Le verdict **positif** est gardé pour toujours : une fois le binaire vu et sa
version acceptée, le coût retombe à une comparaison d'entier. Le verdict **négatif** ne
vaut que pour la ligne en cours — sans cette borne, un binaire trop ancien ferait payer un
`--version`, donc un sous-processus, à chaque frappe. La ligne est la bonne granularité :
un `make install` ou un `brew link` se fait entre deux invites, jamais au milieu d'une
frappe. La **plainte**, enfin, est dite une fois par message et non par shell : si le
binaire réapparaît puis se révèle trop ancien, la seconde raison mérite d'être dite.

`zle -M` et non `print -u2` depuis un widget : écrire sur stderr pendant l'édition abîme la
ligne. C'est aussi ce qui fait que le message arrive **là où l'utilisateur regarde**, au
moment où il tape une commande surveillée — et non au démarrage du shell, où il défile.

Le second garde-fou, celui de version, souffrait exactement du même défaut sans qu'aucune
issue ne le dise. Les traiter séparément aurait laissé la moitié du piège en place.
## 20:14:13 — Demande

> publie la 0.22.1


## 20:20 — Réalisé : la v0.22.1 est publiée et installée

**Demande :** publier la 0.22.1.

**Actions :**
- Section `v0.22.1` du `CHANGELOG`, écrite avant le tag ; extraction simulée avec l'`awk`
  du pipeline (47 lignes, non vide).
- Labels de version corrigés : #184 et #186 portaient `v0.22.0`, or ils sont arrivés
  **après** ce tag. Ils passent en `v0.22.1`.
- Tag poussé, pipeline vert, release publiée avec ses cinq archives.
- Formule du tap en `v0.22.1`, empreinte vérifiée sur deux tirages.
- `brew upgrade jigger` : 0.22.0 → 0.22.1.

**Fichiers :**
- `CHANGELOG.md` — section `v0.22.1`
- `~/git/homebrew-cocktails/Formula/jigger.rb` — url et sha256

**Décisions / notes :** une vérification ajoutée au rituel du tap, et elle a servi : avant
de figer l'empreinte, le greffon a été **extrait de l'archive publiée** pour confirmer
qu'il portait bien les deux correctifs — `POSTDISPLAY` effacé aux deux sites (#185) et
`_jigger_utilisable` présent (#184). Vérifier l'URL et le condensat ne dit rien de ce que
l'archive contient ; c'est pourtant la seule chose qui compte pour qui l'installe.

La même vérification a été refaite sur le greffon **installé**, après `brew upgrade` :
deux occurrences de `POSTDISPLAY`, cinq de `_jigger_utilisable`, et zéro « Greffon
inactif » — la phrase qui signait l'ancien abandon au chargement.

Enfin, la popup a été éprouvée dans un pseudo-terminal sur la pile **publiée** et non celle
du dépôt : l'en-tête du cadre affiche `jigger 0.22.1`. C'est tout l'objet du camp publié —
ce qui tourne est ce qui est distribué.

**Commits :** `a5cdd21` CHANGELOG : la v0.22.1 · tag `v0.22.1`
## 20:26:30 — Demande

> nouvelle issue : créer un service d'automatisation de la promotion de jigger sur X, Instagram, ..... et tout médium où il est possible d'automatiser.

## 20:30:27 — Demande

> un compte projet jigger

## 20:32:21 — Demande

> réserve les identifiants sur mastodon et bluesky

## 20:36:21 — Demande

> mastodon.social, et prépare le rel="me"


## 20:45 — Réalisé : la preuve réciproque du profil Mastodon

**Demande :** réserver les identifiants sur Mastodon et Bluesky ; instance retenue
`mastodon.social`, et préparer le `rel="me"`.

**Actions :**
- Reconnaissance en lecture seule des identifiants, avant toute création : `@jigger` est
  libre sur les quatre instances Mastodon regardées ; `jigger.bsky.social` est **pris**,
  mais `jigger.yg-devworks.com` est libre.
- `<link rel="me">` posé dans l'en-tête de `website/index.html`, vers
  `https://mastodon.social/@jigger`.

**Fichiers :**
- `website/index.html` — le lien de vérification

**Décisions / notes :** je n'ai pas créé les comptes, et ce n'est pas un renoncement
technique. Deux prérequis s'y opposent, dont aucun n'est de mon ressort : la boîte
`jigger@yg-devworks.com` n'existe pas encore, et les deux plateformes vérifient l'adresse
par courriel ; et créer un compte, c'est accepter des conditions d'utilisation — un acte
contractuel au nom d'Yves.

La reconnaissance a fait tomber une urgence que j'avais moi-même écrite. Bluesky accepte
comme identifiant **un domaine qu'on contrôle**, prouvé par un `TXT` sur `_atproto.`. Le
sous-domaine du site convient : `@jigger.yg-devworks.com` ne peut être squatté par
personne. La « course à l'identifiant » que je présentais comme l'étape 0 n'existe donc
pas de ce côté-là.

Le lien est écrit **avant** que le compte existe, ce qui est délibéré et légèrement
inconfortable : sur `mastodon.social`, l'URL d'un profil est entièrement déterminée par
l'instance et le nom. Elle est donc connue d'avance. La contrepartie est réelle et vaut
d'être dite : si quelqu'un prend `@jigger` avant l'inscription, ce lien désigne le compte
de quelqu'un d'autre. C'est un argument pour s'inscrire vite, pas pour attendre.

`<link rel="me">` dans l'en-tête plutôt qu'un lien visible en pied de page : Mastodon se
contente du premier, et le second aurait demandé une clé i18n et une décision de mise en
page qui n'ont rien à voir avec la vérification. À faire séparément si l'on veut le lien
visible.

Le site se déploie par `website/deploy-proxmox.sh`, à la main : la vérification ne
fonctionnera qu'une fois ce script lancé.
## 20:52:15 — Demande

> où créer l'adresse jigger@yg-devworks.com ?

## 20:56:08 — Demande

> [Image #6]

## 21:04:45 — Demande

> l'alias est créé et le mail est arrivé


## 21:10 — Réalisé : l'avatar du projet et les textes de profil

**Demande :** l'alias `jigger@yg-devworks.com` est créé et fonctionne.

**Actions :**
- Avatar rendu depuis `website/jigger-icon.svg` — Mastodon et Bluesky n'acceptent pas le
  SVG. Deux tentatives avant d'y arriver proprement.
- `docs/annonces/profils.md` : nom affiché, bio et métadonnées pour Mastodon et Bluesky,
  écrits et versionnés comme les annonces voisines.

**Fichiers :**
- `website/jigger-avatar.png` — 512 × 512, coins transparents (nouveau)
- `docs/annonces/profils.md` — les textes (nouveau)
- `docs/annonces/README.md` — pourquoi ce fichier n'est pas dans le tableau des annonces

**Décisions / notes :** deux fausses pistes valent d'être consignées, parce qu'elles se
seraient vues trop tard. `qlmanage` rend le SVG en une image **entièrement blanche** — un
aperçu générique, pas un rendu ; l'erreur ne se voit qu'en échantillonnant les pixels, pas
à l'œil sur une vignette. Et Chrome sans `--default-background-color=00000000` peint sa
page en blanc derrière le SVG : les coins arrondis deviennent un halo blanc, invisible sur
fond clair et criant sur le thème sombre de Mastodon. C'est pourquoi le PNG est vérifié
par échantillonnage de pixels — fond, cadre, trait, coins — et non par un coup d'œil.

Le rendu dérive de l'SVG plutôt que d'une transcription à la main dans Pillow, qui aurait
été plus simple et aurait créé une **seconde source de vérité** : le jour où l'icône change,
un avatar transcrit ne suit pas, et personne ne s'en aperçoit.

Le lien du code des profils pointe **GitHub et non le GitLab de référence**. Ce n'est pas
une préférence : `docs/annonces/README.md` consigne qu'un lien vers `gitlab.yg-devworks.com`
a fait rejeter un post sans motif le 17 août. La même prudence vaut pour un profil, qui est
lu par les mêmes automates.

Pas de bannière : `og.png` est en 1200 × 630 et les bannières attendent du 3:1. Une
bannière ratée se remarque plus qu'une bannière absente.
